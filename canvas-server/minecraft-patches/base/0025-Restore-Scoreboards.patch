From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Dueris <duerismc@gmail.com>
Date: Thu, 14 May 2026 17:14:48 -0700
Subject: [PATCH] Restore Scoreboards


diff --git a/io/papermc/paper/threadedregions/RegionizedServer.java b/io/papermc/paper/threadedregions/RegionizedServer.java
index 5e0987b248ae14591d607167cc6bbddeae38672f..0dc7a5e628d681398d3a4a2a56b03e87d2c8283d 100644
--- a/io/papermc/paper/threadedregions/RegionizedServer.java
+++ b/io/papermc/paper/threadedregions/RegionizedServer.java
@@ -361,6 +361,7 @@ public final class RegionizedServer {
         // connection tick
         // ender pearls
         // gui tick
+        // scoreboard
 
         // tick player ping sample
         this.tickPlayerSample();
@@ -382,6 +383,9 @@ public final class RegionizedServer {
 
         // gui tick
         net.minecraft.server.gui.PlayerListComponent.tick();
+
+        // scoreboard
+        MinecraftServer.getServer().getScoreboard().autosave();
         // Canvas end - region threading
     }
 
diff --git a/net/minecraft/commands/Commands.java b/net/minecraft/commands/Commands.java
index ad764d57dd28f3c97faf9b2edd9bfee526da6a17..6658e0c0b3f0f6b7d5c8c2d0ffb35437b335d5d0 100644
--- a/net/minecraft/commands/Commands.java
+++ b/net/minecraft/commands/Commands.java
@@ -238,7 +238,7 @@ public class Commands {
         RotateCommand.register(this.dispatcher);
         SayCommand.register(this.dispatcher);
         //ScheduleCommand.register(this.dispatcher); // Folia - region threading
-        //ScoreboardCommand.register(this.dispatcher, context); // Folia - region threading
+        ScoreboardCommand.register(this.dispatcher, context);
         SeedCommand.register(this.dispatcher, commandSelection != Commands.CommandSelection.INTEGRATED);
         VersionCommand.register(this.dispatcher, commandSelection != Commands.CommandSelection.INTEGRATED);
         SetBlockCommand.register(this.dispatcher, context);
@@ -250,15 +250,15 @@ public class Commands {
         StopwatchCommand.register(this.dispatcher);
         SummonCommand.register(this.dispatcher, context);
         //TagCommand.register(this.dispatcher); // Folia - region threading - TODO later
-        //TeamCommand.register(this.dispatcher, context); // Folia - region threading - TODO later
-        //TeamMsgCommand.register(this.dispatcher); // Folia - region threading - TODO later
+        TeamCommand.register(this.dispatcher, context);
+        TeamMsgCommand.register(this.dispatcher);
         TeleportCommand.register(this.dispatcher);
         TellRawCommand.register(this.dispatcher, context);
         //TestCommand.register(this.dispatcher, context); // Folia - region threading
         //TickCommand.register(this.dispatcher); // Folia - region threading - TODO later
         TimeCommand.register(this.dispatcher, context);
         TitleCommand.register(this.dispatcher, context);
-        //TriggerCommand.register(this.dispatcher); // Folia - region threading - TODO later
+        TriggerCommand.register(this.dispatcher);
         WaypointCommand.register(this.dispatcher, context);
         WeatherCommand.register(this.dispatcher);
         WorldBorderCommand.register(this.dispatcher);
diff --git a/net/minecraft/network/protocol/game/ClientboundSetPlayerTeamPacket.java b/net/minecraft/network/protocol/game/ClientboundSetPlayerTeamPacket.java
index ca45c72bf070e14b57454b7ed8b3998ce755dc81..6a13c530dbd59c3c42d5d53162128426e77d7b53 100644
--- a/net/minecraft/network/protocol/game/ClientboundSetPlayerTeamPacket.java
+++ b/net/minecraft/network/protocol/game/ClientboundSetPlayerTeamPacket.java
@@ -207,7 +207,7 @@ public class ClientboundSetPlayerTeamPacket implements Packet<ClientGamePacketLi
             ComponentSerialization.TRUSTED_STREAM_CODEC.encode(output, this.displayName);
             output.writeByte(this.options);
             Team.Visibility.STREAM_CODEC.encode(output, this.nametagVisibility);
-            Team.CollisionRule.STREAM_CODEC.encode(output, !io.papermc.paper.configuration.GlobalConfiguration.get().collisions.enablePlayerCollisions ? PlayerTeam.CollisionRule.NEVER : this.collisionRule); // Paper - Configurable player collision
+            Team.CollisionRule.STREAM_CODEC.encode(output, this.collisionRule); // Paper - Configurable player collision // Canvas - region threading
             output.writeEnum(this.color);
             ComponentSerialization.TRUSTED_STREAM_CODEC.encode(output, this.playerPrefix);
             ComponentSerialization.TRUSTED_STREAM_CODEC.encode(output, this.playerSuffix);
diff --git a/net/minecraft/server/MinecraftServer.java b/net/minecraft/server/MinecraftServer.java
index 28917976f2cfa1df934e7fef30ee5ecd11085b78..0dab3dbd546986b5a8454b5260f20f2602e83f51 100644
--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -674,19 +674,7 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
 
     // Paper start - rework world loading process
     protected void initPostWorld() {
-        // Paper start - Configurable player collision; Handle collideRule team for player collision toggle
-        final ServerScoreboard scoreboard = this.getScoreboard();
-        final java.util.Collection<String> toRemove = scoreboard.getPlayerTeams().stream().filter(team -> team.getName().startsWith("collideRule_")).map(net.minecraft.world.scores.PlayerTeam::getName).collect(java.util.stream.Collectors.toList());
-        for (String teamName : toRemove) {
-            scoreboard.removePlayerTeam(scoreboard.getPlayerTeam(teamName)); // Clean up after ourselves
-        }
-
-        if (!io.papermc.paper.configuration.GlobalConfiguration.get().collisions.enablePlayerCollisions) {
-            this.getPlayerList().collideRuleTeamName = org.apache.commons.lang3.StringUtils.left("collideRule_" + java.util.concurrent.ThreadLocalRandom.current().nextInt(), 16);
-            net.minecraft.world.scores.PlayerTeam collideTeam = scoreboard.addPlayerTeam(this.getPlayerList().collideRuleTeamName);
-            collideTeam.setSeeFriendlyInvisibles(false); // Because we want to mimic them not being on a team at all
-        }
-        // Paper end - Configurable player collision; Handle collideRule team for player collision toggle
+        // Canvas - region threading
         this.server.enablePlugins(org.bukkit.plugin.PluginLoadOrder.POSTWORLD);
         this.server.spark.registerCommandBeforePlugins(this.server); // Paper - spark
         this.server.spark.enableAfterPlugins(this.server); // Paper - spark
diff --git a/net/minecraft/server/ServerScoreboard.java b/net/minecraft/server/ServerScoreboard.java
index 9bf09b888425e491072864265c8de3885154a0ba..7cb6d183be1a04c4f59b2d335fc706c1645deb9f 100644
--- a/net/minecraft/server/ServerScoreboard.java
+++ b/net/minecraft/server/ServerScoreboard.java
@@ -26,24 +26,45 @@ import org.jspecify.annotations.Nullable;
 
 public class ServerScoreboard extends Scoreboard {
     private final MinecraftServer server;
-    private final Set<Objective> trackedObjectives = Sets.newHashSet();
-    private boolean dirty;
+    // Canvas start - region threading
+    private final Set<Objective> trackedObjectives = new java.util.concurrent.CopyOnWriteArraySet<>();
+    private final java.util.concurrent.atomic.AtomicBoolean dirty = new java.util.concurrent.atomic.AtomicBoolean(false);
+
+    private static final java.util.concurrent.atomic.AtomicLong LAST_AUTOSAVE = new java.util.concurrent.atomic.AtomicLong(System.nanoTime());
+
+    public void autosave() {
+        ensureMainThread("Cannot autosave async");
+        // autosave every minute if dirty
+        final int autoSavePeriod = MinecraftServer.getServer().autosavePeriod;
+        if (autoSavePeriod <= 0) return;
+        final long periodNanos = autoSavePeriod * io.papermc.paper.threadedregions.TickRegionScheduler.getTimeBetweenTicks();
+        final long currTime = System.nanoTime();
+        final long last = LAST_AUTOSAVE.get();
+        if ((currTime - last) > periodNanos && LAST_AUTOSAVE.compareAndSet(last, currTime) && dirty.compareAndExchange(true, false)) {
+            MinecraftServer.getServer().getDataStorage().computeIfAbsent(ScoreboardSaveData.TYPE).setData(this.store());
+            MinecraftServer.getServer().getDataStorage().scheduleSave();
+        }
+    }
+    // Canvas end - region threading
 
     public ServerScoreboard(final MinecraftServer server) {
         this.server = server;
     }
 
     public void load(final ScoreboardSaveData.Packed data) {
-        data.objectives().forEach(x$0 -> this.loadObjective(x$0));
-        data.scores().forEach(x$0 -> this.loadPlayerScore(x$0));
+        // Canvas start - region threading
+        data.objectives().forEach(this::loadObjective);
+        data.scores().forEach(this::loadPlayerScore);
+        // Canvas end - region threading
         data.displaySlots().forEach((slot, name) -> {
             Objective objective = this.getObjective(name);
             this.setDisplayObjective(slot, objective);
         });
-        data.teams().forEach(x$0 -> this.loadPlayerTeam(x$0));
+        data.teams().forEach(this::loadPlayerTeam); // Canvas - region threading
     }
 
     private ScoreboardSaveData.Packed store() {
+        ensureMainThread("Cannot pack display data async"); // Canvas - region threading
         return new ScoreboardSaveData.Packed(this.packObjectives(), this.packPlayerScores(), this.packDisplaySlots(), this.packPlayerTeams());
     }
 
@@ -90,6 +111,7 @@ public class ServerScoreboard extends Scoreboard {
 
     @Override
     public void setDisplayObjective(final DisplaySlot slot, final @Nullable Objective objective) {
+        ensureMainThread("Cannot modify display objective async"); // Canvas - region threading
         Objective old = this.getDisplayObjective(slot);
         super.setDisplayObjective(slot, objective);
         if (old != objective && old != null) {
@@ -163,12 +185,14 @@ public class ServerScoreboard extends Scoreboard {
 
     @Override
     public void onObjectiveAdded(final Objective objective) {
+        ensureMainThread("Cannot add objective async"); // Canvas - region threading
         super.onObjectiveAdded(objective);
         this.setDirty();
     }
 
     @Override
     public void onObjectiveChanged(final Objective objective) {
+        ensureMainThread("Cannot change objective async"); // Canvas - region threading
         super.onObjectiveChanged(objective);
         if (this.trackedObjectives.contains(objective)) {
             this.broadcastAll(new ClientboundSetObjectivePacket(objective, ClientboundSetObjectivePacket.METHOD_CHANGE)); // CraftBukkit
@@ -179,6 +203,7 @@ public class ServerScoreboard extends Scoreboard {
 
     @Override
     public void onObjectiveRemoved(final Objective objective) {
+        ensureMainThread("Cannot remove objective async"); // Canvas - region threading
         super.onObjectiveRemoved(objective);
         if (this.trackedObjectives.contains(objective)) {
             this.stopTrackingObjective(objective);
@@ -189,6 +214,7 @@ public class ServerScoreboard extends Scoreboard {
 
     @Override
     public void onTeamAdded(final PlayerTeam team) {
+        ensureMainThread("Cannot add player team async"); // Canvas - region threading
         super.onTeamAdded(team);
         this.broadcastAll(ClientboundSetPlayerTeamPacket.createAddOrModifyPacket(team, true)); // CraftBukkit
         this.setDirty();
@@ -196,6 +222,7 @@ public class ServerScoreboard extends Scoreboard {
 
     @Override
     public void onTeamChanged(final PlayerTeam team) {
+        ensureMainThread("Cannot change player team async"); // Canvas - region threading
         super.onTeamChanged(team);
         this.broadcastAll(ClientboundSetPlayerTeamPacket.createAddOrModifyPacket(team, false)); // CraftBukkit
         this.updateTeamWaypoints(team);
@@ -204,6 +231,7 @@ public class ServerScoreboard extends Scoreboard {
 
     @Override
     public void onTeamRemoved(final PlayerTeam team) {
+        ensureMainThread("Cannot remove player team async"); // Canvas - region threading
         super.onTeamRemoved(team);
         this.broadcastAll(ClientboundSetPlayerTeamPacket.createRemovePacket(team)); // CraftBukkit
         this.updateTeamWaypoints(team);
@@ -211,12 +239,14 @@ public class ServerScoreboard extends Scoreboard {
     }
 
     protected void setDirty() {
-        this.dirty = true;
+        this.dirty.set(true); // Canvas - region threading
     }
 
     public void storeToSaveDataIfDirty(final ScoreboardSaveData saveData) {
-        if (this.dirty) {
-            this.dirty = false;
+        // Canvas start - region threading
+        ensureMainThread("Cannot save display data async");
+        if (this.dirty.compareAndExchange(true, false)) {
+        // Canvas end - region threading
             saveData.setData(this.store());
         }
     }
@@ -269,6 +299,7 @@ public class ServerScoreboard extends Scoreboard {
     }
 
     public void stopTrackingObjective(final Objective objective) {
+        ensureMainThread("Cannot stop tracking objective async"); // Canvas - region threading
         List<Packet<?>> packets = this.getStopTrackingPackets(objective);
 
         for (ServerPlayer player : this.server.getPlayerList().getPlayers()) {
@@ -293,19 +324,23 @@ public class ServerScoreboard extends Scoreboard {
         return count;
     }
 
+    // note: only called on add/remove
     private void updatePlayerWaypoint(final String player) {
         ServerPlayer serverPlayer = this.server.getPlayerList().getPlayerByName(player);
         if (serverPlayer != null) {
+            // this is fine, remakeConnections schedules an update to all players
             serverPlayer.level().getWaypointManager().remakeConnections(serverPlayer);
         }
     }
 
+    // note: only called on team changed/remove
     private void updateTeamWaypoints(final PlayerTeam team) {
         for (ServerLevel level : this.server.getAllLevels()) {
             team.getPlayers()
                 .stream()
                 .map(name -> this.server.getPlayerList().getPlayerByName(name))
                 .filter(Objects::nonNull)
+                // this is fine, remakeConnections schedules an update to all players
                 .forEach(player -> level.getWaypointManager().remakeConnections(player));
         }
     }
diff --git a/net/minecraft/server/commands/ScoreboardCommand.java b/net/minecraft/server/commands/ScoreboardCommand.java
index a4fa358bf6bb3c84a024620ee77f90b6ac68089b..6ad5b3a48efec788604a854677de4df74a9b71cf 100644
--- a/net/minecraft/server/commands/ScoreboardCommand.java
+++ b/net/minecraft/server/commands/ScoreboardCommand.java
@@ -465,17 +465,38 @@ public class ScoreboardCommand {
         int result = 0;
 
         for (ScoreHolder target : targets) {
-            ScoreAccess score = scoreboard.getOrCreatePlayerScore(target, targetObjective);
+            // Canvas start - region threading
 
-            for (ScoreHolder from : sources) {
-                ScoreAccess sourceScore = scoreboard.getOrCreatePlayerScore(from, sourceObjective);
-                operation.apply(score, sourceScore);
+            // both the target and source should be on the same region and share
+            // the same region to be valid for applying operations
+
+            // this must be an existing entity
+            if (!(target instanceof net.minecraft.world.entity.Entity entity)) {
+                source.sendFailure(Component.literal("Target score holder must be an existing entity, found " + target.getScoreboardName()));
+                continue;
             }
 
-            result += score.get();
+            entity.getBukkitEntity().taskScheduler.scheduleOrExecute((targetEntity) -> {
+                final ScoreAccess score = scoreboard.getOrCreatePlayerScore(targetEntity, targetObjective);
+
+                for (ScoreHolder from : sources) {
+                    // Canvas start - region threading
+                    if (!(from instanceof net.minecraft.world.entity.Entity sourceEntity) || !ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor(sourceEntity)) {
+                        source.sendFailure(Component.literal("Source entity not in same region, skipping"));
+                        continue;
+                    }
+                    ScoreAccess sourceScore = scoreboard.getOrCreatePlayerScore(sourceEntity, sourceObjective);
+                    try {
+                        operation.apply(score, sourceScore);
+                    } catch (CommandSyntaxException cse) {
+                        source.sendFailure(Component.literal(cse.getMessage()));
+                    }
+                }
+            });
+            // Canvas end - region threading
         }
 
-        if (targets.size() == 1) {
+        if (false && targets.size() == 1) { // Canvas - region threading - "result" is no longer accurate
             int finalResult = result;
             source.sendSuccess(
                 () -> Component.translatable(
@@ -500,43 +521,42 @@ public class ScoreboardCommand {
             throw ERROR_NOT_TRIGGER.create();
         } else {
             Scoreboard scoreboard = source.getServer().getScoreboard();
-            int count = 0;
+            // Canvas - region threading
 
             for (ScoreHolder name : names) {
-                ScoreAccess score = scoreboard.getOrCreatePlayerScore(name, objective);
-                if (score.locked()) {
-                    score.unlock();
-                    count++;
+                // Canvas start - region threading
+                if (!(name instanceof net.minecraft.world.entity.Entity entity)) {
+                    throw new UnsupportedOperationException("Score holder must be an entity");
                 }
+                entity.getBukkitEntity().taskScheduler.scheduleOrExecute((scoreHolder) -> {
+                    ScoreAccess score = scoreboard.getOrCreatePlayerScore(scoreHolder, objective);
+                    if (score.locked()) {
+                        score.unlock();
+                    }
+                });
+                // Canvas end - region threading
             }
 
-            if (count == 0) {
-                throw ERROR_TRIGGER_ALREADY_ENABLED.create();
-            } else {
-                if (names.size() == 1) {
-                    source.sendSuccess(
-                        () -> Component.translatable(
-                            "commands.scoreboard.players.enable.success.single", objective.getFormattedDisplayName(), getFirstTargetName(names)
-                        ),
-                        true
-                    );
-                } else {
-                    source.sendSuccess(
-                        () -> Component.translatable("commands.scoreboard.players.enable.success.multiple", objective.getFormattedDisplayName(), names.size()),
-                        true
-                    );
-                }
-
-                return count;
-            }
+            // Canvas start - region threading
+            source.sendSuccess(
+                () -> Component.translatable("commands.scoreboard.players.enable.success.multiple", objective.getFormattedDisplayName(), names.size()),
+                true
+            );
+            // Canvas end - region threading
         }
+        return 0; // Canvas - region threading
     }
 
     private static int resetScores(final CommandSourceStack source, final Collection<ScoreHolder> names) {
         Scoreboard scoreboard = source.getServer().getScoreboard();
 
         for (ScoreHolder name : names) {
-            scoreboard.resetAllPlayerScores(name);
+            // Canvas start - region threading
+            if (!(name instanceof net.minecraft.world.entity.Entity entity)) {
+                throw new UnsupportedOperationException("Score holder must be an entity");
+            }
+            entity.getBukkitEntity().taskScheduler.scheduleOrExecute(scoreboard::resetAllPlayerScores);
+            // Canvas end - region threading
         }
 
         if (names.size() == 1) {
@@ -552,7 +572,13 @@ public class ScoreboardCommand {
         Scoreboard scoreboard = source.getServer().getScoreboard();
 
         for (ScoreHolder name : names) {
-            scoreboard.resetSinglePlayerScore(name, objective);
+            // Canvas start - region threading
+            if (!(name instanceof net.minecraft.world.entity.Entity entity)) {
+                throw new UnsupportedOperationException("Score holder must be an entity");
+            }
+            entity.getBukkitEntity().taskScheduler.scheduleOrExecute((scoreHolder) ->
+                scoreboard.resetSinglePlayerScore(scoreHolder, objective));
+            // Canvas end - region threading
         }
 
         if (names.size() == 1) {
@@ -575,7 +601,13 @@ public class ScoreboardCommand {
         Scoreboard scoreboard = source.getServer().getScoreboard();
 
         for (ScoreHolder name : names) {
-            scoreboard.getOrCreatePlayerScore(name, objective).set(value);
+            // Canvas start - region threading
+            if (!(name instanceof net.minecraft.world.entity.Entity entity)) {
+                throw new UnsupportedOperationException("Score holder must be an entity");
+            }
+            entity.getBukkitEntity().taskScheduler.scheduleOrExecute((scoreHolder) ->
+                scoreboard.getOrCreatePlayerScore(scoreHolder, objective).set(value));
+            // Canvas end - region threading
         }
 
         if (names.size() == 1) {
@@ -601,7 +633,13 @@ public class ScoreboardCommand {
         Scoreboard scoreboard = source.getServer().getScoreboard();
 
         for (ScoreHolder name : names) {
-            scoreboard.getOrCreatePlayerScore(name, objective).display(display);
+            // Canvas start - region threading
+            if (!(name instanceof net.minecraft.world.entity.Entity entity)) {
+                throw new UnsupportedOperationException("Score holder must be an entity");
+            }
+            entity.getBukkitEntity().taskScheduler.scheduleOrExecute((scoreHolder) ->
+                scoreboard.getOrCreatePlayerScore(scoreHolder, objective).display(display));
+            // Canvas end - region threading
         }
 
         if (display == null) {
@@ -645,7 +683,13 @@ public class ScoreboardCommand {
         Scoreboard scoreboard = source.getServer().getScoreboard();
 
         for (ScoreHolder name : names) {
-            scoreboard.getOrCreatePlayerScore(name, objective).numberFormatOverride(numberFormat);
+            // Canvas start - region threading
+            if (!(name instanceof net.minecraft.world.entity.Entity entity)) {
+                throw new UnsupportedOperationException("Score holder must be an entity");
+            }
+            entity.getBukkitEntity().taskScheduler.scheduleOrExecute((scoreHolder) ->
+                scoreboard.getOrCreatePlayerScore(scoreHolder, objective).numberFormatOverride(numberFormat));
+            // Canvas end - region threading
         }
 
         if (numberFormat == null) {
@@ -688,20 +732,18 @@ public class ScoreboardCommand {
         int result = 0;
 
         for (ScoreHolder name : names) {
-            ScoreAccess score = scoreboard.getOrCreatePlayerScore(name, objective);
-            score.set(score.get() + value);
-            result += score.get();
+            // Canvas start - region threading
+            if (!(name instanceof net.minecraft.world.entity.Entity entity)) {
+                throw new UnsupportedOperationException("Score holder must be an entity");
+            }
+            entity.getBukkitEntity().taskScheduler.scheduleOrExecute((scoreHolder) -> {
+                ScoreAccess score = scoreboard.getOrCreatePlayerScore(scoreHolder, objective);
+                score.set(score.get() + value);
+            });
+            // Canvas end - region threading
         }
 
-        if (names.size() == 1) {
-            int finalResult = result;
-            source.sendSuccess(
-                () -> Component.translatable(
-                    "commands.scoreboard.players.add.success.single", value, objective.getFormattedDisplayName(), getFirstTargetName(names), finalResult
-                ),
-                true
-            );
-        } else {
+        { // Canvas - region threading
             source.sendSuccess(
                 () -> Component.translatable("commands.scoreboard.players.add.success.multiple", value, objective.getFormattedDisplayName(), names.size()),
                 true
@@ -716,20 +758,18 @@ public class ScoreboardCommand {
         int result = 0;
 
         for (ScoreHolder name : names) {
-            ScoreAccess score = scoreboard.getOrCreatePlayerScore(name, objective);
-            score.set(score.get() - value);
-            result += score.get();
+            // Canvas start - region threading
+            if (!(name instanceof net.minecraft.world.entity.Entity entity)) {
+                throw new UnsupportedOperationException("Score holder must be an entity");
+            }
+            entity.getBukkitEntity().taskScheduler.scheduleOrExecute((scoreHolder) -> {
+                ScoreAccess score = scoreboard.getOrCreatePlayerScore(scoreHolder, objective);
+                score.set(score.get() - value);
+            });
+            // Canvas end - region threading
         }
 
-        if (names.size() == 1) {
-            int finalResult = result;
-            source.sendSuccess(
-                () -> Component.translatable(
-                    "commands.scoreboard.players.remove.success.single", value, objective.getFormattedDisplayName(), getFirstTargetName(names), finalResult
-                ),
-                true
-            );
-        } else {
+        { // Canvas - region threading
             source.sendSuccess(
                 () -> Component.translatable("commands.scoreboard.players.remove.success.multiple", value, objective.getFormattedDisplayName(), names.size()),
                 true
@@ -756,12 +796,18 @@ public class ScoreboardCommand {
     }
 
     private static int listTrackedPlayerScores(final CommandSourceStack source, final ScoreHolder entity) {
-        Object2IntMap<Objective> scores = source.getServer().getScoreboard().listPlayerScores(entity);
+        // Canvas start - region threading
+        if (!(entity instanceof net.minecraft.world.entity.Entity casted)) {
+            throw new UnsupportedOperationException("Score holder must be an entity");
+        }
+        casted.getBukkitEntity().taskScheduler.scheduleOrExecute((scoreHolder) -> {
+        Object2IntMap<Objective> scores = source.getServer().getScoreboard().listPlayerScores(scoreHolder);
+        // Canvas end - region threading
         if (scores.isEmpty()) {
-            source.sendSuccess(() -> Component.translatable("commands.scoreboard.players.list.entity.empty", entity.getFeedbackDisplayName()), false);
+            source.sendSuccess(() -> Component.translatable("commands.scoreboard.players.list.entity.empty", scoreHolder.getFeedbackDisplayName()), false); // Canvas - region threading
         } else {
             source.sendSuccess(
-                () -> Component.translatable("commands.scoreboard.players.list.entity.success", entity.getFeedbackDisplayName(), scores.size()), false
+                () -> Component.translatable("commands.scoreboard.players.list.entity.success", scoreHolder.getFeedbackDisplayName(), scores.size()), false // Canvas - region threading
             );
             Object2IntMaps.fastForEach(
                 scores,
@@ -774,21 +820,39 @@ public class ScoreboardCommand {
             );
         }
 
-        return scores.size();
+        // Canvas start - region threading
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int clearDisplaySlot(final CommandSourceStack source, final DisplaySlot slot) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         Scoreboard scoreboard = source.getServer().getScoreboard();
         if (scoreboard.getDisplayObjective(slot) == null) {
             throw ERROR_DISPLAY_SLOT_ALREADY_EMPTY.create();
         } else {
             scoreboard.setDisplayObjective(slot, null);
             source.sendSuccess(() -> Component.translatable("commands.scoreboard.objectives.display.cleared", slot.getSerializedName()), true);
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setDisplaySlot(final CommandSourceStack source, final DisplaySlot slot, final Objective objective) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         Scoreboard scoreboard = source.getServer().getScoreboard();
         if (scoreboard.getDisplayObjective(slot) == objective) {
             throw ERROR_DISPLAY_SLOT_ALREADY_SET.create();
@@ -797,11 +861,19 @@ public class ScoreboardCommand {
             source.sendSuccess(
                 () -> Component.translatable("commands.scoreboard.objectives.display.set", slot.getSerializedName(), objective.getDisplayName()), true
             );
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setDisplayName(final CommandSourceStack source, final Objective objective, final Component displayName) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         if (!objective.getDisplayName().equals(displayName)) {
             objective.setDisplayName(displayName);
             source.sendSuccess(
@@ -809,11 +881,13 @@ public class ScoreboardCommand {
                 true
             );
         }
+        }); // Canvas - region threading
 
         return 0;
     }
 
     private static int setDisplayAutoUpdate(final CommandSourceStack source, final Objective objective, final boolean displayAutoUpdate) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         if (objective.displayAutoUpdate() != displayAutoUpdate) {
             objective.setDisplayAutoUpdate(displayAutoUpdate);
             if (displayAutoUpdate) {
@@ -832,38 +906,51 @@ public class ScoreboardCommand {
                 );
             }
         }
+        }); // Canvas - region threading
 
         return 0;
     }
 
     private static int setObjectiveFormat(final CommandSourceStack source, final Objective objective, final @Nullable NumberFormat numberFormat) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         objective.setNumberFormat(numberFormat);
         if (numberFormat != null) {
             source.sendSuccess(() -> Component.translatable("commands.scoreboard.objectives.modify.objectiveFormat.set", objective.getName()), true);
         } else {
             source.sendSuccess(() -> Component.translatable("commands.scoreboard.objectives.modify.objectiveFormat.clear", objective.getName()), true);
         }
+        }); // Canvas - region threading
 
         return 0;
     }
 
     private static int setRenderType(final CommandSourceStack source, final Objective objective, final ObjectiveCriteria.RenderType renderType) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         if (objective.getRenderType() != renderType) {
             objective.setRenderType(renderType);
             source.sendSuccess(() -> Component.translatable("commands.scoreboard.objectives.modify.rendertype", objective.getFormattedDisplayName()), true);
         }
+        }); // Canvas - region threading
 
         return 0;
     }
 
     private static int removeObjective(final CommandSourceStack source, final Objective objective) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         Scoreboard scoreboard = source.getServer().getScoreboard();
         scoreboard.removeObjective(objective);
         source.sendSuccess(() -> Component.translatable("commands.scoreboard.objectives.remove.success", objective.getFormattedDisplayName()), true);
-        return scoreboard.getObjectives().size();
+        // Canvas start - region threading
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int addObjective(final CommandSourceStack source, final String name, final ObjectiveCriteria criteria, final Component displayName) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         Scoreboard scoreboard = source.getServer().getScoreboard();
         if (scoreboard.getObjective(name) != null) {
             throw ERROR_OBJECTIVE_ALREADY_EXISTS.create();
@@ -871,8 +958,15 @@ public class ScoreboardCommand {
             scoreboard.addObjective(name, criteria, displayName, criteria.getDefaultRenderType(), false, null);
             Objective objective = scoreboard.getObjective(name);
             source.sendSuccess(() -> Component.translatable("commands.scoreboard.objectives.add.success", objective.getFormattedDisplayName()), true);
-            return scoreboard.getObjectives().size();
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int listObjectives(final CommandSourceStack source) {
diff --git a/net/minecraft/server/commands/TeamCommand.java b/net/minecraft/server/commands/TeamCommand.java
index 2dca4067cbd55fd6f08a3e90c79167b156a56c8b..a37a5276b5e705e81d7e18fa3462a9b66e849e26 100644
--- a/net/minecraft/server/commands/TeamCommand.java
+++ b/net/minecraft/server/commands/TeamCommand.java
@@ -88,10 +88,7 @@ public class TeamCommand {
                     Commands.literal("remove")
                         .then(Commands.argument("team", TeamArgument.team()).executes(c -> deleteTeam(c.getSource(), TeamArgument.getTeam(c, "team"))))
                 )
-                .then(
-                    Commands.literal("empty")
-                        .then(Commands.argument("team", TeamArgument.team()).executes(c -> emptyTeam(c.getSource(), TeamArgument.getTeam(c, "team"))))
-                )
+                // Canvas - region threading - TODO later, we need a way to schedule to the entity. while we can do this async, it's not a good idea
                 .then(
                     Commands.literal("join")
                         .then(
@@ -272,7 +269,16 @@ public class TeamCommand {
         Scoreboard scoreboard = source.getServer().getScoreboard();
 
         for (ScoreHolder member : members) {
-            scoreboard.removePlayerFromTeam(member.getScoreboardName());
+            // Canvas start - region threading
+            if (member instanceof net.minecraft.world.entity.Entity entity) {
+                entity.getBukkitEntity().taskScheduler.scheduleOrExecute((serverEntity) ->
+                    scoreboard.removePlayerFromTeam(serverEntity.getScoreboardName())
+                );
+            }
+            else {
+                scoreboard.removePlayerFromTeam(member.getScoreboardName());
+            }
+            // Canvas end - region threading
         }
 
         if (members.size() == 1) {
@@ -288,7 +294,17 @@ public class TeamCommand {
         Scoreboard scoreboard = source.getServer().getScoreboard();
 
         for (ScoreHolder member : members) {
-            scoreboard.addPlayerToTeam(member.getScoreboardName(), team);
+            // Canvas start - region threading
+            if (member instanceof net.minecraft.world.entity.Entity entity) {
+                entity.getBukkitEntity().taskScheduler.scheduleOrExecute((serverEntity) ->
+                    scoreboard.addPlayerToTeam(serverEntity.getScoreboardName(), team)
+                );
+                continue;
+            }
+            else {
+                scoreboard.addPlayerToTeam(member.getScoreboardName(), team);
+            }
+            // Canvas end - region threading
         }
 
         if (members.size() == 1) {
@@ -303,6 +319,10 @@ public class TeamCommand {
     }
 
     private static int setNametagVisibility(final CommandSourceStack source, final PlayerTeam team, final Team.Visibility visibility) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         if (team.getNameTagVisibility() == visibility) {
             throw ERROR_TEAM_NAMETAG_VISIBLITY_UNCHANGED.create();
         } else {
@@ -311,11 +331,22 @@ public class TeamCommand {
                 () -> Component.translatable("commands.team.option.nametagVisibility.success", team.getFormattedDisplayName(), visibility.getDisplayName()),
                 true
             );
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setDeathMessageVisibility(final CommandSourceStack source, final PlayerTeam team, final Team.Visibility visibility) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         if (team.getDeathMessageVisibility() == visibility) {
             throw ERROR_TEAM_DEATH_MESSAGE_VISIBLITY_UNCHANGED.create();
         } else {
@@ -324,11 +355,22 @@ public class TeamCommand {
                 () -> Component.translatable("commands.team.option.deathMessageVisibility.success", team.getFormattedDisplayName(), visibility.getDisplayName()),
                 true
             );
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setCollision(final CommandSourceStack source, final PlayerTeam team, final Team.CollisionRule collision) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         if (team.getCollisionRule() == collision) {
             throw ERROR_TEAM_COLLISION_UNCHANGED.create();
         } else {
@@ -336,11 +378,22 @@ public class TeamCommand {
             source.sendSuccess(
                 () -> Component.translatable("commands.team.option.collisionRule.success", team.getFormattedDisplayName(), collision.getDisplayName()), true
             );
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setFriendlySight(final CommandSourceStack source, final PlayerTeam team, final boolean allowed) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         if (team.canSeeFriendlyInvisibles() == allowed) {
             if (allowed) {
                 throw ERROR_TEAM_ALREADY_FRIENDLYINVISIBLES_ENABLED.create();
@@ -353,11 +406,22 @@ public class TeamCommand {
                 () -> Component.translatable("commands.team.option.seeFriendlyInvisibles." + (allowed ? "enabled" : "disabled"), team.getFormattedDisplayName()),
                 true
             );
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setFriendlyFire(final CommandSourceStack source, final PlayerTeam team, final boolean allowed) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         if (team.isAllowFriendlyFire() == allowed) {
             if (allowed) {
                 throw ERROR_TEAM_ALREADY_FRIENDLYFIRE_ENABLED.create();
@@ -369,28 +433,57 @@ public class TeamCommand {
             source.sendSuccess(
                 () -> Component.translatable("commands.team.option.friendlyfire." + (allowed ? "enabled" : "disabled"), team.getFormattedDisplayName()), true
             );
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setDisplayName(final CommandSourceStack source, final PlayerTeam team, final Component displayName) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         if (team.getDisplayName().equals(displayName)) {
             throw ERROR_TEAM_ALREADY_NAME.create();
         } else {
             team.setDisplayName(displayName);
             source.sendSuccess(() -> Component.translatable("commands.team.option.name.success", team.getFormattedDisplayName()), true);
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setColor(final CommandSourceStack source, final PlayerTeam team, final ChatFormatting color) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         if (team.getColor() == color) {
             throw ERROR_TEAM_ALREADY_COLOR.create();
         } else {
             team.setColor(color);
             source.sendSuccess(() -> Component.translatable("commands.team.option.color.success", team.getFormattedDisplayName(), color.getName()), true);
-            return 0;
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int emptyTeam(final CommandSourceStack source, final PlayerTeam team) throws CommandSyntaxException {
@@ -409,10 +502,14 @@ public class TeamCommand {
     }
 
     private static int deleteTeam(final CommandSourceStack source, final PlayerTeam team) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         Scoreboard scoreboard = source.getServer().getScoreboard();
         scoreboard.removePlayerTeam(team);
         source.sendSuccess(() -> Component.translatable("commands.team.remove.success", team.getFormattedDisplayName()), true);
-        return scoreboard.getPlayerTeams().size();
+        // Canvas start - region threading
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int createTeam(final CommandSourceStack source, final String name) throws CommandSyntaxException {
@@ -420,6 +517,10 @@ public class TeamCommand {
     }
 
     private static int createTeam(final CommandSourceStack source, final String name, final Component displayName) throws CommandSyntaxException {
+        // Canvas start - region threading
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> {
+            try {
+        // Canvas end - region threading
         Scoreboard scoreboard = source.getServer().getScoreboard();
         if (scoreboard.getPlayerTeam(name) != null) {
             throw ERROR_TEAM_ALREADY_EXISTS.create();
@@ -427,11 +528,19 @@ public class TeamCommand {
             PlayerTeam team = scoreboard.addPlayerTeam(name);
             team.setDisplayName(displayName);
             source.sendSuccess(() -> Component.translatable("commands.team.add.success", team.getFormattedDisplayName()), true);
-            return scoreboard.getPlayerTeams().size();
+            return; // Canvas - region threading
         }
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int listMembers(final CommandSourceStack source, final PlayerTeam team) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         Collection<String> members = team.getPlayers();
         if (members.isEmpty()) {
             source.sendSuccess(() -> Component.translatable("commands.team.list.members.empty", team.getFormattedDisplayName()), false);
@@ -443,11 +552,14 @@ public class TeamCommand {
                 false
             );
         }
-
-        return members.size();
+        // Canvas start - region threading
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int listTeams(final CommandSourceStack source) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         Collection<PlayerTeam> teams = source.getServer().getScoreboard().getPlayerTeams();
         if (teams.isEmpty()) {
             source.sendSuccess(() -> Component.translatable("commands.team.list.teams.empty"), false);
@@ -459,19 +571,25 @@ public class TeamCommand {
                 false
             );
         }
-
-        return teams.size();
+        // Canvas start - region threading
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setPrefix(final CommandSourceStack source, final PlayerTeam team, final Component prefix) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         team.setPlayerPrefix(prefix);
         source.sendSuccess(() -> Component.translatable("commands.team.option.prefix.success", prefix), false);
+        }); // Canvas - region threading
         return 1;
     }
 
     private static int setSuffix(final CommandSourceStack source, final PlayerTeam team, final Component suffix) {
+        io.papermc.paper.threadedregions.RegionizedServer.getInstance().scheduleToOrExecute(() -> { // Canvas - region threading
         team.setPlayerSuffix(suffix);
         source.sendSuccess(() -> Component.translatable("commands.team.option.suffix.success", suffix), false);
+        }); // Canvas - region threading
         return 1;
     }
 }
diff --git a/net/minecraft/server/commands/TriggerCommand.java b/net/minecraft/server/commands/TriggerCommand.java
index 088b7b91154546fc9c020cab5cefbb4b89762ac8..9d5f76ff2892fd38a21397698a986027774f5832 100644
--- a/net/minecraft/server/commands/TriggerCommand.java
+++ b/net/minecraft/server/commands/TriggerCommand.java
@@ -89,24 +89,54 @@ public class TriggerCommand {
     }
 
     private static int addValue(final CommandSourceStack source, final ServerPlayer player, final Objective objective, final int amount) throws CommandSyntaxException {
-        ScoreAccess score = getScore(source.getServer().getScoreboard(), player, objective);
+        // Canvas start - region threading
+        player.getBukkitEntity().taskScheduler.scheduleOrExecute((entityPlayer) -> {
+            try {
+        ScoreAccess score = getScore(source.getServer().getScoreboard(), entityPlayer, objective);
+        // Canvas end - region threading
         int newValue = score.add(amount);
         source.sendSuccess(() -> Component.translatable("commands.trigger.add.success", objective.getFormattedDisplayName(), amount), true);
-        return newValue;
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int setValue(final CommandSourceStack source, final ServerPlayer player, final Objective objective, final int amount) throws CommandSyntaxException {
-        ScoreAccess score = getScore(source.getServer().getScoreboard(), player, objective);
+        // Canvas start - region threading
+        player.getBukkitEntity().taskScheduler.scheduleOrExecute((entityPlayer) -> {
+            try {
+        ScoreAccess score = getScore(source.getServer().getScoreboard(), entityPlayer, objective);
+        // Canvas end - region threading
         score.set(amount);
         source.sendSuccess(() -> Component.translatable("commands.trigger.set.success", objective.getFormattedDisplayName(), amount), true);
-        return amount;
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static int simpleTrigger(final CommandSourceStack source, final ServerPlayer player, final Objective objective) throws CommandSyntaxException {
-        ScoreAccess score = getScore(source.getServer().getScoreboard(), player, objective);
+        // Canvas start - region threading
+        player.getBukkitEntity().taskScheduler.scheduleOrExecute((entityPlayer) -> {
+            try {
+        ScoreAccess score = getScore(source.getServer().getScoreboard(), entityPlayer, objective);
+        // Canvas end - region threading
         int newValue = score.add(1);
         source.sendSuccess(() -> Component.translatable("commands.trigger.simple.success", objective.getFormattedDisplayName()), true);
-        return newValue;
+        // Canvas start - region threading
+            } catch (CommandSyntaxException cse) {
+                source.sendFailure(Component.literal(cse.getMessage()));
+            }
+        });
+        return 0;
+        // Canvas end - region threading
     }
 
     private static ScoreAccess getScore(final Scoreboard scoreboard, final ScoreHolder scoreHolder, final Objective objective) throws CommandSyntaxException {
diff --git a/net/minecraft/server/players/PlayerList.java b/net/minecraft/server/players/PlayerList.java
index 30f05aa84cfe04d603dcdcfafcc0a6ce7150ef48..9fd6f35bd1e9799040fcb18b63e52e51bc9674f7 100644
--- a/net/minecraft/server/players/PlayerList.java
+++ b/net/minecraft/server/players/PlayerList.java
@@ -134,7 +134,7 @@ public abstract class PlayerList {
     // CraftBukkit start
     private org.bukkit.craftbukkit.CraftServer cserver;
     private final Map<String,ServerPlayer> playersByName = new java.util.HashMap<>();
-    public @Nullable String collideRuleTeamName; // Paper - Configurable player collision
+    // Canvas - region threading
 
     // Folia start - region threading
     private final Object connectionsStateLock = new Object();
@@ -272,7 +272,7 @@ public abstract class PlayerList {
         this.sendPlayerPermissionLevel(player);
         player.getStats().markAllDirty();
         player.getRecipeBook().sendInitialRecipeBook(player);
-        //this.updateEntireScoreboard(level.getScoreboard(), player); // Folia - region threading
+        this.updateEntireScoreboard(level.getScoreboard(), player);
         this.server.invalidateStatus();
         MutableComponent component;
         if (player.getGameProfile().name().equalsIgnoreCase(oldName)) {
@@ -375,13 +375,7 @@ public abstract class PlayerList {
         player.initInventoryMenu();
         this.server.notificationManager().playerJoined(player);
         playerConnection.resumeFlushing();
-        // Paper start - Configurable player collision; Add to collideRule team if needed
-        final net.minecraft.world.scores.Scoreboard scoreboard = this.getServer().getLevel(Level.OVERWORLD).getScoreboard();
-        final PlayerTeam collideRuleTeam = scoreboard.getPlayerTeam(this.collideRuleTeamName);
-        if (false && this.collideRuleTeamName != null && collideRuleTeam != null && player.getTeam() == null) { // Folia - region threading
-            scoreboard.addPlayerToTeam(player.getScoreboardName(), collideRuleTeam);
-        }
-        // Paper end - Configurable player collision
+        // Canvas - region threading
         // CraftBukkit start - moved down
         LOGGER.info(
             "{}[{}] logged in with entity id {} at ([{}]{}, {}, {})", // Paper - add world identifier
@@ -520,15 +514,7 @@ public abstract class PlayerList {
 
         // CraftBukkit end
 
-        // Paper start - Configurable player collision; Remove from collideRule team if needed
-        if (false && this.collideRuleTeamName != null) { // Folia - region threading
-            final net.minecraft.world.scores.Scoreboard scoreBoard = this.server.getLevel(Level.OVERWORLD).getScoreboard();
-            final PlayerTeam team = scoreBoard.getPlayersTeam(this.collideRuleTeamName);
-            if (player.getTeam() == team && team != null) {
-                scoreBoard.removePlayerFromTeam(player.getScoreboardName(), team);
-            }
-        }
-        // Paper end - Configurable player collision
+        // Canvas - region threading
 
         // Paper - Drop carried item when player has disconnected
         if (!player.containerMenu.getCarried().isEmpty()) {
@@ -1199,13 +1185,7 @@ public abstract class PlayerList {
         }
         // CraftBukkit end
 
-        // Paper start - Configurable player collision; Remove collideRule team if it exists
-        if (false && this.collideRuleTeamName != null) { // Folia - region threading
-            final net.minecraft.world.scores.Scoreboard scoreboard = this.getServer().getLevel(Level.OVERWORLD).getScoreboard();
-            final PlayerTeam team = scoreboard.getPlayersTeam(this.collideRuleTeamName);
-            if (team != null) scoreboard.removePlayerTeam(team);
-        }
-        // Paper end - Configurable player collision
+        // Canvas - region threading
     }
 
     public void broadcastSystemMessage(final Component message, final boolean overlay) {
diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index 362a885fded2cc8e6564b1b0ea324d7b68111085..7354b81152238752b7aece6e038f98c085b29a86 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -3655,11 +3655,6 @@ public abstract class Entity
     }
 
     public @Nullable PlayerTeam getTeam() {
-        // Folia start - region threading
-        if (true) {
-            return null;
-        }
-        // Folia end - region threading
         if (!this.level().paperConfig().scoreboards.allowNonPlayerEntitiesOnScoreboards && !(this instanceof Player)) { return null; } // Paper - Perf: Disable Scoreboards for non players by default
         return this.level().getScoreboard().getPlayersTeam(this.getScoreboardName());
     }
diff --git a/net/minecraft/world/entity/EntitySelector.java b/net/minecraft/world/entity/EntitySelector.java
index 050e1ba6cdc4c50aa188cef88f421a9a6c5f0836..29cac514ebe3da4a25d0da9b9a8ee3cde5a95e02 100644
--- a/net/minecraft/world/entity/EntitySelector.java
+++ b/net/minecraft/world/entity/EntitySelector.java
@@ -54,7 +54,7 @@ public final class EntitySelector {
                     } else if (!entity.level().isClientSide() || input instanceof Player player && player.isLocalPlayer()) {
                         Team theirTeam = input.getTeam();
                         Team.CollisionRule theirCollisionRule = theirTeam == null ? Team.CollisionRule.ALWAYS : theirTeam.getCollisionRule();
-                        if (theirCollisionRule == Team.CollisionRule.NEVER || (input instanceof Player && !io.papermc.paper.configuration.GlobalConfiguration.get().collisions.enablePlayerCollisions)) { // Paper - Configurable player collision
+                        if (theirCollisionRule == Team.CollisionRule.NEVER) { // Paper - Configurable player collision // Canvas - region threading
                             return false;
                         } else {
                             boolean sameTeam = ownTeam != null && ownTeam.isAlliedTo(theirTeam);
diff --git a/net/minecraft/world/scores/Objective.java b/net/minecraft/world/scores/Objective.java
index 277f9cd3c7d4005dd0c86d941e2acc7b331904d2..d4ddbdf895e19cf15929bc6ba4ea2258ab92d0da 100644
--- a/net/minecraft/world/scores/Objective.java
+++ b/net/minecraft/world/scores/Objective.java
@@ -17,11 +17,10 @@ public class Objective {
     private final Scoreboard scoreboard;
     private final String name;
     private final ObjectiveCriteria criteria;
-    public Component displayName;
-    private Component formattedDisplayName;
-    private ObjectiveCriteria.RenderType renderType;
-    private boolean displayAutoUpdate;
-    private @Nullable NumberFormat numberFormat;
+    // Canvas start - region threading
+    private final io.canvasmc.canvas.util.LockedReference<io.canvasmc.canvas.world.scores.ObjectiveData> objectiveData =
+        new io.canvasmc.canvas.util.LockedReference<>(new io.canvasmc.canvas.world.scores.ObjectiveData());
+    // Canvas end - region threading
 
     public Objective(
         final Scoreboard scoreboard,
@@ -35,15 +34,15 @@ public class Objective {
         this.scoreboard = scoreboard;
         this.name = name;
         this.criteria = criteria;
-        this.displayName = displayName;
-        this.formattedDisplayName = this.createFormattedDisplayName();
-        this.renderType = renderType;
-        this.displayAutoUpdate = displayAutoUpdate;
-        this.numberFormat = numberFormat;
+        // Canvas start - region threading
+        this.objectiveData.swapValue((_) -> new io.canvasmc.canvas.world.scores.ObjectiveData(
+            createFormattedDisplayName(displayName), renderType, displayAutoUpdate, numberFormat, displayName
+        ));
+        // Canvas end - region threading
     }
 
     public Objective.Packed pack() {
-        return new Objective.Packed(this.name, this.criteria, this.displayName, this.renderType, this.displayAutoUpdate, Optional.ofNullable(this.numberFormat));
+        return this.objectiveData.getValue().pack(this.name, this.criteria); // Canvas - region threading
     }
 
     public Scoreboard getScoreboard() {
@@ -59,53 +58,76 @@ public class Objective {
     }
 
     public Component getDisplayName() {
-        return this.displayName;
+        return this.objectiveData.getValue().getDisplayName(); // Canvas - region threading
     }
 
     public boolean displayAutoUpdate() {
-        return this.displayAutoUpdate;
+        return this.objectiveData.getValue().doesDisplayAutoUpdate(); // Canvas - region threading
     }
 
     public @Nullable NumberFormat numberFormat() {
-        return this.numberFormat;
+        return this.objectiveData.getValue().getNumberFormat(); // Canvas - region threading
     }
 
     public NumberFormat numberFormatOrDefault(final NumberFormat _default) {
-        return Objects.requireNonNullElse(this.numberFormat, _default);
+        return Objects.requireNonNullElse(numberFormat(), _default); // Canvas - region threading
     }
 
     private Component createFormattedDisplayName() {
+    // Canvas start - region threading
+        return createFormattedDisplayName(getDisplayName());
+    }
+
+    private Component createFormattedDisplayName(final Component displayName) {
+    // Canvas end - region threading
         return ComponentUtils.wrapInSquareBrackets(
-            this.displayName.copy().withStyle(s -> s.withHoverEvent(new HoverEvent.ShowText(Component.literal(this.name))))
+            displayName.copy().withStyle(s -> s.withHoverEvent(new HoverEvent.ShowText(Component.literal(this.name)))) // Canvas - region threading
         );
     }
 
     public Component getFormattedDisplayName() {
-        return this.formattedDisplayName;
+        return this.objectiveData.getValue().getFormattedDisplayName(); // Canvas - region threading
     }
 
     public void setDisplayName(final Component name) {
-        this.displayName = name;
-        this.formattedDisplayName = this.createFormattedDisplayName();
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set display name of objective async");
+        this.objectiveData.swapValue((data) -> {
+            final io.canvasmc.canvas.world.scores.ObjectiveData newData =
+                io.canvasmc.canvas.world.scores.ObjectiveData.copyOf(data);
+            newData.setDisplayName(name);
+            newData.setFormattedDisplayName(createFormattedDisplayName(name));
+            return newData;
+        });
+        // Canvas end - region threading
         this.scoreboard.onObjectiveChanged(this);
     }
 
     public ObjectiveCriteria.RenderType getRenderType() {
-        return this.renderType;
+        return this.objectiveData.getValue().getRenderType(); // Canvas - region threading
     }
 
     public void setRenderType(final ObjectiveCriteria.RenderType renderType) {
-        this.renderType = renderType;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set render type of objective async");
+        this.objectiveData.swapValue((data) -> io.canvasmc.canvas.world.scores.ObjectiveData.copyOf(data).setRenderType(renderType));
+        // Canvas end - region threading
         this.scoreboard.onObjectiveChanged(this);
     }
 
     public void setDisplayAutoUpdate(final boolean displayAutoUpdate) {
-        this.displayAutoUpdate = displayAutoUpdate;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set display auto update flag of objective async");
+        this.objectiveData.swapValue((data) -> io.canvasmc.canvas.world.scores.ObjectiveData.copyOf(data).setDisplayAutoUpdate(displayAutoUpdate));
+        // Canvas end - region threading
         this.scoreboard.onObjectiveChanged(this);
     }
 
     public void setNumberFormat(final @Nullable NumberFormat numberFormat) {
-        this.numberFormat = numberFormat;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set number format of objective async");
+        this.objectiveData.swapValue((data) -> io.canvasmc.canvas.world.scores.ObjectiveData.copyOf(data).setNumberFormat(numberFormat));
+        // Canvas end - region threading
         this.scoreboard.onObjectiveChanged(this);
     }
 
diff --git a/net/minecraft/world/scores/PlayerScores.java b/net/minecraft/world/scores/PlayerScores.java
index 18efc6c44b94d24b9f4a3a828cfba1410131c6f3..fd48ba936144841c1c983e78386e1106536dc808 100644
--- a/net/minecraft/world/scores/PlayerScores.java
+++ b/net/minecraft/world/scores/PlayerScores.java
@@ -9,7 +9,11 @@ import java.util.function.Consumer;
 import org.jspecify.annotations.Nullable;
 
 public class PlayerScores {
-    private final Reference2ObjectOpenHashMap<Objective, Score> scores = new Reference2ObjectOpenHashMap<>(16, 0.5F);
+    // Canvas start - region threading
+    // we synchronize instead of make concurrent since we want to make this atomic of sorts
+    private final it.unimi.dsi.fastutil.objects.Reference2ObjectMap<Objective, Score> scores =
+        it.unimi.dsi.fastutil.objects.Reference2ObjectMaps.synchronize(new Reference2ObjectOpenHashMap<>(16, 0.5F));
+    // Canvas end - region threading
 
     public @Nullable Score get(final Objective objective) {
         return this.scores.get(objective);
diff --git a/net/minecraft/world/scores/PlayerTeam.java b/net/minecraft/world/scores/PlayerTeam.java
index acb8ffc862860a304c4fa100acecfce746f5fffe..53a226f2d5487aaf5bea3651ad228a1f6c530e9b 100644
--- a/net/minecraft/world/scores/PlayerTeam.java
+++ b/net/minecraft/world/scores/PlayerTeam.java
@@ -22,39 +22,22 @@ public class PlayerTeam extends Team {
     private static final int BIT_SEE_INVISIBLES = 1;
     private final Scoreboard scoreboard;
     private final String name;
-    private final Set<String> players = Sets.newHashSet();
-    private Component displayName;
-    private Component playerPrefix = CommonComponents.EMPTY;
-    private Component playerSuffix = CommonComponents.EMPTY;
-    private boolean allowFriendlyFire = true;
-    private boolean seeFriendlyInvisibles = true;
-    private Team.Visibility nameTagVisibility = Team.Visibility.ALWAYS;
-    private Team.Visibility deathMessageVisibility = Team.Visibility.ALWAYS;
-    private ChatFormatting color = ChatFormatting.RESET;
-    private Team.CollisionRule collisionRule = Team.CollisionRule.ALWAYS;
+    // Canvas start - region threading
+    private final Set<String> players = new java.util.concurrent.CopyOnWriteArraySet<>();
+    private final io.canvasmc.canvas.util.LockedReference<io.canvasmc.canvas.world.scores.TeamData> teamData =
+        new io.canvasmc.canvas.util.LockedReference<>(new io.canvasmc.canvas.world.scores.TeamData());
+    // Canvas end - region threading
     private final Style displayNameStyle;
 
     public PlayerTeam(final Scoreboard scoreboard, final String name) {
         this.scoreboard = scoreboard;
         this.name = name;
-        this.displayName = Component.literal(name);
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setDisplayName(Component.literal(name))); // Canvas - region threading
         this.displayNameStyle = Style.EMPTY.withInsertion(name).withHoverEvent(new HoverEvent.ShowText(Component.literal(name)));
     }
 
     public PlayerTeam.Packed pack() {
-        return new PlayerTeam.Packed(
-            this.name,
-            Optional.of(this.displayName),
-            this.color != ChatFormatting.RESET ? Optional.of(this.color) : Optional.empty(),
-            this.allowFriendlyFire,
-            this.seeFriendlyInvisibles,
-            this.playerPrefix,
-            this.playerSuffix,
-            this.nameTagVisibility,
-            this.deathMessageVisibility,
-            this.collisionRule,
-            List.copyOf(this.players)
-        );
+        return this.teamData.getValue().pack(this.name, List.copyOf(this.players)); // Canvas - region threading
     }
 
     public Scoreboard getScoreboard() {
@@ -67,11 +50,11 @@ public class PlayerTeam extends Team {
     }
 
     public Component getDisplayName() {
-        return this.displayName;
+        return this.teamData.getValue().getDisplayName(); // Canvas - region threading
     }
 
     public MutableComponent getFormattedDisplayName() {
-        MutableComponent result = ComponentUtils.wrapInSquareBrackets(this.displayName.copy().withStyle(this.displayNameStyle));
+        MutableComponent result = ComponentUtils.wrapInSquareBrackets(getDisplayName().copy().withStyle(this.displayNameStyle)); // Canvas - region threading
         ChatFormatting color = this.getColor();
         if (color != ChatFormatting.RESET) {
             result.withStyle(color);
@@ -81,30 +64,37 @@ public class PlayerTeam extends Team {
     }
 
     public void setDisplayName(final Component displayName) {
+        this.scoreboard.ensureMainThread("Cannot set display name for team async"); // Canvas - region threading
         if (displayName == null) {
             throw new IllegalArgumentException("Name cannot be null");
         } else {
-            this.displayName = displayName;
+            this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setDisplayName(displayName)); // Canvas - region threading
             this.scoreboard.onTeamChanged(this);
         }
     }
 
     public void setPlayerPrefix(final @Nullable Component playerPrefix) {
-        this.playerPrefix = playerPrefix == null ? CommonComponents.EMPTY : playerPrefix;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set player prefix for team async");
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setPlayerPrefix(playerPrefix == null ? CommonComponents.EMPTY : playerPrefix));
+        // Canvas end - region threading
         this.scoreboard.onTeamChanged(this);
     }
 
     public Component getPlayerPrefix() {
-        return this.playerPrefix;
+        return this.teamData.getValue().getPlayerPrefix(); // Canvas - region threading
     }
 
     public void setPlayerSuffix(final @Nullable Component playerSuffix) {
-        this.playerSuffix = playerSuffix == null ? CommonComponents.EMPTY : playerSuffix;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set player suffix for team async");
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setPlayerSuffix(playerSuffix == null ? CommonComponents.EMPTY : playerSuffix));
+        // Canvas end - region threading
         this.scoreboard.onTeamChanged(this);
     }
 
     public Component getPlayerSuffix() {
-        return this.playerSuffix;
+        return this.teamData.getValue().getPlayerSuffix(); // Canvas - region threading
     }
 
     @Override
@@ -114,7 +104,7 @@ public class PlayerTeam extends Team {
 
     @Override
     public MutableComponent getFormattedName(final Component teamMemberName) {
-        MutableComponent result = Component.empty().append(this.playerPrefix).append(teamMemberName).append(this.playerSuffix);
+        MutableComponent result = Component.empty().append(getPlayerPrefix()).append(teamMemberName).append(getPlayerSuffix()); // Canvas - region threading
         ChatFormatting color = this.getColor();
         if (color != ChatFormatting.RESET) {
             result.withStyle(color);
@@ -129,51 +119,66 @@ public class PlayerTeam extends Team {
 
     @Override
     public boolean isAllowFriendlyFire() {
-        return this.allowFriendlyFire;
+        return this.teamData.getValue().isAllowFriendlyFire(); // Canvas - region threading
     }
 
     public void setAllowFriendlyFire(final boolean allowFriendlyFire) {
-        this.allowFriendlyFire = allowFriendlyFire;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set friendly fire flag for team async");
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setAllowFriendlyFire(allowFriendlyFire));
+        // Canvas end - region threading
         this.scoreboard.onTeamChanged(this);
     }
 
     @Override
     public boolean canSeeFriendlyInvisibles() {
-        return this.seeFriendlyInvisibles;
+        return this.teamData.getValue().isSeeFriendlyInvisibles(); // Canvas - region threading
     }
 
     public void setSeeFriendlyInvisibles(final boolean seeFriendlyInvisibles) {
-        this.seeFriendlyInvisibles = seeFriendlyInvisibles;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set friendly invisibles flag for team async");
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setSeeFriendlyInvisibles(seeFriendlyInvisibles));
+        // Canvas end - region threading
         this.scoreboard.onTeamChanged(this);
     }
 
     @Override
     public Team.Visibility getNameTagVisibility() {
-        return this.nameTagVisibility;
+        return this.teamData.getValue().getNameTagVisibility(); // Canvas - region threading
     }
 
     @Override
     public Team.Visibility getDeathMessageVisibility() {
-        return this.deathMessageVisibility;
+        return this.teamData.getValue().getDeathMessageVisibility(); // Canvas - region threading
     }
 
     public void setNameTagVisibility(final Team.Visibility visibility) {
-        this.nameTagVisibility = visibility;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set name tag visibility for team async");
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setNameTagVisibility(visibility));
         this.scoreboard.onTeamChanged(this);
+        // Canvas end - region threading
     }
 
     public void setDeathMessageVisibility(final Team.Visibility visibility) {
-        this.deathMessageVisibility = visibility;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set death message visibility for team async");
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setDeathMessageVisibility(visibility));
         this.scoreboard.onTeamChanged(this);
+        // Canvas end - region threading
     }
 
     @Override
     public Team.CollisionRule getCollisionRule() {
-        return this.collisionRule;
+        return this.teamData.getValue().getCollisionRule(); // Canvas - region threading
     }
 
     public void setCollisionRule(final Team.CollisionRule collisionRule) {
-        this.collisionRule = collisionRule;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set collision rule for team async");
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setCollisionRule(collisionRule));
+        // Canvas end - region threading
         this.scoreboard.onTeamChanged(this);
     }
 
@@ -196,13 +201,16 @@ public class PlayerTeam extends Team {
     }
 
     public void setColor(final ChatFormatting color) {
-        this.color = color;
+        // Canvas start - region threading
+        this.scoreboard.ensureMainThread("Cannot set color for team async");
+        this.teamData.swapValue((data) -> io.canvasmc.canvas.world.scores.TeamData.copyOf(data).setColor(color));
+        // Canvas end - region threading
         this.scoreboard.onTeamChanged(this);
     }
 
     @Override
     public ChatFormatting getColor() {
-        return this.color;
+        return this.teamData.getValue().getColor(); // Canvas - region threading
     }
 
     public record Packed(
diff --git a/net/minecraft/world/scores/Scoreboard.java b/net/minecraft/world/scores/Scoreboard.java
index 3013937e4b458cd46b6f3e72ce46692a43511423..4a752c58635138c608e841aba1b938aba5de1b86 100644
--- a/net/minecraft/world/scores/Scoreboard.java
+++ b/net/minecraft/world/scores/Scoreboard.java
@@ -30,17 +30,46 @@ import org.slf4j.Logger;
 public class Scoreboard {
     public static final String HIDDEN_SCORE_PREFIX = "#";
     private static final Logger LOGGER = LogUtils.getLogger();
-    private final Object2ObjectMap<String, Objective> objectivesByName = new Object2ObjectOpenHashMap<>(16, 0.5F);
-    private final Reference2ObjectMap<ObjectiveCriteria, List<Objective>> objectivesByCriteria = new Reference2ObjectOpenHashMap<>();
-    private final Map<String, PlayerScores> playerScores = new Object2ObjectOpenHashMap<>(16, 0.5F);
-    private final Map<DisplaySlot, Objective> displayObjectives = new EnumMap<>(DisplaySlot.class);
-    private final Object2ObjectMap<String, PlayerTeam> teamsByName = new Object2ObjectOpenHashMap<>();
-    private final Object2ObjectMap<String, PlayerTeam> teamsByPlayer = new Object2ObjectOpenHashMap<>();
+    // Canvas start - region threading
+    // by objective name
+    private final ca.spottedleaf.concurrentutil.map.SWMRHashTable<String, Objective> objectivesByName = new ca.spottedleaf.concurrentutil.map.SWMRHashTable<>(16, 0.5F);
+    // note - by score holder name: this needs to be read/written to async, must be fully thread-safe
+    //        it should also be noted that for players, this is their profile name, while for non player
+    //        entities the name is the string uuid
+    private final Map<String, PlayerScores> scoresByName = new java.util.concurrent.ConcurrentHashMap<>(16, 0.5F);
+    private final io.canvasmc.canvas.util.LockedReference<net.minecraft.world.scores.Scoreboard.Entry> teamReferences = new io.canvasmc.canvas.util.LockedReference<>(
+        new Entry(
+            new Object2ObjectOpenHashMap<>(),
+            new Object2ObjectOpenHashMap<>()
+        )
+    );
+    private final java.util.concurrent.atomic.AtomicReferenceArray<Objective> displayObjectives = new java.util.concurrent.atomic.AtomicReferenceArray<>(new Objective[DisplaySlot.values().length]);
+
+    private record Entry(
+        Object2ObjectOpenHashMap<String, net.minecraft.world.scores.PlayerTeam> teamsByName,
+        Object2ObjectMap<String, PlayerTeam> teamsByHolder
+    ) {}
+
+    public void ensureMainThread(final String reason) {
+        // if in startup, we don't care, and if we are in shutdown, we don't care
+        if (io.papermc.paper.threadedregions.TickRegions.started && !io.papermc.paper.threadedregions.RegionShutdownThread.canvas$isShutdown()) {
+            io.papermc.paper.threadedregions.RegionizedServer.ensureGlobalTickThread(reason);
+        }
+    }
+
+    protected void ensureStartup(final String reason) {
+        if (io.papermc.paper.threadedregions.TickRegions.started) {
+            throw new IllegalStateException(reason);
+        }
+    }
+    // Canvas end - region threading
 
     public @Nullable Objective getObjective(final @Nullable String name) {
+        if (name == null) return null; // Canvas - region threading - SWMRHashTable enforces nonnull keys
         return this.objectivesByName.get(name);
     }
 
+    // note: must be on global tick
     public Objective addObjective(
         final String name,
         final ObjectiveCriteria criteria,
@@ -52,8 +81,9 @@ public class Scoreboard {
         if (this.objectivesByName.containsKey(name)) {
             throw new IllegalArgumentException("An objective with the name '" + name + "' already exists!");
         } else {
+            ensureMainThread("Cannot add objective async"); // Canvas - region threading
             Objective objective = new Objective(this, name, criteria, displayName, renderType, displayAutoUpdate, numberFormat);
-            this.objectivesByCriteria.computeIfAbsent(criteria, k -> Lists.newArrayList()).add(objective);
+            // Canvas - region threading
             this.objectivesByName.put(name, objective);
             this.onObjectiveAdded(objective);
             return objective;
@@ -61,11 +91,21 @@ public class Scoreboard {
     }
 
     public final void forAllObjectives(final ObjectiveCriteria criteria, final ScoreHolder name, final Consumer<ScoreAccess> operation) {
-        this.objectivesByCriteria.getOrDefault(criteria, Collections.emptyList()).forEach(o -> operation.accept(this.getOrCreatePlayerScore(name, o, true)));
+        // Canvas start - region threading
+        if (name instanceof Entity entity) {
+            ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread(entity, "Cannot iterate over objectives for entity async");
+        }
+        // this basically is just for each objective that has a certain criteria instance
+        for (final Objective objective : this.objectivesByName.values()) {
+            if (objective.getCriteria() == criteria) {
+                operation.accept(this.getOrCreatePlayerScore(name, objective, true));
+            }
+        }
+        // Canvas end - region threading
     }
 
     private PlayerScores getOrCreatePlayerInfo(final String name) {
-        return this.playerScores.computeIfAbsent(name, k -> new PlayerScores());
+        return this.scoresByName.computeIfAbsent(name, k -> new PlayerScores()); // Canvas - region threading
     }
 
     public ScoreAccess getOrCreatePlayerScore(final ScoreHolder holder, final Objective objective) {
@@ -73,6 +113,11 @@ public class Scoreboard {
     }
 
     public ScoreAccess getOrCreatePlayerScore(final ScoreHolder scoreHolder, final Objective objective, final boolean forceWritable) {
+        // Canvas start - region threading
+        if (scoreHolder instanceof Entity entity) {
+            ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread(entity, "Cannot get or create score for entity async");
+        }
+        // Canvas end - region threading
         final boolean canModify = forceWritable || !objective.getCriteria().isReadOnly();
         PlayerScores playerScore = this.getOrCreatePlayerInfo(scoreHolder.getScoreboardName());
         final MutableBoolean requiresSync = new MutableBoolean();
@@ -81,14 +126,26 @@ public class Scoreboard {
             {
                 Objects.requireNonNull(Scoreboard.this);
             }
+            // Canvas start - region threading
+
+            @Override
+            public synchronized int add(final int count) {
+                return ScoreAccess.super.add(count);
+            }
+
+            @Override
+            public synchronized int increment() {
+                return ScoreAccess.super.increment();
+            }
+            // Canvas end - region threading
 
             @Override
-            public int get() {
+            public synchronized int get() { // Canvas - region threading
                 return score.value();
             }
 
             @Override
-            public void set(final int value) {
+            public synchronized void set(final int value) { // Canvas - region threading
                 if (!canModify) {
                     throw new IllegalStateException("Cannot modify read-only score");
                 } else {
@@ -113,12 +170,12 @@ public class Scoreboard {
             }
 
             @Override
-            public @Nullable Component display() {
+            public synchronized @Nullable Component display() { // Canvas - region threading
                 return score.display();
             }
 
             @Override
-            public void display(final @Nullable Component display) {
+            public synchronized void display(final @Nullable Component display) { // Canvas - region threading
                 if (requiresSync.isTrue() || !Objects.equals(display, score.display())) {
                     score.display(display);
                     this.sendScoreToPlayers();
@@ -126,13 +183,13 @@ public class Scoreboard {
             }
 
             @Override
-            public void numberFormatOverride(final @Nullable NumberFormat numberFormat) {
+            public synchronized void numberFormatOverride(final @Nullable NumberFormat numberFormat) { // Canvas - region threading
                 score.numberFormat(numberFormat);
                 this.sendScoreToPlayers();
             }
 
             @Override
-            public boolean locked() {
+            public synchronized boolean locked() { // Canvas - region threading
                 return score.isLocked();
             }
 
@@ -146,7 +203,7 @@ public class Scoreboard {
                 this.setLocked(true);
             }
 
-            private void setLocked(final boolean locked) {
+            private synchronized void setLocked(final boolean locked) { // Canvas - region threading
                 score.setLocked(locked);
                 if (requiresSync.isTrue()) {
                     this.sendScoreToPlayers();
@@ -162,14 +219,19 @@ public class Scoreboard {
         };
     }
 
+    // note: no need for thread check, this is read only
     public @Nullable ReadOnlyScoreInfo getPlayerScoreInfo(final ScoreHolder name, final Objective objective) {
-        PlayerScores playerScore = this.playerScores.get(name.getScoreboardName());
-        return playerScore != null ? playerScore.get(objective) : null;
+        // Canvas start - region threading
+        PlayerScores playerScore = this.scoresByName.get(name.getScoreboardName());
+        // we wrap with our own implementation because technically you can cast ReadOnlyScoreInfo to Score
+        // which then you can modify it with... no thanks!
+        return playerScore != null ? new io.canvasmc.canvas.world.scores.ReadOnlyScore(playerScore.get(objective)) : null;
+        // Canvas end - region threading
     }
 
     public Collection<PlayerScoreEntry> listPlayerScores(final Objective objective) {
         List<PlayerScoreEntry> result = new ArrayList<>();
-        this.playerScores.forEach((player, scores) -> {
+        this.scoresByName.forEach((player, scores) -> { // Canvas - region threading
             Score score = scores.get(objective);
             if (score != null) {
                 result.add(new PlayerScoreEntry(player, score.value(), score.display(), score.numberFormat()));
@@ -187,22 +249,32 @@ public class Scoreboard {
     }
 
     public Collection<ScoreHolder> getTrackedPlayers() {
-        return this.playerScores.keySet().stream().map(ScoreHolder::forNameOnly).toList();
+        return this.scoresByName.keySet().stream().map(ScoreHolder::forNameOnly).toList(); // Canvas - region threading
     }
 
     public void resetAllPlayerScores(final ScoreHolder player) {
-        PlayerScores removed = this.playerScores.remove(player.getScoreboardName());
+        // Canvas start - region threading
+        if (player instanceof Entity entity) {
+            ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread(entity, "Cannot reset all scores for score holder async");
+        }
+        PlayerScores removed = this.scoresByName.remove(player.getScoreboardName());
+        // Canvas end - region threading
         if (removed != null) {
             this.onPlayerRemoved(player);
         }
     }
 
     public void resetSinglePlayerScore(final ScoreHolder player, final Objective objective) {
-        PlayerScores scores = this.playerScores.get(player.getScoreboardName());
+        // Canvas start - region threading
+        if (player instanceof Entity entity) {
+            ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread(entity, "Cannot reset single score for score holder async");
+        }
+        PlayerScores scores = this.scoresByName.get(player.getScoreboardName());
+        // Canvas end - region threading
         if (scores != null) {
             boolean hasRemoved = scores.remove(objective);
             if (!scores.hasScores()) {
-                PlayerScores removedPlayer = this.playerScores.remove(player.getScoreboardName());
+                PlayerScores removedPlayer = this.scoresByName.remove(player.getScoreboardName()); // Canvas - region threading
                 if (removedPlayer != null) {
                     this.onPlayerRemoved(player);
                 }
@@ -213,104 +285,170 @@ public class Scoreboard {
     }
 
     public Object2IntMap<Objective> listPlayerScores(final ScoreHolder player) {
-        PlayerScores scores = this.playerScores.get(player.getScoreboardName());
+        // Canvas start - region threading
+        if (player instanceof Entity entity) {
+            ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread(entity, "Cannot reset single score for score holder async");
+        }
+        PlayerScores scores = this.scoresByName.get(player.getScoreboardName());
+        // Canvas end - region threading
         return scores != null ? scores.listScores() : Object2IntMaps.emptyMap();
     }
 
+    // note: must be on global tick
     public void removeObjective(final Objective objective) {
-        this.objectivesByName.remove(objective.getName());
+        ensureMainThread("Cannot remove objective async"); // Canvas - region threading
 
         for (DisplaySlot value : DisplaySlot.values()) {
             if (this.getDisplayObjective(value) == objective) {
+                // this is fine, we can only modify this on the global tick, and we are on the global tick
+                // so we don't risk concurrent modification, only concurrent reads
                 this.setDisplayObjective(value, null);
             }
         }
 
-        List<Objective> objectives = this.objectivesByCriteria.get(objective.getCriteria());
-        if (objectives != null) {
-            objectives.remove(objective);
-        }
-
-        for (PlayerScores playerScore : this.playerScores.values()) {
+        for (PlayerScores playerScore : this.scoresByName.values()) { // Canvas - region threading
             playerScore.remove(objective);
         }
 
+        this.objectivesByName.remove(objective.getName()); // Canvas - region threading
         this.onObjectiveRemoved(objective);
     }
 
     public void setDisplayObjective(final DisplaySlot slot, final @Nullable Objective objective) {
-        this.displayObjectives.put(slot, objective);
+        // Canvas start - region threading
+        ensureMainThread("Cannot modify display objective async");
+        this.displayObjectives.set(slot.ordinal(), objective);
+        // Canvas end - region threading
     }
 
     public @Nullable Objective getDisplayObjective(final DisplaySlot slot) {
-        return this.displayObjectives.get(slot);
+        return this.displayObjectives.get(slot.ordinal()); // Canvas - region threading
     }
 
     public @Nullable PlayerTeam getPlayerTeam(final String name) {
-        return this.teamsByName.get(name);
+        return this.teamReferences.getValue().teamsByName.get(name); // Canvas - region threading
     }
 
+    // note: must be on global tick
     public PlayerTeam addPlayerTeam(final String name) {
-        PlayerTeam team = this.getPlayerTeam(name);
-        if (team != null) {
-            LOGGER.warn("Requested creation of existing team '{}'", name);
-            return team;
-        } else {
-            team = new PlayerTeam(this, name);
-            this.teamsByName.put(name, team);
-            this.onTeamAdded(team);
-            return team;
+        // Canvas start - region threading
+        ensureMainThread("Cannot add player team async");
+
+        final MutableBoolean isNew = new MutableBoolean(false);
+        PlayerTeam res = this.teamReferences.swapAndGet((current) -> {
+            if (current.teamsByName.containsKey(name)) {
+                LOGGER.warn("Requested creation of existing team '{}'", name);
+                return current;
+            }
+            var newTeamsByName = new Object2ObjectOpenHashMap<>(current.teamsByName);
+            var newTeamsByHolder = new Object2ObjectOpenHashMap<>(current.teamsByHolder);
+
+            newTeamsByName.put(name, new PlayerTeam(this, name));
+            isNew.setTrue();
+
+            return new Entry(newTeamsByName, newTeamsByHolder);
+        }).teamsByName.get(name);
+
+        if (isNew.isTrue()) {
+            this.onTeamAdded(res);
         }
+
+        return res;
+        // Canvas end - region threading
     }
 
+    // note: must be on global tick
     public void removePlayerTeam(final PlayerTeam team) {
-        this.teamsByName.remove(team.getName());
+        // Canvas start - region threading
+        ensureMainThread("Cannot remove player team async");
 
-        for (String player : team.getPlayers()) {
-            this.teamsByPlayer.remove(player);
-        }
+        this.teamReferences.swapValue((current) -> {
+            var newTeamsByName = new Object2ObjectOpenHashMap<>(current.teamsByName);
+            var newTeamsByHolder = new Object2ObjectOpenHashMap<>(current.teamsByHolder);
+
+            newTeamsByName.remove(team.getName());
+            for (final String scoreHolderName : team.getPlayers()) {
+                newTeamsByHolder.remove(scoreHolderName);
+            }
+
+            return new Entry(newTeamsByName, newTeamsByHolder);
+        });
 
         this.onTeamRemoved(team);
+        // Canvas end - region threading
     }
 
     public boolean addPlayerToTeam(final String player, final PlayerTeam team) {
-        if (this.getPlayersTeam(player) != null) {
-            this.removePlayerFromTeam(player);
-        }
+        // Canvas start - region threading
+        final MutableBoolean added = new MutableBoolean(false);
+
+        this.teamReferences.swapValue((current) -> {
+            var newTeamsByHolder = new Object2ObjectOpenHashMap<>(current.teamsByHolder);
+            if (current.teamsByHolder.containsKey(player)) {
+                final PlayerTeam previous = newTeamsByHolder.remove(player);
+                previous.getPlayers().remove(player);
+            }
 
-        this.teamsByPlayer.put(player, team);
-        return team.getPlayers().add(player);
+            newTeamsByHolder.put(player, team);
+            added.setValue(team.getPlayers().add(player));
+
+            return new Entry(current.teamsByName, newTeamsByHolder);
+        });
+
+        return added.isTrue();
+        // Canvas end - region threading
     }
 
     public boolean removePlayerFromTeam(final String player) {
-        PlayerTeam team = this.getPlayersTeam(player);
-        if (team != null) {
-            this.removePlayerFromTeam(player, team);
-            return true;
-        } else {
-            return false;
-        }
+        // Canvas start - region threading
+        final MutableBoolean wasInTeam = new MutableBoolean(false);
+
+        this.teamReferences.swapValue((current) -> {
+            // if not contained by holder map, not in team
+            if (!current.teamsByHolder.containsKey(player)) {
+                return current;
+            }
+
+            var newTeamsByHolder = new Object2ObjectOpenHashMap<>(current.teamsByHolder);
+            final PlayerTeam removedFrom = newTeamsByHolder.remove(player);
+            removedFrom.getPlayers().remove(player);
+
+            wasInTeam.setTrue();
+
+            return new Entry(current.teamsByName, newTeamsByHolder);
+        });
+
+        return wasInTeam.isTrue();
+        // Canvas end - region threading
     }
 
     public void removePlayerFromTeam(final String player, final PlayerTeam team) {
-        if (this.getPlayersTeam(player) != team) {
-            throw new IllegalStateException("Player is either on another team or not on any team. Cannot remove from team '" + team.getName() + "'.");
-        } else {
-            this.teamsByPlayer.remove(player);
-            team.getPlayers().remove(player);
-        }
+        // Canvas start - region threading
+        this.teamReferences.swapValue((current) -> {
+            final PlayerTeam removingFrom = current.teamsByHolder.get(player);
+            if (removingFrom != team) {
+                throw new IllegalStateException("Player is either on another team or not on any team. Cannot remove from team '" + team.getName() + "'.");
+            }
+
+            var newTeamsByHolder = new Object2ObjectOpenHashMap<>(current.teamsByHolder);
+            newTeamsByHolder.remove(player);
+            removingFrom.getPlayers().remove(player);
+
+            return new Entry(current.teamsByName, newTeamsByHolder);
+        });
+        // Canvas end - region threading
     }
 
     public Collection<String> getTeamNames() {
-        return this.teamsByName.keySet();
+        return this.teamReferences.getValue().teamsByName.keySet(); // Canvas - region threading
     }
 
     public Collection<PlayerTeam> getPlayerTeams() {
-        return this.teamsByName.values();
+        return this.teamReferences.getValue().teamsByName.values(); // Canvas - region threading
     }
 
     public @Nullable PlayerTeam getPlayersTeam(final String name) {
-        return this.teamsByPlayer.get(name);
+        return this.teamReferences.getValue().teamsByHolder.get(name); // Canvas - region threading
     }
 
     public void onObjectiveAdded(final Objective objective) {
@@ -343,15 +481,16 @@ public class Scoreboard {
     public void onTeamRemoved(final PlayerTeam team) {
     }
 
+    // note: there are no internal usages of this
     public void entityRemoved(final Entity entity) {
-        if (!(entity instanceof Player) && !entity.isAlive()) {
-            this.resetAllPlayerScores(entity);
-            this.removePlayerFromTeam(entity.getScoreboardName());
-        }
+        throw new UnsupportedOperationException(); // Canvas - region threading
     }
 
     protected List<Scoreboard.PackedScore> packPlayerScores() {
-        return this.playerScores
+        // Canvas start - region threading
+        ensureMainThread("Cannot pack player scores async");
+        return this.scoresByName
+        // Canvas end - region threading
             .entrySet()
             .stream()
             .flatMap(
@@ -367,7 +506,9 @@ public class Scoreboard {
             .toList();
     }
 
+    // note: called only at startup
     protected void loadPlayerScore(final Scoreboard.PackedScore score) {
+        ensureStartup("Can only load player score at startup"); // Canvas - region threading
         Objective objective = this.getObjective(score.objective);
         if (objective == null) {
             LOGGER.error("Unknown objective {} for name {}, ignoring", score.objective, score.owner);
@@ -377,10 +518,13 @@ public class Scoreboard {
     }
 
     protected List<PlayerTeam.Packed> packPlayerTeams() {
+        ensureMainThread("Cannot pack player teams async"); // Canvas - region threading
         return this.getPlayerTeams().stream().filter(p -> io.papermc.paper.configuration.GlobalConfiguration.get().scoreboards.saveEmptyScoreboardTeams || !p.getPlayers().isEmpty()).map(PlayerTeam::pack).toList(); // Paper - Don't save empty scoreboard teams to scoreboard.dat
     }
 
+    // note: called only at startup
     protected void loadPlayerTeam(final PlayerTeam.Packed packed) {
+        ensureStartup("Can only load player team at startup"); // Canvas - region threading
         PlayerTeam team = this.addPlayerTeam(packed.name());
         packed.displayName().ifPresent(team::setDisplayName);
         packed.color().ifPresent(team::setColor);
@@ -398,10 +542,13 @@ public class Scoreboard {
     }
 
     protected List<Objective.Packed> packObjectives() {
+        ensureMainThread("Cannot pack objectives async"); // Canvas - region threading
         return this.getObjectives().stream().map(Objective::pack).toList();
     }
 
+    // note: called only at startup
     protected void loadObjective(final Objective.Packed objective) {
+        ensureStartup("Can only load objective at startup"); // Canvas - region threading
         this.addObjective(
             objective.name(),
             objective.criteria(),
@@ -413,6 +560,7 @@ public class Scoreboard {
     }
 
     protected Map<DisplaySlot, String> packDisplaySlots() {
+        ensureMainThread("Cannot pack display slots async"); // Canvas - region threading
         Map<DisplaySlot, String> displaySlots = new EnumMap<>(DisplaySlot.class);
 
         for (DisplaySlot slot : DisplaySlot.values()) {
diff --git a/net/minecraft/world/scores/ScoreboardSaveData.java b/net/minecraft/world/scores/ScoreboardSaveData.java
index 9f8497761fedb78ad2ce86ab704772a3258958b9..db51e07f1638e099a297f412ae0963a4ca37a854 100644
--- a/net/minecraft/world/scores/ScoreboardSaveData.java
+++ b/net/minecraft/world/scores/ScoreboardSaveData.java
@@ -31,6 +31,7 @@ public class ScoreboardSaveData extends SavedData {
     }
 
     public void setData(final ScoreboardSaveData.Packed data) {
+        net.minecraft.server.MinecraftServer.getServer().getScoreboard().ensureMainThread("Cannot save scoreboard data async"); // Canvas - region threading
         if (!data.equals(this.data)) {
             this.data = data;
             this.setDirty();
diff --git a/net/minecraft/world/scores/criteria/ObjectiveCriteria.java b/net/minecraft/world/scores/criteria/ObjectiveCriteria.java
index 9379005e86e501ded702879662a516de190d239c..fb6a43f49a9346d15fd42a1436802d44ed5bdc65 100644
--- a/net/minecraft/world/scores/criteria/ObjectiveCriteria.java
+++ b/net/minecraft/world/scores/criteria/ObjectiveCriteria.java
@@ -15,7 +15,7 @@ import net.minecraft.util.StringRepresentable;
 
 public class ObjectiveCriteria {
     private static final Map<String, ObjectiveCriteria> CUSTOM_CRITERIA = Maps.newHashMap();
-    public static final Map<String, ObjectiveCriteria> CRITERIA_CACHE = Maps.newHashMap();
+    public static final Map<String, ObjectiveCriteria> CRITERIA_CACHE = Maps.newConcurrentMap(); // Canvas - region threading
     public static final Codec<ObjectiveCriteria> CODEC = Codec.STRING
         .comapFlatMap(
             name -> byName(name).map(DataResult::success).orElse(DataResult.error(() -> "No scoreboard criteria with name: " + name)),
