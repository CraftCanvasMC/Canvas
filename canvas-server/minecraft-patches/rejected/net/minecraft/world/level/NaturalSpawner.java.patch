From fb2fa76bcfb03ba2831bd257c68fc51821e86ead Mon Sep 17 00:00:00 2001
From: File <noreply+automated@papermc.io>
Date: Sun, 20 Apr 1997 15:37:42 +0200
Subject: [PATCH] canvas File Patches


diff --git a/net/minecraft/world/level/NaturalSpawner.java b/net/minecraft/world/level/NaturalSpawner.java
index f8c9f238aee91eb3bca5ec2cd8851cd35aa9ce66..884b59e49606e91c20a9859fd7bd36f6b9c20286 100644
--- a/net/minecraft/world/level/NaturalSpawner.java
+++ b/net/minecraft/world/level/NaturalSpawner.java
@@ -68,50 +68,93 @@ public final class NaturalSpawner {
         int spawnableChunkCount, Iterable<Entity> entities, NaturalSpawner.ChunkGetter chunkGetter, LocalMobCapCalculator calculator
     ) {
         // Paper start - Optional per player mob spawns
-        return createState(spawnableChunkCount, entities, chunkGetter, calculator, false);
+        return createState(spawnableChunkCount, calculator, false, io.papermc.paper.threadedregions.TickRegionScheduler.getCurrentRegionizedWorldData()); // Canvas - optimize natural spawning
     }
 
+    // Canvas start - optimize natural spawning
+    private static int[] calculateLimits(io.papermc.paper.threadedregions.RegionizedWorldData worldData, int spawnableChunkCount) {
+        int[] retVal = new int[SPAWNING_CATEGORIES.length];
+        ServerLevel world = worldData.world;
+        for (MobCategory category : MobCategory.values()) {
+            if (category == MobCategory.MISC) continue;
+            // normally we would check if the category is valid for spawn limits, however the SPAWNING_CATEGORIES
+            // array filters this for us already, so we don't need to do an extra call
+            int rawLimit = world.getWorld().getSpawnLimit(org.bukkit.craftbukkit.util.CraftSpawnCategory.toBukkit(category));
+            // we need to apply the same ratio calculation as canSpawnForCategoryGlobal
+            retVal[category.ordinal()] = rawLimit * spawnableChunkCount / NaturalSpawner.MAGIC_NUMBER;
+        }
+        return retVal;
+    }
+
+    // Canvas end - optimize natural spawning
     public static NaturalSpawner.SpawnState createState(
-        int spawnableChunkCount, Iterable<Entity> entities, NaturalSpawner.ChunkGetter chunkGetter, LocalMobCapCalculator calculator, final boolean countMobs
+        int spawnableChunkCount, LocalMobCapCalculator calculator, final boolean countMobs, io.papermc.paper.threadedregions.RegionizedWorldData worldData // Canvas - optimize natural spawning
     ) {
         // Paper end - Optional per player mob spawns
         PotentialCalculator potentialCalculator = new PotentialCalculator();
+        int[] limits = calculateLimits(worldData, spawnableChunkCount); // Canvas - optimize natural spawning
         Object2IntOpenHashMap<MobCategory> map = new Object2IntOpenHashMap<>();
 
-        for (Entity entity : entities) {
+        // Canvas start - optimize natural spawning
+        final boolean perPlayerMobSpawning = worldData.world.paperConfig().entities.spawning.perPlayerMobSpawns;
+        // optimized iteration path, go by chunks
+        for (LevelChunk chunk : worldData.getChunks().getRawDataUnchecked()) {
+            if (chunk == null) continue; // unlikely, but check to be sure
+        for (Entity entity : chunk.moonrise$getChunkHolder().getEntityChunk().getEntityArr()) {
+            if (entity == null) continue; // very likely, must check
+            MobCategory category = entity.getType().getCategory();
+            // if we skip because we hit the limit, it won't change the outcome, since if it skips because
+            // of the limit hit, the category will be filtered anyway later
+            if (category == MobCategory.MISC) continue;
+            final boolean shouldSkipBecauseOfLimit = limits[category.ordinal()] <= map.getInt(category);
+            if (!perPlayerMobSpawning && shouldSkipBecauseOfLimit) continue;
             if (!(entity instanceof Mob mob && (mob.isPersistenceRequired() || mob.requiresCustomPersistence()))) {
-                MobCategory category = entity.getType().getCategory();
-                if (category != MobCategory.MISC) {
-                    // Paper start - Only count natural spawns
-                    if (!entity.level().paperConfig().entities.spawning.countAllMobsForSpawning &&
-                        !(entity.spawnReason == org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.NATURAL ||
-                            entity.spawnReason == org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.CHUNK_GEN)) {
-                        continue;
+                // Paper start - Only count natural spawns
+                if (!entity.level().paperConfig().entities.spawning.countAllMobsForSpawning &&
+                    !(entity.spawnReason == org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.NATURAL ||
+                        entity.spawnReason == org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason.CHUNK_GEN)) {
+                    continue;
+                }
+                // Paper end - Only count natural spawns
+                BlockPos blockPos = entity.blockPosition();
+
+                // getting the rough biome is expensive, if we are skipping because of
+                // the limit, why tf are we gonna do this, since there is no impact by skipping this anyway
+
+                // the only real reason we don't skip the full thing with per-player mob spawns is because
+                // per-player mob spawns require 'updatePlayerMobTypeMap' to be called for accurate spawning
+
+                // as a result, if we are doing per-player spawning, we try and skip as much as possible to
+                // reduce expensive CPU time without costing vanilla mob spawning functionality
+
+                // Note: this should be safe, this only effects the rate of spawning, and specifically for a
+                // few mobs for a few nether biomes, so farms shouldn't be impacted
+                if (!perPlayerMobSpawning || !shouldSkipBecauseOfLimit) {
+                    // only do this lookup if we aren't using per-player spawns(since we skip above), and
+                    // if we are doing per-player mob spawning and not over the limit yet
+                    MobSpawnSettings.MobSpawnCost mobSpawnCost = getRoughBiome(blockPos, chunk).getMobSettings().getMobSpawnCost(entity.getType());
+                    if (mobSpawnCost != null) {
+                        potentialCalculator.addCharge(blockPos, mobSpawnCost.charge());
                     }
-                    // Paper end - Only count natural spawns
-                    BlockPos blockPos = entity.blockPosition();
-                    chunkGetter.query(ChunkPos.asLong(blockPos), chunk -> {
-                        MobSpawnSettings.MobSpawnCost mobSpawnCost = getRoughBiome(blockPos, chunk).getMobSettings().getMobSpawnCost(entity.getType());
-                        if (mobSpawnCost != null) {
-                            potentialCalculator.addCharge(entity.blockPosition(), mobSpawnCost.charge());
-                        }
+                }
 
-                        if (calculator != null && entity instanceof Mob) { // Paper - Optional per player mob spawns
-                            calculator.addMob(chunk.getPos(), category);
-                        }
+                // only runs if we are not doing per-player
+                if (calculator != null && entity instanceof Mob) {
+                    calculator.addMob(chunk.getPos(), category);
+                }
 
-                        map.addTo(category, 1);
-                        // Paper start - Optional per player mob spawns
-                        if (countMobs) {
-                            chunk.level.getChunkSource().chunkMap.updatePlayerMobTypeMap(entity);
-                        }
-                        // Paper end - Optional per player mob spawns
-                    });
+                map.addTo(category, 1);
+                // Paper start - Optional per player mob spawns
+                if (countMobs) {
+                    chunk.level.getChunkSource().chunkMap.updatePlayerMobTypeMap(entity);
                 }
+                // Paper end - Optional per player mob spawns
+            }
+        // Canvas end - optimize natural spawning
             }
         }
 
-        return new NaturalSpawner.SpawnState(spawnableChunkCount, map, potentialCalculator, calculator);
+        return new NaturalSpawner.SpawnState(spawnableChunkCount, map, potentialCalculator, calculator, worldData, limits); // Canvas - optimize natural spawning
     }
 
     static Biome getRoughBiome(BlockPos pos, ChunkAccess chunk) {
@@ -153,6 +196,11 @@ public final class NaturalSpawner {
     }
 
     public static void spawnForChunk(ServerLevel level, LevelChunk chunk, NaturalSpawner.SpawnState spawnState, List<MobCategory> categories) {
+        // Canvas start - optimize natural spawning
+        final ca.spottedleaf.moonrise.common.list.ReferenceList<net.minecraft.server.level.ServerPlayer> inRange =
+            level.moonrise$getNearbyPlayers().getPlayers(chunk.getPos(), ca.spottedleaf.moonrise.common.misc.NearbyPlayers.NearbyMapType.TICK_VIEW_DISTANCE);
+        net.minecraft.server.level.ServerPlayer closestPlayer = null;
+        // Canvas end - optimize natural spawning
         for (MobCategory mobCategory : categories) {
             // Paper start - Optional per player mob spawns
             final boolean canSpawn;
@@ -167,12 +215,17 @@ public final class NaturalSpawner {
 
                 // Apply per-player limit
                 int minDiff = Integer.MAX_VALUE;
-                final ca.spottedleaf.moonrise.common.list.ReferenceList<net.minecraft.server.level.ServerPlayer> inRange =
-                    level.moonrise$getNearbyPlayers().getPlayers(chunk.getPos(), ca.spottedleaf.moonrise.common.misc.NearbyPlayers.NearbyMapType.TICK_VIEW_DISTANCE);
+                // Canvas - move up
                 if (inRange != null) {
                     final net.minecraft.server.level.ServerPlayer[] backingSet = inRange.getRawDataUnchecked();
                     for (int k = 0, len = inRange.size(); k < len; k++) {
-                        minDiff = Math.min(limit - level.getChunkSource().chunkMap.getMobCountNear(backingSet[k], mobCategory), minDiff);
+                        // Canvas start - optimize natural spawning
+                        int diff = limit - level.getChunkSource().chunkMap.getMobCountNear(backingSet[k], mobCategory);
+                        if (diff < minDiff) {
+                            minDiff = diff;
+                            closestPlayer = backingSet[k];
+                        }
+                        // Canvas end - optimize natural spawning
                     }
                 }
 
@@ -182,8 +235,10 @@ public final class NaturalSpawner {
                 canSpawn = spawnState.canSpawnForCategoryLocal(mobCategory, chunk.getPos());
             }
             if (canSpawn) {
-                spawnCategoryForChunk(mobCategory, level, chunk, spawnState::canSpawn, spawnState::afterSpawn,
-                    maxSpawns, level.paperConfig().entities.spawning.perPlayerMobSpawns ? level.getChunkSource().chunkMap::updatePlayerMobTypeMap : null);
+                // Canvas start - optimize natural spawning
+                spawnCategoryForChunk(mobCategory, spawnState, chunk, spawnState::canSpawn, spawnState::afterSpawn,
+                    maxSpawns, level.paperConfig().entities.spawning.perPlayerMobSpawns ? level.getChunkSource().chunkMap::updatePlayerMobTypeMap : null, closestPlayer);
+                // Canvas end - optimize natural spawning
                 // Paper end - Optional per player mob spawns
             }
         }
@@ -199,21 +254,28 @@ public final class NaturalSpawner {
     }
     // Paper end - Add mobcaps commands
 
+    // Canvas start - optimize natural spawning
     public static void spawnCategoryForChunk(
-        MobCategory category, ServerLevel level, LevelChunk chunk, NaturalSpawner.SpawnPredicate filter, NaturalSpawner.AfterSpawnCallback callback
-    ) {
-        // Paper start - Optional per player mob spawns
-        spawnCategoryForChunk(category, level, chunk, filter, callback, Integer.MAX_VALUE, null);
-    }
-    public static void spawnCategoryForChunk(
-        MobCategory category, ServerLevel level, LevelChunk chunk, NaturalSpawner.SpawnPredicate filter, NaturalSpawner.AfterSpawnCallback callback, final int maxSpawns, final Consumer<Entity> trackEntity
+        MobCategory category, NaturalSpawner.SpawnState spawnState, LevelChunk chunk, NaturalSpawner.SpawnPredicate filter, NaturalSpawner.AfterSpawnCallback callback, final int maxSpawns, final Consumer<Entity> trackEntity, net.minecraft.server.level.ServerPlayer closestPlayer
     ) {
-        // Paper end - Optional per player mob spawns
-        BlockPos randomPosWithin = getRandomPosWithin(level, chunk);
-        if (randomPosWithin.getY() >= level.getMinY() + 1) {
-            spawnCategoryForPosition(category, level, chunk, randomPosWithin, filter, callback, maxSpawns, trackEntity); // Paper - Optional per player mob spawns
+        final io.papermc.paper.threadedregions.RegionizedWorldData regionizedWorldData = spawnState.regionizedWorldData;
+        final ServerLevel world = regionizedWorldData.world;
+
+        final ChunkPos pos = chunk.getPos();
+        final RandomSource random = regionizedWorldData.simpleUnsafeLocalRandom;
+
+        // using the min bounds and then adding an RNG with bound of 16 means it won't escape this chunk
+        int i = pos.getMinBlockX() + random.nextInt(16);
+        int i1 = pos.getMinBlockZ() + random.nextInt(16);
+        int i2 = chunk.getHeight(Heightmap.Types.WORLD_SURFACE, i, i1) + 1;
+        int i3 = Mth.randomBetweenInclusive(random, world.getMinY(), i2);
+
+        BlockPos randomPosWithin = new BlockPos(i, i3, i1);
+        if (randomPosWithin.getY() >= world.getMinY() + 1) {
+            spawnCategoryForPosition(category, spawnState, chunk, randomPosWithin, filter, callback, maxSpawns, trackEntity, closestPlayer); // Paper - Optional per player mob spawns
         }
     }
+    // Canvas end - optimize natural spawning
 
     @VisibleForDebug
     public static void spawnCategoryForPosition(MobCategory category, ServerLevel level, BlockPos pos) {
@@ -228,53 +290,89 @@ public final class NaturalSpawner {
         NaturalSpawner.SpawnPredicate filter,
         NaturalSpawner.AfterSpawnCallback callback
     ) {
-        spawnCategoryForPosition(category, level, chunk, pos, filter, callback, Integer.MAX_VALUE, null);
+        throw new UnsupportedOperationException(); // Canvas - optimize natural spawning
     }
+    // Canvas start - optimize natural spawning
+
+    private static int clamp(int val, int min, int max) {
+        return Math.max(min, Math.min(val, max));
+    }
+
     public static void spawnCategoryForPosition(
-        MobCategory category, ServerLevel level, ChunkAccess chunk, BlockPos pos, NaturalSpawner.SpawnPredicate filter, NaturalSpawner.AfterSpawnCallback callback, final int maxSpawns, final @Nullable Consumer<Entity> trackEntity
+        MobCategory category, NaturalSpawner.SpawnState state, LevelChunk chunk, BlockPos pos, NaturalSpawner.SpawnPredicate filter, NaturalSpawner.AfterSpawnCallback callback, final int maxSpawns, final @Nullable Consumer<Entity> trackEntity, net.minecraft.server.level.ServerPlayer closestPlayer
     ) {
         // Paper end - Optional per player mob spawns
+        final io.papermc.paper.threadedregions.RegionizedWorldData regionizedWorldData = state.regionizedWorldData;
+        final ServerLevel level = regionizedWorldData.world;
+        final RandomSource fastRandom = regionizedWorldData.simpleUnsafeLocalRandom;
+        final net.minecraft.world.level.border.WorldBorder border = level.getWorldBorder();
+
+        final int minX = chunk.getPos().getMinBlockX();
+        final int minZ = chunk.getPos().getMinBlockZ();
+        final int maxX = chunk.getPos().getMaxBlockX();
+        final int maxZ = chunk.getPos().getMaxBlockZ();
+
         StructureManager structureManager = level.structureManager();
         ChunkGenerator generator = level.getChunkSource().getGenerator();
         int y = pos.getY();
-        BlockState blockState = level.getBlockStateIfLoadedAndInBounds(pos); // Paper - don't load chunks for mob spawn
-        if (blockState != null && !blockState.isRedstoneConductor(chunk, pos)) { // Paper - don't load chunks for mob spawn
+        // the pos is literally in bounds of this chunk, don't fetch the chunk again
+        BlockState blockState = chunk.getBlockStateFinal(pos.getX(), pos.getY(), pos.getZ());
+        if (!blockState.isRedstoneConductor(chunk, pos)) {
+    // Canvas end - optimize natural spawning
             BlockPos.MutableBlockPos mutableBlockPos = new BlockPos.MutableBlockPos();
             int i = 0;
 
-            for (int i1 = 0; i1 < 3; i1++) {
-                int x = pos.getX();
-                int z = pos.getZ();
+            for (int groupI = 0; groupI < 3; groupI++) { // Canvas - rename
+                int centerX = pos.getX(); // Canvas - rename
+                int centerZ = pos.getZ(); // Canvas - rename
                 int i2 = 6;
                 MobSpawnSettings.SpawnerData spawnerData = null;
                 SpawnGroupData spawnGroupData = null;
-                int ceil = Mth.ceil(level.random.nextFloat() * 4.0F);
+                int ceil = Mth.ceil(fastRandom.nextFloat() * 4.0F); // Canvas - use faster random
                 int i3 = 0;
 
-                for (int i4 = 0; i4 < ceil; i4++) {
-                    x += level.random.nextInt(6) - level.random.nextInt(6);
-                    z += level.random.nextInt(6) - level.random.nextInt(6);
+                // Canvas start - optimize natural spawning
+                for (int mobI = 0; mobI < ceil; mobI++) {
+                    // this cuts random calls in half, which is def a micro-opt, but
+                    // reduces possible random calls from 48 -> 24, while almost preserving
+                    // the vanilla spread logic. the vanilla logic uses triangular
+                    // distribution, and this one uses uniform distribution, meaning
+                    // mob clusters will spawn slightly more spread out, rather than clustered
+                    // more towards the center. barely noticeable, but worth mentioning
+                    long randomPair = fastRandom.nextLong();
+                    int x = centerX + (int)((randomPair & 0xFFFF) * 11L >> 16) - 5;
+                    int z = centerZ + (int)(((randomPair >>> 32) & 0xFFFF) * 11L >> 16) - 5;
+
+                    // clamp to the chunk bounds, so we can reduce chunk lookup calls since
+                    // all blocks will be within this chunk
+                    x = clamp(x, minX, maxX);
+                    z = clamp(z, minZ, maxZ);
                     mutableBlockPos.set(x, y, z);
+
                     double d = x + 0.5;
                     double d1 = z + 0.5;
-                    Player nearestPlayer = level.getNearestPlayer(d, y, d1, -1.0, false);
+                    // we run through an inRange set already, so why not try and use those results to help us out?
+                    // the biggest difference we would face here is like... 16 blocks max?
+                    Player nearestPlayer = closestPlayer == null ? level.getNearestPlayer(d, y, d1, -1.0, false) : closestPlayer;
                     if (nearestPlayer != null) {
                         double d2 = nearestPlayer.distanceToSqr(d, y, d1);
-                        if (level.isLoadedAndInBounds(mutableBlockPos) && isRightDistanceToPlayerAndSpawnPoint(level, chunk, mutableBlockPos, d2)) { // Paper - don't load chunks for mob spawn
+                        if (border.isWithinBounds(mutableBlockPos) && isRightDistanceToPlayerAndSpawnPoint(level, chunk, mutableBlockPos, d2)) { // Paper - don't load chunks for mob spawn
                             if (spawnerData == null) {
                                 Optional<MobSpawnSettings.SpawnerData> randomSpawnMobAt = getRandomSpawnMobAt(
-                                    level, structureManager, generator, category, level.random, mutableBlockPos
+                                    // passing chunk here is ok, since the pos is clamped to the chunk
+                                    chunk, structureManager, generator, category, fastRandom, mutableBlockPos
+                // Canvas end - optimize natural spawning
                                 );
                                 if (randomSpawnMobAt.isEmpty()) {
                                     break;
                                 }
 
                                 spawnerData = randomSpawnMobAt.get();
-                                ceil = spawnerData.minCount() + level.random.nextInt(1 + spawnerData.maxCount() - spawnerData.minCount());
+                                ceil = spawnerData.minCount() + fastRandom.nextInt(1 + spawnerData.maxCount() - spawnerData.minCount()); // Canvas - optimize natural spawning
                             }
 
                             // Paper start - PreCreatureSpawnEvent
-                            PreSpawnStatus doSpawning = isValidSpawnPostitionForType(level, category, structureManager, generator, spawnerData, mutableBlockPos, d2);
+                            PreSpawnStatus doSpawning = isValidSpawnPostitionForType(level, category, structureManager, generator, spawnerData, mutableBlockPos, chunk, fastRandom, d2); // Canvas - pass chunk and random
                             // Paper start - per player mob count backoff
                             if (doSpawning == PreSpawnStatus.ABORT || doSpawning == PreSpawnStatus.CANCELLED) {
                                 level.getChunkSource().chunkMap.updateFailurePlayerMobTypeMap(mutableBlockPos.getX() >> 4, mutableBlockPos.getZ() >> 4, category);
@@ -291,7 +389,7 @@ public final class NaturalSpawner {
                                     return;
                                 }
 
-                                mobForSpawn.snapTo(d, y, d1, level.random.nextFloat() * 360.0F, 0.0F);
+                                mobForSpawn.snapTo(d, y, d1, fastRandom.nextFloat() * 360.0F, 0.0F); // Canvas - optimize natural spawning
                                 if (isValidPositionForMob(level, mobForSpawn, d2)) {
                                     spawnGroupData = mobForSpawn.finalizeSpawn(
                                         level, level.getCurrentDifficultyAt(mobForSpawn.blockPosition()), EntitySpawnReason.NATURAL, spawnGroupData
@@ -335,8 +433,12 @@ public final class NaturalSpawner {
                 && respawnData.pos().closerToCenterThan(new Vec3(pos.getX() + 0.5, pos.getY(), pos.getZ() + 0.5), 24.0)) {
                 return false;
             } else {
-                ChunkPos chunkPos = new ChunkPos(pos);
-                return Objects.equals(chunkPos, chunk.getPos()) || level.canSpawnEntitiesInChunk(chunkPos);
+                // Canvas start - optimize natural spawning
+                // 'pos' is a mutable block pos clamped to the passed ChunkAccess, so this check is stupid
+                // ChunkPos chunkPos = new ChunkPos(pos);
+                // return Objects.equals(chunkPos, chunk.getPos()) || level.canSpawnEntitiesInChunk(chunkPos);
+                return true;
+                // Canvas end - optimize natural spawning
             }
         }
     }
@@ -356,6 +458,8 @@ public final class NaturalSpawner {
         ChunkGenerator generator,
         MobSpawnSettings.SpawnerData data,
         BlockPos.MutableBlockPos pos,
+        LevelChunk chunk, // Canvas - pass chunk
+        RandomSource random, // Canvas - pass random
         double distance
     ) {
         EntityType<?> entityType = data.type();
@@ -377,9 +481,9 @@ public final class NaturalSpawner {
                     || !(distance > entityType.getCategory().getDespawnDistance() * entityType.getCategory().getDespawnDistance())
             )
             && entityType.canSummon()
-            && canSpawnMobAt(level, structureManager, generator, category, data, pos)
+            && canSpawnMobAt(chunk, structureManager, generator, category, data, pos) // Canvas - pass chunk
             && SpawnPlacements.isSpawnPositionOk(entityType, level, pos)
-            && SpawnPlacements.checkSpawnRules(entityType, level, EntitySpawnReason.NATURAL, pos, level.random)
+            && SpawnPlacements.checkSpawnRules(entityType, level, EntitySpawnReason.NATURAL, pos, random) // Canvas - pass faster random
             && level.noCollision(entityType.getSpawnAABB(pos.getX() + 0.5, pos.getY(), pos.getZ() + 0.5));
         return success ? PreSpawnStatus.SUCCESS : PreSpawnStatus.FAIL; // Paper - PreCreatureSpawnEvent
     }
@@ -409,30 +513,30 @@ public final class NaturalSpawner {
     }
 
     private static Optional<MobSpawnSettings.SpawnerData> getRandomSpawnMobAt(
-        ServerLevel level, StructureManager structureManager, ChunkGenerator generator, MobCategory category, RandomSource random, BlockPos pos
+        LevelChunk chunk, StructureManager structureManager, ChunkGenerator generator, MobCategory category, RandomSource random, BlockPos pos // Canvas - pass chunk
     ) {
-        Holder<Biome> biome = level.getBiome(pos);
+        Holder<Biome> biome = chunk.level.getBiome(pos); // Canvas - pass chunk
         return category == MobCategory.WATER_AMBIENT && biome.is(BiomeTags.REDUCED_WATER_AMBIENT_SPAWNS) && random.nextFloat() < 0.98F
             ? Optional.empty()
-            : mobsAt(level, structureManager, generator, category, pos, biome).getRandom(random);
+            : mobsAt(chunk, structureManager, generator, category, pos, biome).getRandom(random); // Canvas - pass chunk
     }
 
     private static boolean canSpawnMobAt(
-        ServerLevel level, StructureManager structureManager, ChunkGenerator generator, MobCategory category, MobSpawnSettings.SpawnerData data, BlockPos pos
+        LevelChunk chunk, StructureManager structureManager, ChunkGenerator generator, MobCategory category, MobSpawnSettings.SpawnerData data, BlockPos pos // Canvas - pass chunk
     ) {
-        return mobsAt(level, structureManager, generator, category, pos, null).contains(data);
+        return mobsAt(chunk, structureManager, generator, category, pos, null).contains(data); // Canvas - pass chunk
     }
 
     private static WeightedList<MobSpawnSettings.SpawnerData> mobsAt(
-        ServerLevel level, StructureManager structureManager, ChunkGenerator generator, MobCategory category, BlockPos pos, @Nullable Holder<Biome> biome
+        LevelChunk chunk, StructureManager structureManager, ChunkGenerator generator, MobCategory category, BlockPos pos, @Nullable Holder<Biome> biome // Canvas - pass chunk
     ) {
-        return isInNetherFortressBounds(pos, level, category, structureManager)
+        return isInNetherFortressBounds(pos, chunk, category, structureManager) // Canvas - pass chunk
             ? NetherFortressStructure.FORTRESS_ENEMIES
-            : generator.getMobsAt(biome != null ? biome : level.getBiome(pos), structureManager, category, pos);
+            : generator.getMobsAt(biome != null ? biome : chunk.level.getBiome(pos), structureManager, category, pos, chunk.getAllReferences()); // Canvas - reduce chunk lookup
     }
 
-    public static boolean isInNetherFortressBounds(BlockPos pos, ServerLevel level, MobCategory category, StructureManager structureManager) {
-        if (category == MobCategory.MONSTER && level.getBlockState(pos.below()).is(Blocks.NETHER_BRICKS)) {
+    public static boolean isInNetherFortressBounds(BlockPos pos, LevelChunk chunk, MobCategory category, StructureManager structureManager) { // Canvas - pass chunk
+        if (category == MobCategory.MONSTER && chunk.getBlockState(pos.below()).is(Blocks.NETHER_BRICKS)) { // Canvas - reduce chunk lookup
             Structure structure = structureManager.registryAccess().lookupOrThrow(Registries.STRUCTURE).getValue(BuiltinStructures.FORTRESS);
             return structure != null && structureManager.getStructureAt(pos, structure).isValid();
         } else {
@@ -575,18 +679,22 @@ public final class NaturalSpawner {
         private @Nullable BlockPos lastCheckedPos;
         private @Nullable EntityType<?> lastCheckedType;
         private double lastCharge;
+        public final io.papermc.paper.threadedregions.RegionizedWorldData regionizedWorldData; // Canvas - optimize natural spawning
+        public final int[] limits; // Canvas - optimize natural spawning
 
         SpawnState(
             int spawnableChunkCount,
             Object2IntOpenHashMap<MobCategory> mobCategoryCounts,
             PotentialCalculator spawnPotential,
-            LocalMobCapCalculator localMobCapCalculator
+            LocalMobCapCalculator localMobCapCalculator, io.papermc.paper.threadedregions.RegionizedWorldData regionizedWorldData, int[] limits // Canvas - optimize natural spawning
         ) {
             this.spawnableChunkCount = spawnableChunkCount;
             this.mobCategoryCounts = mobCategoryCounts;
             this.spawnPotential = spawnPotential;
             this.localMobCapCalculator = localMobCapCalculator;
             this.unmodifiableMobCategoryCounts = Object2IntMaps.unmodifiable(mobCategoryCounts);
+            this.regionizedWorldData = regionizedWorldData; // Canvas - optimize natural spawning
+            this.limits = limits; // Canvas - optimize natural spawning
         }
 
         private boolean canSpawn(EntityType<?> entityType, BlockPos pos, ChunkAccess chunk) {
