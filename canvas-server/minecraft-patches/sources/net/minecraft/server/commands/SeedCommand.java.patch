--- a/net/minecraft/server/commands/SeedCommand.java
+++ b/net/minecraft/server/commands/SeedCommand.java
@@ -15,6 +_,18 @@
                     long seed = commandContext.getSource().getLevel().getSeed();
                     Component component = ComponentUtils.copyOnClickText(String.valueOf(seed));
                     commandContext.getSource().sendSuccess(() -> Component.translatable("commands.seed.success", component), false);
+                    // Canvas start - Secure seed V2 (Luminol/Matter port)
+                    if (io.canvasmc.canvas.Config.INSTANCE != null
+                        && io.canvasmc.canvas.Config.INSTANCE.secureSeedProtection.enabled
+                        && io.canvasmc.canvas.Config.INSTANCE.secureSeedProtection.version >= 2
+                        && io.canvasmc.canvas.world.levelgen.Globals.worldSeed != null) {
+                        String featureSeedStr = io.canvasmc.canvas.world.levelgen.HashingV2.featureSeedToString(
+                            io.canvasmc.canvas.world.levelgen.Globals.worldSeed);
+                        Component featureSeedComponent = ComponentUtils.copyOnClickText(featureSeedStr);
+                        commandContext.getSource().sendSuccess(
+                            () -> Component.translatable("Feature seed: %s", featureSeedComponent), false);
+                    }
+                    // Canvas end - Secure seed V2
                     return (int)seed;
                 })
         );
