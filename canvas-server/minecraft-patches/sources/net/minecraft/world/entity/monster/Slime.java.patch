--- a/net/minecraft/world/entity/monster/Slime.java
+++ b/net/minecraft/world/entity/monster/Slime.java
@@ -323,7 +_,15 @@
             }

             ChunkPos chunkPos = new ChunkPos(pos);
-            boolean flag = level.getMinecraftWorld().paperConfig().entities.spawning.allChunksAreSlimeChunks || WorldgenRandom.seedSlimeChunk(chunkPos.x, chunkPos.z, ((WorldGenLevel) level).getSeed(), level.getMinecraftWorld().spigotConfig.slimeSeed).nextInt(10) == 0; // Paper
+            // Canvas start - Secure seed V2 (Luminol/Matter port) - cached secure slime-chunk lookup
+            boolean isSlimeChunk;
+            if (io.canvasmc.canvas.Config.INSTANCE != null
+                && io.canvasmc.canvas.Config.INSTANCE.secureSeedProtection.enabled
+                && io.canvasmc.canvas.world.levelgen.Globals.isActive()) {
+                isSlimeChunk = level.getChunk(chunkPos.x, chunkPos.z).canvas$isSlimeChunk();
+            } else {
+                isSlimeChunk = WorldgenRandom.seedSlimeChunk(chunkPos.x, chunkPos.z, ((WorldGenLevel) level).getSeed(), level.getMinecraftWorld().spigotConfig.slimeSeed).nextInt(10) == 0;
+            }
+            boolean flag = level.getMinecraftWorld().paperConfig().entities.spawning.allChunksAreSlimeChunks || isSlimeChunk;
+            // Canvas end - Secure seed V2
                 // Paper start - Replace rules for Height in Slime Chunks
                 final double maxHeightSlimeChunk = level.getMinecraftWorld().paperConfig().entities.spawning.slimeSpawnHeight.slimeChunk.maximum;
                 if (random.nextInt(10) == 0 && flag && pos.getY() < maxHeightSlimeChunk) {
