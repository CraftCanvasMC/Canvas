--- a/net/minecraft/world/level/levelgen/RandomState.java
+++ b/net/minecraft/world/level/levelgen/RandomState.java
@@ -35,10 +_,28 @@
     }

     private RandomState(NoiseGeneratorSettings settings, HolderGetter<NormalNoise.NoiseParameters> noiseParametersGetter, final long levelSeed) {
-        this.random = settings.getRandomSource().newInstance(levelSeed).forkPositional();
+        // this.random = settings.getRandomSource().newInstance(levelSeed).forkPositional(); // Canvas - Secure seed V2 (Luminol/Matter port)
+        // Canvas start - Secure seed V2 (Luminol/Matter port)
+        final long[] secureWorldSeed = (io.canvasmc.canvas.Config.INSTANCE.secureSeedProtection.enabled
+                && io.canvasmc.canvas.Config.INSTANCE.secureSeedProtection.version >= 2)
+            ? io.canvasmc.canvas.world.levelgen.Globals.worldSeed
+            : null;
+
+        long terrainSeed = (secureWorldSeed != null)
+            ? io.canvasmc.canvas.world.levelgen.HashingV2.getTerrainSeed(secureWorldSeed, io.canvasmc.canvas.world.levelgen.HashingV2.TerrainType.BASE_TERRAIN)
+            : levelSeed;
+        long aquiferSeed = (secureWorldSeed != null)
+            ? io.canvasmc.canvas.world.levelgen.HashingV2.getTerrainSeed(secureWorldSeed, io.canvasmc.canvas.world.levelgen.HashingV2.TerrainType.AQUIFER)
+            : levelSeed;
+        long oreSeed = (secureWorldSeed != null)
+            ? io.canvasmc.canvas.world.levelgen.HashingV2.getTerrainSeed(secureWorldSeed, io.canvasmc.canvas.world.levelgen.HashingV2.TerrainType.ORE)
+            : levelSeed;
+        // Canvas end - Secure seed V2 (Luminol/Matter port)
+        this.random = settings.getRandomSource().newInstance(terrainSeed).forkPositional(); // Canvas - Secure seed V2
         this.noises = noiseParametersGetter;
-        this.aquiferRandom = this.random.fromHashOf(Identifier.withDefaultNamespace("aquifer")).forkPositional();
-        this.oreRandom = this.random.fromHashOf(Identifier.withDefaultNamespace("ore")).forkPositional();
+        // this.aquiferRandom = this.random.fromHashOf(Identifier.withDefaultNamespace("aquifer")).forkPositional(); // Canvas - Secure seed V2
+        // this.oreRandom = this.random.fromHashOf(Identifier.withDefaultNamespace("ore")).forkPositional(); // Canvas - Secure seed V2
+        this.aquiferRandom = settings.getRandomSource().newInstance(aquiferSeed).forkPositional(); // Canvas - Secure seed V2
+        this.oreRandom = settings.getRandomSource().newInstance(oreSeed).forkPositional(); // Canvas - Secure seed V2
         this.noiseIntances = new ConcurrentHashMap<>();
         this.positionalRandoms = new ConcurrentHashMap<>();
         this.surfaceSystem = new SurfaceSystem(this, settings.defaultBlock(), settings.seaLevel(), this.random);
@@ -48,6 +_,15 @@
             private final Map<DensityFunction, DensityFunction> wrapped = new HashMap<>();

             private RandomSource newLegacyInstance(long seed) {
+                // Canvas start - Secure seed V2 (Luminol/Matter port)
+                if (secureWorldSeed != null) {
+                    long climateSeed = io.canvasmc.canvas.world.levelgen.HashingV2.getTerrainSeed(
+                        secureWorldSeed, io.canvasmc.canvas.world.levelgen.HashingV2.TerrainType.CLIMATE);
+                    return new io.canvasmc.canvas.world.levelgen.WorldgenCryptoRandom(
+                        0, 0, io.canvasmc.canvas.world.levelgen.Salt.UNDEFINED,
+                        climateSeed + seed);
+                }
+                // Canvas end - Secure seed V2
                 return new LegacyRandomSource(levelSeed + seed);
             }

@@ -85,7 +_,12 @@
                     return blendedNoise.withNewRandom(randomSource);
                 } else {
                     return (DensityFunction)(densityFunction instanceof DensityFunctions.EndIslandDensityFunction
-                        ? new DensityFunctions.EndIslandDensityFunction(levelSeed)
+                        // Canvas start - Secure seed V2 (Luminol/Matter port)
+                        // ? new DensityFunctions.EndIslandDensityFunction(levelSeed)
+                        ? new DensityFunctions.EndIslandDensityFunction(secureWorldSeed != null
+                            ? io.canvasmc.canvas.world.levelgen.HashingV2.getTerrainSeed(secureWorldSeed, io.canvasmc.canvas.world.levelgen.HashingV2.TerrainType.END_ISLANDS)
+                            : levelSeed)
+                        // Canvas end - Secure seed V2
                         : densityFunction);
                 }
             }
