--- a/net/minecraft/world/level/levelgen/structure/placement/StructurePlacement.java
+++ b/net/minecraft/world/level/levelgen/structure/placement/StructurePlacement.java
@@ -119,8 +_,16 @@
     public abstract StructurePlacementType<?> type();

     private static boolean probabilityReducer(long levelSeed, int regionX, int regionZ, int salt, float frequency, @javax.annotation.Nullable Integer saltOverride) { // Paper - Add missing structure set seed configs; ignore here
-        WorldgenRandom worldgenRandom = new WorldgenRandom(new LegacyRandomSource(0L));
-        worldgenRandom.setLargeFeatureWithSalt(levelSeed, regionX, regionZ, salt);
+        // Canvas start - Secure seed V2 (Luminol/Matter port)
+        WorldgenRandom worldgenRandom;
+        if (io.canvasmc.canvas.world.levelgen.Globals.isActive()) {
+            worldgenRandom = new io.canvasmc.canvas.world.levelgen.WorldgenCryptoRandom(
+                regionX, regionZ, io.canvasmc.canvas.world.levelgen.Salt.UNDEFINED, salt);
+        } else {
+            worldgenRandom = new WorldgenRandom(new LegacyRandomSource(0L));
+            worldgenRandom.setLargeFeatureWithSalt(levelSeed, regionX, regionZ, salt);
+        }
+        // Canvas end - Secure seed V2
         return worldgenRandom.nextFloat() < (saltOverride != null ? frequency * saltOverride : frequency); // Paper - Add missing structure set seed configs
     }

