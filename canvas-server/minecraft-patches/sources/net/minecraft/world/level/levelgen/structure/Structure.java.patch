--- a/net/minecraft/world/level/levelgen/structure/Structure.java
+++ b/net/minecraft/world/level/levelgen/structure/Structure.java
@@ -249,6 +_,12 @@
         }

         private static WorldgenRandom makeRandom(long seed, ChunkPos chunkPos) {
+            // Canvas start - Secure seed V2 (Luminol/Matter port)
+            if (io.canvasmc.canvas.world.levelgen.Globals.isActive()) {
+                return new io.canvasmc.canvas.world.levelgen.WorldgenCryptoRandom(
+                    chunkPos.x, chunkPos.z, io.canvasmc.canvas.world.levelgen.Salt.GENERATE_FEATURE, seed);
+            }
+            // Canvas end - Secure seed V2
             WorldgenRandom worldgenRandom = new WorldgenRandom(new LegacyRandomSource(0L));
             worldgenRandom.setLargeFeatureSeed(seed, chunkPos.x, chunkPos.z);
             return worldgenRandom;
