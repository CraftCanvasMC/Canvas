--- a/net/minecraft/world/level/levelgen/WorldOptions.java
+++ b/net/minecraft/world/level/levelgen/WorldOptions.java
@@ -9,8 +_,18 @@
 import org.apache.commons.lang3.StringUtils;

 public class WorldOptions {
+    // Canvas start - Secure seed V2 (Luminol/Matter port)
+    private static final boolean canvas$secureSeedEnabled = io.canvasmc.canvas.Config.INSTANCE != null
+        && io.canvasmc.canvas.Config.INSTANCE.secureSeedProtection.enabled
+        && io.canvasmc.canvas.Config.INSTANCE.secureSeedProtection.version >= 2;
     public static final MapCodec<WorldOptions> CODEC = RecordCodecBuilder.mapCodec(
-        instance -> instance.group(
+        instance -> canvas$secureSeedEnabled
+            ? instance.group(
+                Codec.LONG.fieldOf("seed").stable().forGetter(WorldOptions::seed),
+                Codec.STRING.fieldOf("feature_seed").orElseGet(() -> io.canvasmc.canvas.world.levelgen.HashingV2.featureSeedToString(io.canvasmc.canvas.world.levelgen.HashingV2.createRandomFeatureSeed())).stable().forGetter(WorldOptions::featureSeedString),
+                Codec.BOOL.fieldOf("generate_features").orElse(true).stable().forGetter(WorldOptions::generateStructures),
+                Codec.BOOL.fieldOf("bonus_chest").orElse(false).stable().forGetter(WorldOptions::generateBonusChest),
+                Codec.STRING.lenientOptionalFieldOf("legacy_custom_options").stable().forGetter(worldOptions -> worldOptions.legacyCustomOptions)
+            ).apply(instance, instance.stable(WorldOptions::canvas$createWithFeatureSeed))
+            : instance.group(
                 Codec.LONG.fieldOf("seed").stable().forGetter(WorldOptions::seed),
                 Codec.BOOL.fieldOf("generate_features").orElse(true).stable().forGetter(WorldOptions::generateStructures),
                 Codec.BOOL.fieldOf("bonus_chest").orElse(false).stable().forGetter(WorldOptions::generateBonusChest),
@@ -18,8 +_,10 @@
             )
             .apply(instance, instance.stable(WorldOptions::new))
     );
+    // Canvas end - Secure seed V2
     public static final WorldOptions DEMO_OPTIONS = new WorldOptions("North Carolina".hashCode(), true, true);
     private final long seed;
+    private long[] featureSeed = io.canvasmc.canvas.world.levelgen.HashingV2.createRandomFeatureSeed(); // Canvas - Secure seed V2
     private final boolean generateStructures;
     private final boolean generateBonusChest;
     private final Optional<String> legacyCustomOptions;
@@ -36,6 +_,30 @@
         return new WorldOptions(randomSeed(), false, false);
     }

+    // Canvas start - Secure seed V2 (Luminol/Matter port)
+    public WorldOptions(long seed, long[] featureSeed, boolean generateStructures, boolean bonusChest) {
+        this(seed, generateStructures, bonusChest, Optional.empty());
+        if (featureSeed != null) {
+            this.featureSeed = featureSeed;
+        }
+    }
+
+    private static WorldOptions canvas$createWithFeatureSeed(long seed, String featureSeedString, boolean generateStructures, boolean bonusChest, Optional<String> legacyCustomOptions) {
+        long[] parsed = io.canvasmc.canvas.world.levelgen.HashingV2.parseFeatureSeed(featureSeedString);
+        if (parsed == null) {
+            parsed = io.canvasmc.canvas.world.levelgen.HashingV2.createRandomFeatureSeed();
+        }
+        WorldOptions options = new WorldOptions(seed, generateStructures, bonusChest, legacyCustomOptions);
+        options.featureSeed = parsed;
+        return options;
+    }
+
+    public long[] featureSeed() { return this.featureSeed; }
+
+    private String featureSeedString() {
+        return io.canvasmc.canvas.world.levelgen.HashingV2.featureSeedToString(this.featureSeed);
+    }
+    // Canvas end - Secure seed V2
+
     private WorldOptions(long seed, boolean generateStructures, boolean generateBonusChest, Optional<String> legacyCustomOptions) {
         this.seed = seed;
         this.generateStructures = generateStructures;
