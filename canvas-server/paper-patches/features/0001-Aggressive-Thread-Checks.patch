From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Dueris <duerismc@gmail.com>
Date: Mon, 13 Apr 2026 21:04:01 -0700
Subject: [PATCH] Aggressive Thread Checks


diff --git a/src/main/java/ca/spottedleaf/moonrise/common/util/TickThread.java b/src/main/java/ca/spottedleaf/moonrise/common/util/TickThread.java
index 43f28f75c877fed9d83c098804cbc54a267f9e24..49bdb4563170e1622365c1ff9f74e4c18e17acdc 100644
--- a/src/main/java/ca/spottedleaf/moonrise/common/util/TickThread.java
+++ b/src/main/java/ca/spottedleaf/moonrise/common/util/TickThread.java
@@ -309,4 +309,46 @@ public class TickThread extends Thread {
             return ((entity.hasNullCallback() || entity.isRemoved())) && isTickThreadFor((net.minecraft.server.level.ServerLevel)world, entity.chunkPosition());
         }
     }
+    // Canvas start - thread check more API
+    public static void canvas$tryEnsureThreadCheck(final Entity entity, String reason) {
+        final int mode = io.canvasmc.canvas.GlobalConfiguration.getInstance().extraThreadChecksLevel;
+        if (mode == 0) {
+            return;
+        }
+
+        if (entity instanceof net.minecraft.server.level.ServerPlayer serverPlayer
+            && (serverPlayer.joining || serverPlayer.isRemoved())) {
+            // if player is joining or removed, not in world
+            // we really only care about tick guarding when it is DURING the player tick lifecycle
+            return;
+        }
+
+        if (mode == 1) {
+            if (!isTickThreadFor(entity)) {
+                LOGGER.warn("Unsafe access detected for Spigot/Paper API, please contact the related plugin developers to resolve this");
+                LOGGER.warn(reason, new Throwable());
+            }
+            return;
+        }
+
+        ensureTickThread(entity, reason);
+    }
+
+    public static void canvas$tryEnsureThreadCheck(final Level level, final double blockX, final double blockZ, String reason) {
+        final int mode = io.canvasmc.canvas.GlobalConfiguration.getInstance().extraThreadChecksLevel;
+        if (mode == 0) {
+            return;
+        }
+
+        if (mode == 1) {
+            if (!isTickThreadFor(level, blockX, blockZ)) {
+                LOGGER.warn("Unsafe access detected for Spigot/Paper API, please contact the related plugin developers to resolve this");
+                LOGGER.warn(reason, new Throwable());
+            }
+            return;
+        }
+
+        ensureTickThread(level, blockX, blockZ, reason);
+    }
+    // Canvas end - thread check more API
 }
diff --git a/src/main/java/org/bukkit/craftbukkit/entity/CraftEntity.java b/src/main/java/org/bukkit/craftbukkit/entity/CraftEntity.java
index d43b1a6e6a8ac2a21b0a048e838115b0403170dd..cc88008baae5caac28d01d6e6bc6fd25cd0a3c13 100644
--- a/src/main/java/org/bukkit/craftbukkit/entity/CraftEntity.java
+++ b/src/main/java/org/bukkit/craftbukkit/entity/CraftEntity.java
@@ -199,6 +199,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public Vector getVelocity() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access entity velocity off region"); // Canvas - aggressive thread checks
         return CraftVector.toBukkit(this.entity.getDeltaMovement());
     }
 
@@ -206,6 +207,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
     public void setVelocity(Vector velocity) {
         Preconditions.checkArgument(velocity != null, "velocity");
         velocity.checkFinite();
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set entity velocity off region"); // Canvas - aggressive thread checks
         // Paper start - Warn server owners when plugins try to set super high velocities
         if (!(this instanceof org.bukkit.entity.Projectile || this instanceof org.bukkit.entity.Minecart) && isUnsafeVelocity(velocity)) {
             CraftServer.excessiveVelEx = new Exception("Excessive velocity set detected: tried to set velocity of entity " + entity.getScoreboardName() + " id #" + getEntityId() + " to (" + velocity.getX() + "," + velocity.getY() + "," + velocity.getZ() + ").");
@@ -240,22 +242,26 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public double getHeight() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access entity height off region"); // Canvas - aggressive thread checks
         return this.getHandle().getBbHeight();
     }
 
     @Override
     public double getWidth() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access entity width off region"); // Canvas - aggressive thread checks
         return this.getHandle().getBbWidth();
     }
 
     @Override
     public BoundingBox getBoundingBox() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access entity bounding box off region"); // Canvas - aggressive thread checks
         AABB bb = this.getHandle().getBoundingBox();
         return new BoundingBox(bb.minX, bb.minY, bb.minZ, bb.maxX, bb.maxY, bb.maxZ);
     }
 
     @Override
     public boolean isOnGround() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access if entity is off the ground off the owning region"); // Canvas - aggressive thread checks
         if (this.entity instanceof AbstractArrow abstractArrow) {
             return abstractArrow.isInGround();
         }
@@ -264,6 +270,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean isInWater() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if entity is in water off region"); // Canvas - aggressive thread checks
         return this.entity.isInWater();
     }
 
@@ -274,6 +281,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public void setRotation(float yaw, float pitch) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set rotation of entity off region"); // Canvas - aggressive thread checks
         NumberConversions.checkFinite(pitch, "pitch not finite");
         NumberConversions.checkFinite(yaw, "yaw not finite");
 
@@ -364,6 +372,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public void lookAt(double x, double y, double z, LookAnchor entityAnchor) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot make entity look at anchor off region"); // Canvas - aggressive thread checks
         this.getHandle().lookAt(toNmsAnchor(entityAnchor), new net.minecraft.world.phys.Vec3(x, y, z));
     }
 
@@ -384,7 +393,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
     @Override
     public List<org.bukkit.entity.Entity> getNearbyEntities(double x, double y, double z) {
         Preconditions.checkState(!this.entity.generation, "Cannot get nearby entities during world generation");
-        org.spigotmc.AsyncCatcher.catchOp("getNearbyEntities"); // Spigot
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get nearby entities off region"); // Canvas - aggressive thread checks
 
         List<Entity> entities = this.getHandle().level().getEntities(this.entity, this.entity.getBoundingBox().inflate(x, y, z), Predicates.alwaysTrue());
         List<org.bukkit.entity.Entity> result = new java.util.ArrayList<>(entities.size());
@@ -397,21 +406,25 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public int getEntityId() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity id off region"); // Canvas - aggressive thread checks
         return this.getHandle().getId();
     }
 
     @Override
     public int getFireTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get fire ticks off region"); // Canvas - aggressive thread checks
         return this.getHandle().getRemainingFireTicks();
     }
 
     @Override
     public int getMaxFireTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get max fire ticks off region"); // Canvas - aggressive thread checks
         return this.getHandle().getFireImmuneTicks();
     }
 
     @Override
     public void setFireTicks(int ticks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set fire ticks off region"); // Canvas - aggressive thread checks
         this.getHandle().setRemainingFireTicks(ticks);
     }
 
@@ -423,65 +436,77 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public void setVisualFire(final TriState fire) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set visual fire off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(fire != null, "TriState cannot be null");
         this.getHandle().visualFire = fire;
     }
 
     @Override
     public boolean isVisualFire() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot fetch is visual fire off region"); // Canvas - aggressive thread checks
         return getVisualFire().toBooleanOrElse(false);
     }
 
     @Override
     public TriState getVisualFire() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot fetch is visual fire off region"); // Canvas - aggressive thread checks
         return this.getHandle().visualFire;
     }
 
     @Override
     public int getFreezeTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get freeze ticks off region"); // Canvas - aggressive thread checks
         return this.getHandle().getTicksFrozen();
     }
 
     @Override
     public int getMaxFreezeTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get max freeze ticks off region"); // Canvas - aggressive thread checks
         return this.getHandle().getTicksRequiredToFreeze();
     }
 
     @Override
     public void setFreezeTicks(int ticks) {
         Preconditions.checkArgument(0 <= ticks, "Ticks (%s) cannot be less than 0", ticks);
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set freeze ticks off region"); // Canvas - aggressive thread checks
 
         this.getHandle().setTicksFrozen(ticks);
     }
 
     @Override
     public boolean isFrozen() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot fetch if entity is frozen off region"); // Canvas - aggressive thread checks
         return this.getHandle().isFullyFrozen();
     }
 
     @Override
     public boolean isFreezeTickingLocked() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if the entity has freezing ticks locked off region"); // Canvas - aggressive thread checks
         return this.entity.freezeLocked;
     }
 
     @Override
     public void lockFreezeTicks(boolean locked) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot lock or unlock freeze ticks off region"); // Canvas - aggressive thread checks
         this.entity.freezeLocked = locked;
     }
 
     @Override
     public void remove() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot remove entity off region"); // Canvas - aggressive thread checks
         this.entity.pluginRemoved = true;
         this.entity.discard(this.getHandle().generation ? null : EntityRemoveEvent.Cause.PLUGIN);
     }
 
     @Override
     public boolean isDead() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is dead off region"); // Canvas - aggressive thread checks
         return !this.entity.isAlive();
     }
 
     @Override
     public boolean isValid() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is valid off region"); // Canvas - aggressive thread checks
         return this.entity.isAlive() && this.entity.valid;
     }
 
@@ -492,22 +517,26 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean isPersistent() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is persistent off region"); // Canvas - aggressive thread checks
         return this.entity.persist;
     }
 
     @Override
     public void setPersistent(boolean persistent) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set if entity is persistent off region"); // Canvas - aggressive thread checks
         this.entity.persist = persistent;
     }
 
     @Override
     public org.bukkit.entity.Entity getPassenger() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get passenger off region"); // Canvas - aggressive thread checks
         return this.isEmpty() ? null : this.getHandle().getPassengers().getFirst().getBukkitEntity();
     }
 
     @Override
     public boolean setPassenger(org.bukkit.entity.Entity passenger) {
         Preconditions.checkArgument(!this.equals(passenger), "Entity cannot ride itself.");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set entity passengers off region"); // Canvas - aggressive thread checks
         if (passenger instanceof CraftEntity) {
             this.eject();
             return ((CraftEntity) passenger).getHandle().startRiding(this.getHandle());
@@ -518,6 +547,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public List<org.bukkit.entity.Entity> getPassengers() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity passengers off region"); // Canvas - aggressive thread checks
         return Lists.newArrayList(Lists.transform(this.getHandle().getPassengers(), (Function<Entity, org.bukkit.entity.Entity>) Entity::getBukkitEntity));
     }
 
@@ -525,6 +555,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
     public boolean addPassenger(org.bukkit.entity.Entity passenger) {
         Preconditions.checkArgument(passenger != null, "Entity passenger cannot be null");
         Preconditions.checkArgument(!this.equals(passenger), "Entity cannot ride itself.");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot add passenger off region"); // Canvas - aggressive thread checks
 
         return ((CraftEntity) passenger).getHandle().startRiding(this.getHandle(), true, true);
     }
@@ -532,6 +563,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
     @Override
     public boolean removePassenger(org.bukkit.entity.Entity passenger) {
         Preconditions.checkArgument(passenger != null, "Entity passenger cannot be null");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot remove passenger off region"); // Canvas - aggressive thread checks
 
         ((CraftEntity) passenger).getHandle().stopRiding();
         return true;
@@ -539,11 +571,13 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean isEmpty() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if entity has passengers off region"); // Canvas - aggressive thread checks
         return !this.getHandle().isVehicle();
     }
 
     @Override
     public boolean eject() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot eject passengers off region"); // Canvas - aggressive thread checks
         if (this.isEmpty()) {
             return false;
         }
@@ -554,27 +588,32 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public ItemStack getPickItemStack() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get pick item off region"); // Canvas - aggressive thread checks
         net.minecraft.world.item.ItemStack stack = this.getHandle().getPickResult();
         return stack == null ? ItemStack.empty() : stack.asBukkitCopy();
     }
 
     @Override
     public float getFallDistance() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get fall distance off region"); // Canvas - aggressive thread checks
         return (float) this.getHandle().fallDistance;
     }
 
     @Override
     public void setFallDistance(float distance) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set fall distance off region"); // Canvas - aggressive thread checks
         this.getHandle().fallDistance = distance;
     }
 
     @Override
     public void setLastDamageCause(EntityDamageEvent event) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set last damage cause off region"); // Canvas - aggressive thread checks
         this.lastDamageEvent = event;
     }
 
     @Override
     public EntityDamageEvent getLastDamageCause() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get last damage cause off region"); // Canvas - aggressive thread checks
         return this.lastDamageEvent;
     }
 
@@ -585,12 +624,14 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public int getTicksLived() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get ticks lived off region"); // Canvas - aggressive thread checks
         return this.getHandle().totalEntityAge;
     }
 
     @Override
     public void setTicksLived(int value) {
         Preconditions.checkArgument(value > 0, "Age value (%s) must be greater than 0", value);
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set ticks lived off region"); // Canvas - aggressive thread checks
         this.getHandle().tickCount = value;
         this.getHandle().totalEntityAge = value;
     }
@@ -605,52 +646,62 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
         Preconditions.checkArgument(effect != null, "Entity effect cannot be null");
         Preconditions.checkState(!this.entity.generation, "Cannot play effect during world generation");
         Preconditions.checkArgument(effect.isApplicableTo(this), "Entity effect cannot apply to this entity");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot play entity effect off region"); // Canvas - aggressive thread checks
 
         this.getHandle().level().broadcastEntityEvent(this.getHandle(), effect.getData());
     }
 
     @Override
     public Sound getSwimSound() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get swim sound off region"); // Canvas - aggressive thread checks
         return CraftSound.minecraftToBukkit(this.getHandle().getSwimSound());
     }
 
     @Override
     public Sound getSwimSplashSound() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get swim splash sound off region"); // Canvas - aggressive thread checks
         return CraftSound.minecraftToBukkit(this.getHandle().getSwimSplashSound());
     }
 
     @Override
     public Sound getSwimHighSpeedSplashSound() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get high speed splash sound off region"); // Canvas - aggressive thread checks
         return CraftSound.minecraftToBukkit(this.getHandle().getSwimHighSpeedSplashSound());
     }
 
     @Override
     public void setMetadata(String metadataKey, MetadataValue newMetadataValue) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set metadata off region"); // Canvas - aggressive thread checks
         this.server.getEntityMetadata().setMetadata(this, metadataKey, newMetadataValue);
     }
 
     @Override
     public List<MetadataValue> getMetadata(String metadataKey) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get metadata off region"); // Canvas - aggressive thread checks
         return this.server.getEntityMetadata().getMetadata(this, metadataKey);
     }
 
     @Override
     public boolean hasMetadata(String metadataKey) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if entity has metadata key off region"); // Canvas - aggressive thread checks
         return this.server.getEntityMetadata().hasMetadata(this, metadataKey);
     }
 
     @Override
     public void removeMetadata(String metadataKey, Plugin owningPlugin) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot remove metadata off region"); // Canvas - aggressive thread checks
         this.server.getEntityMetadata().removeMetadata(this, metadataKey, owningPlugin);
     }
 
     @Override
     public boolean isInsideVehicle() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is a passenger off region"); // Canvas - aggressive thread checks
         return this.getHandle().isPassenger();
     }
 
     @Override
     public boolean leaveVehicle() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot force entity to leave vehicle off region"); // Canvas - aggressive thread checks
         if (!this.isInsideVehicle()) {
             return false;
         }
@@ -661,6 +712,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public org.bukkit.entity.Entity getVehicle() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity vehicle off region"); // Canvas - aggressive thread checks
         if (!this.isInsideVehicle()) {
             return null;
         }
@@ -670,12 +722,14 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public net.kyori.adventure.text.Component customName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get custom name off region"); // Canvas - aggressive thread checks
         final Component name = this.getHandle().getCustomName();
         return name != null ? io.papermc.paper.adventure.PaperAdventure.asAdventure(name) : null;
     }
 
     @Override
     public void customName(final net.kyori.adventure.text.Component customName) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set custom name off region"); // Canvas - aggressive thread checks
         this.getHandle().setCustomName(customName != null ? io.papermc.paper.adventure.PaperAdventure.asVanilla(customName) : null);
     }
 
@@ -686,6 +740,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public void setCustomName(String name) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set custom name off region"); // Canvas - aggressive thread checks
         // sane limit for name length
         if (name != null && name.length() > 256) {
             name = name.substring(0, 256);
@@ -696,6 +751,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public String getCustomName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get custom name off region"); // Canvas - aggressive thread checks
         Component name = this.getHandle().getCustomName();
 
         if (name == null) {
@@ -707,16 +763,19 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public void setCustomNameVisible(boolean flag) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set if custom name should be visible for entity off region"); // Canvas - aggressive thread checks
         this.getHandle().setCustomNameVisible(flag);
     }
 
     @Override
     public boolean isCustomNameVisible() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if custom name should be visible for entity off region"); // Canvas - aggressive thread checks
         return this.getHandle().isCustomNameVisible();
     }
 
     @Override
     public void setVisibleByDefault(boolean visible) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set entity visible by default off region"); // Canvas - aggressive thread checks
         if (this.getHandle().visibleByDefault != visible) {
             if (visible) {
                 // Making visible by default, reset and show to all players
@@ -736,12 +795,14 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean isVisibleByDefault() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity visible by default off region"); // Canvas - aggressive thread checks
         return this.getHandle().visibleByDefault;
     }
 
     @Override
     public Set<Player> getTrackedBy() {
         Preconditions.checkState(!this.entity.generation, "Cannot get tracking players during world generation");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get players tracking entity off region"); // Canvas - aggressive thread checks
         ImmutableSet.Builder<Player> players = ImmutableSet.builder();
 
         ServerLevel world = ((CraftWorld) this.getWorld()).getHandle();
@@ -760,6 +821,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
     public boolean isTrackedBy(final Player player) {
         Preconditions.checkState(!this.entity.generation, "Cannot check tracking players during world generation");
         Preconditions.checkArgument(player != null, "Player cannot be null");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if entity is tracked by player off region"); // Canvas - aggressive thread checks
 
         ServerLevel world = ((CraftWorld) this.getWorld()).getHandle();
         ChunkMap.TrackedEntity entityTracker = this.getHandle().moonrise$getTrackedEntity(); // Folia - region threading
@@ -790,16 +852,19 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public String getName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity name off region"); // Canvas - aggressive thread checks
         return CraftChatMessage.fromComponent(this.getHandle().getName());
     }
 
     @Override
     public net.kyori.adventure.text.@org.jetbrains.annotations.NotNull Component name() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity name off region"); // Canvas - aggressive thread checks
         return io.papermc.paper.adventure.PaperAdventure.asAdventure(this.getHandle().getName());
     }
 
     @Override
     public net.kyori.adventure.text.@org.jetbrains.annotations.NotNull Component teamDisplayName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get team display name name off region"); // Canvas - aggressive thread checks
         return io.papermc.paper.adventure.PaperAdventure.asAdventure(this.getHandle().getDisplayName());
     }
 
@@ -870,97 +935,116 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public void setGlowing(boolean flag) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set glowing flag off region"); // Canvas - aggressive thread checks
         this.getHandle().setGlowingTag(flag);
     }
 
     @Override
     public boolean isGlowing() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get glowing flag off region"); // Canvas - aggressive thread checks
         return this.getHandle().isCurrentlyGlowing();
     }
 
     @Override
     public void setInvulnerable(boolean flag) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set invulnerable flag off region"); // Canvas - aggressive thread checks
         this.getHandle().setInvulnerable(flag);
     }
 
     @Override
     public boolean isInvulnerable() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get invulnerable flag off region"); // Canvas - aggressive thread checks
         return this.getHandle().isInvulnerableToBase(this.getHandle().damageSources().generic());
     }
 
     @Override
     public boolean isSilent() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is silent off region"); // Canvas - aggressive thread checks
         return this.getHandle().isSilent();
     }
 
     @Override
     public void setSilent(boolean flag) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set if entity is silent off region"); // Canvas - aggressive thread checks
         this.getHandle().setSilent(flag);
     }
 
     @Override
     public boolean hasGravity() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if entity has gravity off region"); // Canvas - aggressive thread checks
         return !this.getHandle().isNoGravity();
     }
 
     @Override
     public void setGravity(boolean gravity) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set if entity has gravity off region"); // Canvas - aggressive thread checks
         this.getHandle().setNoGravity(!gravity);
     }
 
     @Override
     public int getPortalCooldown() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get portal cooldown off region"); // Canvas - aggressive thread checks
         return this.getHandle().getPortalCooldown();
     }
 
     @Override
     public void setPortalCooldown(int cooldown) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set portal cooldown off region"); // Canvas - aggressive thread checks
         this.getHandle().setPortalCooldown(cooldown);
     }
 
     @Override
     public Set<String> getScoreboardTags() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get scoreboard tags off region"); // Canvas - aggressive thread checks
         return this.getHandle().entityTags();
     }
 
     @Override
     public boolean addScoreboardTag(String tag) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot add scoreboard tags off region"); // Canvas - aggressive thread checks
         return this.getHandle().addTag(tag);
     }
 
     @Override
     public boolean removeScoreboardTag(String tag) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot remove scoreboard tags off region"); // Canvas - aggressive thread checks
         return this.getHandle().removeTag(tag);
     }
 
     @Override
     public PistonMoveReaction getPistonMoveReaction() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get the entity piston move reaction off region"); // Canvas - aggressive thread checks
         return PistonMoveReaction.getById(this.getHandle().getPistonPushReaction().ordinal());
     }
 
     @Override
     public BlockFace getFacing() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get facing blockface off region"); // Canvas - aggressive thread checks
         // Use this method over getDirection because it handles boats and minecarts.
         return CraftBlock.notchToBlockFace(this.getHandle().getMotionDirection());
     }
 
     @Override
     public CraftPersistentDataContainer getPersistentDataContainer() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get PDC off region"); // Canvas - aggressive thread checks
         return this.persistentDataContainer;
     }
 
     @Override
     public Pose getPose() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity pose off region"); // Canvas - aggressive thread checks
         return Pose.values()[this.getHandle().getPose().ordinal()];
     }
 
     @Override
     public void setSneaking(boolean sneak) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set sneaking flag off region"); // Canvas - aggressive thread checks
         this.getHandle().setShiftKeyDown(sneak);
     }
 
     @Override
     public boolean isSneaking() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get sneaking flag off region"); // Canvas - aggressive thread checks
         return this.getHandle().isShiftKeyDown();
     }
 
@@ -971,6 +1055,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
     }
 
     public void setPose0(net.minecraft.world.entity.Pose pose, boolean fixed) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set entity pose off region"); // Canvas - aggressive thread checks
         final Entity handle = this.getHandle();
         handle.fixedPose = false;
         handle.setPose(pose);
@@ -979,6 +1064,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean hasFixedPose() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity has a fixed pose off region"); // Canvas - aggressive thread checks
         return this.getHandle().fixedPose;
     }
 
@@ -994,6 +1080,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public String getAsString() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot serialize entity to string off region"); // Canvas - aggressive thread checks
         try (final ProblemReporter.ScopedCollector problemReporter = new ProblemReporter.ScopedCollector(
             () -> "Entity#toString", LOGGER
         )) {
@@ -1011,11 +1098,13 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public EntitySnapshot createSnapshot() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot create entity snapshot off region"); // Canvas - aggressive thread checks
         return CraftEntitySnapshot.create(this);
     }
 
     @Override
     public org.bukkit.entity.Entity copy() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot copy entity off region"); // Canvas - aggressive thread checks
         Entity copy = this.copy(this.getHandle().level());
         Preconditions.checkArgument(copy != null, "Error creating new entity.");
 
@@ -1024,6 +1113,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public org.bukkit.entity.Entity copy(Location location) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot copy entity off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(location.getWorld() != null, "Location has no world");
 
         Entity copy = this.copy(((CraftWorld) location.getWorld()).getHandle());
@@ -1195,6 +1285,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public Location getOrigin() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity origin off region"); // Canvas - aggressive thread checks
         if (this.getHandle().origin == null) {
             return null;
         }
@@ -1209,36 +1300,43 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean fromMobSpawner() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is from spawner off region"); // Canvas - aggressive thread checks
         return this.getHandle().spawnedViaMobSpawner;
     }
 
     @Override
     public org.bukkit.event.entity.CreatureSpawnEvent.SpawnReason getEntitySpawnReason() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity spawn reason off region"); // Canvas - aggressive thread checks
         return this.getHandle().spawnReason;
     }
 
     @Override
     public boolean isUnderWater() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is underwater off region"); // Canvas - aggressive thread checks
         return this.getHandle().isUnderWater();
     }
 
     @Override
     public boolean isInRain() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is in rain off region"); // Canvas - aggressive thread checks
         return this.getHandle().isInRain();
     }
 
     @Override
     public boolean isInLava() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is in lava off region"); // Canvas - aggressive thread checks
         return this.getHandle().isInLava();
     }
 
     @Override
     public boolean isTicking() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is ticking off region"); // Canvas - aggressive thread checks
         return this.getHandle().isTicking();
     }
 
     @Override
     public Set<org.bukkit.entity.Player> getTrackedPlayers() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get tracked players off region"); // Canvas - aggressive thread checks
         ChunkMap.TrackedEntity tracker = this.entity.moonrise$getTrackedEntity(); // Folia - region threading
         if (tracker == null) {
             return java.util.Collections.emptySet();
@@ -1255,6 +1353,8 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
     public boolean spawnAt(Location location, CreatureSpawnEvent.SpawnReason reason) {
         Preconditions.checkNotNull(location, "location cannot be null");
         Preconditions.checkNotNull(reason, "reason cannot be null");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot try spawn entity off region"); // Canvas - aggressive thread checks
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(((CraftWorld) location.getWorld()).getHandle(), location.getX(), location.getZ(), "Cannot spawn entity at position not owned by the current region"); // Canvas - aggressive thread checks
         this.entity.setLevel(((CraftWorld) location.getWorld()).getHandle());
         this.entity.setPos(location.getX(), location.getY(), location.getZ());
         this.entity.setRot(location.getYaw(), location.getPitch());
@@ -1267,6 +1367,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean isInPowderedSnow() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is in powdered snow off region"); // Canvas - aggressive thread checks
         return this.getHandle().isInPowderSnow || this.getHandle().wasInPowderSnow; // depending on the location in the entity "tick" either could be needed.
     }
 
@@ -1297,27 +1398,32 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean isInvisible() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity is invisible off region"); // Canvas - aggressive thread checks
         return this.getHandle().isInvisible();
     }
 
     @Override
     public void setInvisible(boolean invisible) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set invisible flag off region"); // Canvas - aggressive thread checks
         this.getHandle().persistentInvisibility = invisible;
         this.getHandle().setSharedFlag(Entity.FLAG_INVISIBLE, invisible);
     }
 
     @Override
     public void setNoPhysics(boolean noPhysics) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set no physics flag off region"); // Canvas - aggressive thread checks
         this.getHandle().noPhysics = noPhysics;
     }
 
     @Override
     public boolean hasNoPhysics() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get no physics flag off region"); // Canvas - aggressive thread checks
         return this.getHandle().noPhysics;
     }
 
     @Override
     public boolean collidesAt(@org.jetbrains.annotations.NotNull Location location) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if entity collides at location off region"); // Canvas - aggressive thread checks
         net.minecraft.world.phys.AABB box = this.getHandle().getBoundingBoxAt(location.getX(), location.getY(), location.getZ());
 
         return !this.getHandle().level().noCollision(this.getHandle(), box);
@@ -1325,6 +1431,7 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public boolean wouldCollideUsing(@org.jetbrains.annotations.NotNull BoundingBox boundingBox) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if entity would collide with a bounding box off the owning entity context"); // Canvas - aggressive thread checks
         net.minecraft.world.phys.AABB box = new AABB(boundingBox.getMinX(), boundingBox.getMinY(), boundingBox.getMinZ(), boundingBox.getMaxX(), boundingBox.getMaxY(), boundingBox.getMaxZ());
 
         return !this.getHandle().level().noCollision(this.getHandle(), box);
@@ -1332,12 +1439,14 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
 
     @Override
     public String getScoreboardEntryName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity scoreboard entry name off region"); // Canvas - aggressive thread checks
         return this.getHandle().getScoreboardName();
     }
 
     @Override
     public void broadcastHurtAnimation(java.util.Collection<Player> players) {
         //noinspection SuspiciousMethodCalls
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot broadcast hurt animation off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(!players.contains(this), "Cannot broadcast hurt animation to self without a yaw");
         for (final org.bukkit.entity.Player player : players) {
             ((CraftPlayer) player).sendHurtAnimation(0, this);
@@ -1345,16 +1454,19 @@ public abstract class CraftEntity implements org.bukkit.entity.Entity {
     }
     @Override
     public <T> @Nullable T getData(@NotNull final DataComponentType.Valued<T> type) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get data component value off region"); // Canvas - aggressive thread checks
         return this.entity.get(io.papermc.paper.datacomponent.PaperDataComponentType.bukkitToMinecraft(type));
     }
 
     @Override
     public <T> @Nullable T getDataOrDefault(@NotNull final DataComponentType.Valued<? extends T> type, @Nullable final T fallback) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get data component value off region"); // Canvas - aggressive thread checks
         return this.entity.getOrDefault(io.papermc.paper.datacomponent.PaperDataComponentType.bukkitToMinecraft(type), fallback);
     }
 
     @Override
     public boolean hasData(final @NotNull DataComponentType type) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if a data component type is present on entity off region"); // Canvas - aggressive thread checks
         return this.entity.get(io.papermc.paper.datacomponent.PaperDataComponentType.bukkitToMinecraft(type)) != null;
     }
 
diff --git a/src/main/java/org/bukkit/craftbukkit/entity/CraftHumanEntity.java b/src/main/java/org/bukkit/craftbukkit/entity/CraftHumanEntity.java
index 0299c282682f623eab358d97604c6b62464a7bac..255504e2c2108fcf05df200b1934e8b67c883944 100644
--- a/src/main/java/org/bukkit/craftbukkit/entity/CraftHumanEntity.java
+++ b/src/main/java/org/bukkit/craftbukkit/entity/CraftHumanEntity.java
@@ -110,41 +110,49 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public PlayerInventory getInventory() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player inventory off region"); // Canvas - aggressive thread checks
         return this.inventory;
     }
 
     @Override
     public EntityEquipment getEquipment() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player equipment off region"); // Canvas - aggressive thread checks
         return this.inventory;
     }
 
     @Override
     public Inventory getEnderChest() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player ender chest off region"); // Canvas - aggressive thread checks
         return this.enderChest;
     }
 
     @Override
     public MainHand getMainHand() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player main hand off region"); // Canvas - aggressive thread checks
         return this.getHandle().getMainArm() == HumanoidArm.LEFT ? MainHand.LEFT : MainHand.RIGHT;
     }
 
     @Override
     public ItemStack getItemInHand() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player hand off region"); // Canvas - aggressive thread checks
         return this.getInventory().getItemInMainHand();
     }
 
     @Override
     public void setItemInHand(ItemStack item) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player hand off region"); // Canvas - aggressive thread checks
         this.getInventory().setItemInMainHand(item);
     }
 
     @Override
     public ItemStack getItemOnCursor() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get item on cursor off region"); // Canvas - aggressive thread checks
         return CraftItemStack.asCraftMirror(this.getHandle().containerMenu.getCarried());
     }
 
     @Override
     public void setItemOnCursor(ItemStack item) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set item on cursor off region"); // Canvas - aggressive thread checks
         net.minecraft.world.item.ItemStack stack = CraftItemStack.asNMSCopy(item);
         this.getHandle().containerMenu.setCarried(stack);
         if (this instanceof CraftPlayer) {
@@ -154,21 +162,25 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public void setHurtDirection(float hurtDirection) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set hurt direction off region"); // Canvas - aggressive thread checks
         this.getHandle().hurtDir = hurtDirection;
     }
 
     @Override
     public boolean isDeeplySleeping() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get is deeply sleeping off region"); // Canvas - aggressive thread checks
         return getHandle().isSleepingLongEnough();
     }
 
     @Override
     public int getSleepTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get sleep ticks off region"); // Canvas - aggressive thread checks
         return this.getHandle().sleepCounter;
     }
 
     @Override
     public Location getPotentialRespawnLocation() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get potental respawn location off region"); // Canvas - aggressive thread checks
         ServerPlayer.RespawnConfig respawnConfig = ((ServerPlayer) this.getHandle()).getRespawnConfig();
         if (respawnConfig == null) {
             return null;
@@ -184,6 +196,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public org.bukkit.entity.FishHook getFishHook() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get fish hook off region"); // Canvas - aggressive thread checks
         if (getHandle().fishing == null) {
             return null;
         }
@@ -195,6 +208,8 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
         Preconditions.checkArgument(location != null, "Location cannot be null");
         Preconditions.checkArgument(location.getWorld() != null, "Location needs to be in a world");
         Preconditions.checkArgument(location.getWorld().equals(this.getWorld()), "Cannot sleep across worlds");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot try sleep off region"); // Canvas - aggressive thread checks
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(((org.bukkit.craftbukkit.CraftWorld) location.getWorld()).getHandle(), location.getX(), location.getZ(), "Cannot start sleeping in a bed location off region"); // Canvas - aggressive thread checks
 
         BlockPos pos = CraftLocation.toBlockPos(location);
         BlockState state = this.getHandle().level().getBlockState(pos);
@@ -215,6 +230,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public void wakeup(boolean setSpawnLocation) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot wake up off region"); // Canvas - aggressive thread checks
         Preconditions.checkState(this.isSleeping(), "Cannot wakeup if not sleeping");
 
         this.getHandle().stopSleepInBed(true, setSpawnLocation);
@@ -222,6 +238,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public void startRiptideAttack(int duration, float damage, ItemStack attackItem) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot start riptide attack off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(duration > 0, "Duration must be greater than 0");
         Preconditions.checkArgument(damage >= 0, "Damage must not be negative");
 
@@ -230,6 +247,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public Location getBedLocation() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get bed location off region"); // Canvas - aggressive thread checks
         Preconditions.checkState(this.isSleeping(), "Not sleeping");
 
         BlockPos bed = this.getHandle().getSleepingPos().get();
@@ -309,11 +327,13 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public GameMode getGameMode() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get gamemode off region"); // Canvas - aggressive thread checks
         return this.mode;
     }
 
     @Override
     public void setGameMode(GameMode mode) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set gamemode off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(mode != null, "GameMode cannot be null");
 
         this.mode = mode;
@@ -321,12 +341,14 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public InventoryView getOpenInventory() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get open inventory off region"); // Canvas - aggressive thread checks
         return this.getHandle().containerMenu.getBukkitView();
     }
 
     @Override
     public InventoryView openInventory(Inventory inventory) {
         if (!(this.getHandle() instanceof ServerPlayer)) return null;
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open inventory off region"); // Canvas - aggressive thread checks
         ServerPlayer player = (ServerPlayer) this.getHandle();
         AbstractContainerMenu formerContainer = this.getHandle().containerMenu;
 
@@ -401,6 +423,8 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
                 return null;
             }
         }
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open inventory off region"); // Canvas - aggressive thread checks
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(((org.bukkit.craftbukkit.CraftWorld) location.getWorld()).getHandle(), location.getX(), location.getZ(), "Cannot open off region workbench"); // Canvas - aggressive thread checks
         this.getHandle().openMenu(Blocks.CRAFTING_TABLE.defaultBlockState().getMenuProvider(this.getHandle().level(), CraftLocation.toBlockPos(location)));
         if (force) {
             this.getHandle().containerMenu.checkReachable = false;
@@ -420,6 +444,8 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
             }
         }
 
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open inventory off region"); // Canvas - aggressive thread checks
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(((org.bukkit.craftbukkit.CraftWorld) location.getWorld()).getHandle(), location.getX(), location.getZ(), "Cannot open off region enchant table"); // Canvas - aggressive thread checks
         // If there isn't an enchant table we can force create one, won't be very useful though.
         BlockPos pos = CraftLocation.toBlockPos(location);
         // Paper start
@@ -446,6 +472,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
         Preconditions.checkArgument(this.equals(inventory.getPlayer()), "InventoryView must belong to the opening player");
         if (!(this.getHandle() instanceof ServerPlayer)) return; // TODO: NPC support?
         if (((ServerPlayer) this.getHandle()).connection == null) return;
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open inventory off region"); // Canvas - aggressive thread checks
         if (this.getHandle().containerMenu != this.getHandle().inventoryMenu) {
             // fire INVENTORY_CLOSE if one already open
             ((ServerPlayer) this.getHandle()).connection.handleContainerClose(new ServerboundContainerClosePacket(this.getHandle().containerMenu.containerId), org.bukkit.event.inventory.InventoryCloseEvent.Reason.OPEN_NEW); // Paper - Inventory close reason
@@ -509,6 +536,8 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
             // we're not supposed to have multiple people using the same merchant, so we have to close it.
             merchant.getTrader().closeInventory();
         }
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open inventory off region"); // Canvas - aggressive thread checks
+        if (merchant instanceof org.bukkit.craftbukkit.entity.CraftAbstractVillager vill) ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(vill.entity, "Cannot open inventory off region"); // Canvas - aggressive thread checks
 
         net.minecraft.world.item.trading.Merchant mcMerchant;
         Component name;
@@ -563,10 +592,11 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     }
 
     private InventoryView openInventory(Location location, boolean force, Material material) {
-        org.spigotmc.AsyncCatcher.catchOp("open" + material);
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open inventory off region"); // Canvas - aggressive thread checks
         if (location == null) {
             location = this.getLocation();
         }
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(((org.bukkit.craftbukkit.CraftWorld) location.getWorld()).getHandle(), location.getX(), location.getZ(), "Cannot open off region inventory view for " + material); // Canvas - aggressive thread checks
         if (!force) {
             Block block = location.getBlock();
             if (block.getType() != material) {
@@ -596,16 +626,19 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public void closeInventory(org.bukkit.event.inventory.InventoryCloseEvent.Reason reason) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot close inventory off region"); // Canvas - aggressive thread checks
         this.getHandle().closeContainer(reason);
     }
 
     @Override
     public boolean isBlocking() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check is blocking off region"); // Canvas - aggressive thread checks
         return this.getHandle().isBlocking();
     }
 
     @Override
     public boolean isHandRaised() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check is hand raised off region"); // Canvas - aggressive thread checks
         return this.getHandle().isUsingItem();
     }
 
@@ -616,21 +649,25 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public int getEnchantmentSeed() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get enchantment seed off region"); // Canvas - aggressive thread checks
         return this.getHandle().enchantmentSeed;
     }
 
     @Override
     public void setEnchantmentSeed(int i) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set enchantment seed off region"); // Canvas - aggressive thread checks
         this.getHandle().enchantmentSeed = i;
     }
 
     @Override
     public int getExpToLevel() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get exp needed for next level off region"); // Canvas - aggressive thread checks
         return this.getHandle().getXpNeededForNextLevel();
     }
 
     @Override
     public float getAttackCooldown() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot attack cooldown off region"); // Canvas - aggressive thread checks
         return this.getHandle().getAttackStrengthScale(0.5f);
     }
 
@@ -653,6 +690,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     @Override
     public boolean hasCooldown(ItemStack item) {
         Preconditions.checkArgument(item != null, "Item cannot be null");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if has cooldown off region"); // Canvas - aggressive thread checks
 
         return this.getHandle().getCooldowns().isOnCooldown(CraftItemStack.asNMSCopy(item));
     }
@@ -660,6 +698,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     @Override
     public int getCooldown(ItemStack item) {
         Preconditions.checkArgument(item != null, "Item cannot be null");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get cooldown for item off region"); // Canvas - aggressive thread checks
 
         Identifier group = this.getHandle().getCooldowns().getCooldownGroup(CraftItemStack.asNMSCopy(item));
         if (group == null) {
@@ -674,6 +713,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     public void setCooldown(ItemStack item, int ticks) {
         Preconditions.checkArgument(item != null, "Item cannot be null");
         Preconditions.checkArgument(ticks >= 0, "Cannot have negative cooldown");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set cooldown for item off region"); // Canvas - aggressive thread checks
 
         this.getHandle().getCooldowns().addCooldown(CraftItemStack.asNMSCopy(item), ticks);
     }
@@ -681,6 +721,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     @Override
     public int getCooldown(Key key) {
         Preconditions.checkArgument(key != null, "Key cannot be null");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get cooldown off region"); // Canvas - aggressive thread checks
 
         ItemCooldowns.CooldownInstance cooldown = this.getHandle().getCooldowns().cooldowns.get(PaperAdventure.asVanilla(key));
         return (cooldown == null) ? 0 : Math.max(0, cooldown.endTime() - this.getHandle().getCooldowns().tickCount);
@@ -690,6 +731,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     public void setCooldown(Key key, int ticks) {
         Preconditions.checkArgument(key != null, "Key cannot be null");
         Preconditions.checkArgument(ticks >= 0, "Cannot have negative cooldown");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set cooldown off region"); // Canvas - aggressive thread checks
 
         this.getHandle().getCooldowns().addCooldown(PaperAdventure.asVanilla(key), ticks);
     }
@@ -706,11 +748,13 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public int discoverRecipes(Collection<NamespacedKey> recipes) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot discover recipes off region"); // Canvas - aggressive thread checks
         return this.getHandle().awardRecipes(this.bukkitKeysToMinecraftRecipes(recipes));
     }
 
     @Override
     public int undiscoverRecipes(Collection<NamespacedKey> recipes) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot undiscover recipes off region"); // Canvas - aggressive thread checks
         return this.getHandle().resetRecipes(this.bukkitKeysToMinecraftRecipes(recipes));
     }
 
@@ -768,12 +812,14 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public void openSign(final org.bukkit.block.Sign sign, final org.bukkit.block.sign.Side side) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open sign off region"); // Canvas - aggressive thread checks
         org.bukkit.craftbukkit.block.CraftSign.openSign(sign, (CraftPlayer) this, side);
     }
 
     @Override
     public boolean dropItem(boolean dropAll) {
         if (!(this.getHandle() instanceof ServerPlayer player)) return false;
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot drop item(s) off region"); // Canvas - aggressive thread checks
         boolean success = player.drop(dropAll);
         if (!success) {
             return false;
@@ -789,6 +835,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     @Nullable
     public Item dropItem(final int slot, final int amount, final boolean throwRandomly, final @Nullable Consumer<Item> entityOperation) {
         Preconditions.checkArgument(slot >= 0 && slot < this.inventory.getSize(), "Slot %s is not a valid inventory slot.", slot);
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot drop item(s) off region"); // Canvas - aggressive thread checks
 
         return internalDropItemFromInventory(this.inventory.getItem(slot), amount, throwRandomly, entityOperation);
     }
@@ -796,6 +843,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     @Override
     @Nullable
     public Item dropItem(final @NotNull EquipmentSlot slot, final int amount, final boolean throwRandomly, final @Nullable Consumer<Item> entityOperation) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot drop item(s) off region"); // Canvas - aggressive thread checks
         return internalDropItemFromInventory(this.inventory.getItem(slot), amount, throwRandomly, entityOperation);
     }
 
@@ -818,6 +866,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
         // its itemstack from the players inventory. As such, we cannot reuse #internalDropItemFromInventory.
         Preconditions.checkArgument(itemStack != null, "Cannot drop a null itemstack");
         if (itemStack.isEmpty()) return null;
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot drop item(s) off region"); // Canvas - aggressive thread checks
 
         final net.minecraft.world.item.ItemStack nmsItemStack = CraftItemStack.asNMSCopy(itemStack);
 
@@ -828,71 +877,85 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public float getExhaustion() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get exhaustion off region"); // Canvas - aggressive thread checks
         return this.getHandle().getFoodData().exhaustionLevel;
     }
 
     @Override
     public void setExhaustion(float value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set exhaustion off region"); // Canvas - aggressive thread checks
         this.getHandle().getFoodData().exhaustionLevel = value;
     }
 
     @Override
     public float getSaturation() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get saturation off region"); // Canvas - aggressive thread checks
         return this.getHandle().getFoodData().saturationLevel;
     }
 
     @Override
     public void setSaturation(float value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set saturation off region"); // Canvas - aggressive thread checks
         this.getHandle().getFoodData().saturationLevel = value;
     }
 
     @Override
     public int getFoodLevel() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get food level off region"); // Canvas - aggressive thread checks
         return this.getHandle().getFoodData().foodLevel;
     }
 
     @Override
     public void setFoodLevel(int value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set food level off region"); // Canvas - aggressive thread checks
         this.getHandle().getFoodData().foodLevel = value;
     }
 
     @Override
     public int getSaturatedRegenRate() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get saturated regen rate off region"); // Canvas - aggressive thread checks
         return this.getHandle().getFoodData().saturatedRegenRate;
     }
 
     @Override
     public void setSaturatedRegenRate(int i) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set saturated regen rate off region"); // Canvas - aggressive thread checks
         this.getHandle().getFoodData().saturatedRegenRate = i;
     }
 
     @Override
     public int getUnsaturatedRegenRate() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get unsaturated regen rate off region"); // Canvas - aggressive thread checks
         return this.getHandle().getFoodData().unsaturatedRegenRate;
     }
 
     @Override
     public void setUnsaturatedRegenRate(int i) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set unsaturated regen rate off region"); // Canvas - aggressive thread checks
         this.getHandle().getFoodData().unsaturatedRegenRate = i;
     }
 
     @Override
     public int getStarvationRate() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get starvation rate off region"); // Canvas - aggressive thread checks
         return this.getHandle().getFoodData().starvationRate;
     }
 
     @Override
     public void setStarvationRate(int i) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set starvation rate off region"); // Canvas - aggressive thread checks
         this.getHandle().getFoodData().starvationRate = i;
     }
 
     @Override
     public Location getLastDeathLocation() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get last death location off region"); // Canvas - aggressive thread checks
         return this.getHandle().getLastDeathLocation().map(CraftLocation::fromGlobalPos).orElse(null);
     }
 
     @Override
     public void setLastDeathLocation(Location location) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set last death location off region"); // Canvas - aggressive thread checks
         this.getHandle().setLastDeathLocation(Optional.ofNullable(location).map(CraftLocation::toGlobalPos));
     }
 
@@ -900,6 +963,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
     public Firework fireworkBoost(ItemStack fireworkItemStack) {
         Preconditions.checkArgument(fireworkItemStack != null, "fireworkItemStack must not be null");
         Preconditions.checkArgument(fireworkItemStack.getType() == Material.FIREWORK_ROCKET, "fireworkItemStack must be of type %s", Material.FIREWORK_ROCKET);
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot firework boost off region"); // Canvas - aggressive thread checks
 
         FireworkRocketEntity fireworks = new FireworkRocketEntity(this.getHandle().level(), CraftItemStack.asNMSCopy(fireworkItemStack), this.getHandle());
         boolean success = this.getHandle().level().addFreshEntity(fireworks, SpawnReason.CUSTOM);
@@ -908,6 +972,7 @@ public class CraftHumanEntity extends CraftLivingEntity implements HumanEntity {
 
     @Override
     public boolean canUseEquipmentSlot(org.bukkit.inventory.EquipmentSlot slot) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot trigger equipment slot use off region"); // Canvas - aggressive thread checks
         net.minecraft.world.entity.EquipmentSlot equipmentSlot = CraftEquipmentSlot.getNMS(slot);
         return (equipmentSlot.getType() == net.minecraft.world.entity.EquipmentSlot.Type.HUMANOID_ARMOR || equipmentSlot.getType() == net.minecraft.world.entity.EquipmentSlot.Type.HAND) && super.canUseEquipmentSlot(slot);
     }
diff --git a/src/main/java/org/bukkit/craftbukkit/entity/CraftLivingEntity.java b/src/main/java/org/bukkit/craftbukkit/entity/CraftLivingEntity.java
index db26642e0da92d81a9ae6bc5d6ea4febadab7080..3fef711809d4823bb4de1ed95f4ab48f9ed9ae92 100644
--- a/src/main/java/org/bukkit/craftbukkit/entity/CraftLivingEntity.java
+++ b/src/main/java/org/bukkit/craftbukkit/entity/CraftLivingEntity.java
@@ -133,6 +133,12 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
             "Health value (%s) must be between 0 and %s. (attribute base value: %s%s)",
             health, this.getMaxHealth(), this.getHandle().getAttribute(Attributes.MAX_HEALTH).getBaseValue(), this instanceof CraftPlayer ? ", player: " + this.getName() : ""
         );
+        // for GETTING health, for a player this is fine since it runs through the CraftPlayer health field,
+        // while other entities use the synched entity data. this is fine because entities are tick-guarded
+        // for that, while players are not and should not. *setting* health technically is also safe, except
+        // for when we are setting it to 0, because then the player is told to die, which needs to be on the
+        // correct thread context, so it's safer to try and keep this guarded
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set health async"); // Canvas - aggressive thread checks
         // Paper end
 
         // during world generation, we don't want to run logic for dropping items and xp
@@ -150,29 +156,34 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public void heal(final double amount, final EntityRegainHealthEvent.RegainReason reason) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot heal async"); // Canvas - aggressive thread checks
         this.getHandle().heal((float) amount, reason);
     }
 
     @Override
     public double getAbsorptionAmount() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get absorption amount async"); // Canvas - aggressive thread checks - uses synched entity data for players
         return this.getHandle().getAbsorptionAmount();
     }
 
     @Override
     public void setAbsorptionAmount(double amount) {
         Preconditions.checkArgument(amount >= 0 && Double.isFinite(amount), "amount < 0 or non-finite");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set absorption amount async"); // Canvas - aggressive thread checks - uses synched entity data for players
 
         this.getHandle().setAbsorptionAmount((float) amount);
     }
 
     @Override
     public double getMaxHealth() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get max health async"); // Canvas - aggressive thread checks
         return this.getHandle().getMaxHealth();
     }
 
     @Override
     public void setMaxHealth(double amount) {
         Preconditions.checkArgument(amount > 0, "Max health amount (%s) must be greater than 0", amount);
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set max health async"); // Canvas - aggressive thread checks
 
         this.getHandle().getAttribute(Attributes.MAX_HEALTH).setBaseValue(amount);
 
@@ -183,11 +194,13 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public void resetMaxHealth() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot reset max health async"); // Canvas - aggressive thread checks - guard here too bc we access attributes
         this.setMaxHealth(this.getHandle().getAttribute(Attributes.MAX_HEALTH).getAttribute().value().getDefaultValue());
     }
 
     @Override
     public double getEyeHeight() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get eye height async"); // Canvas - aggressive thread checks
         return this.getHandle().getEyeHeight();
     }
 
@@ -198,6 +211,7 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     private List<Block> getLineOfSight(Set<Material> transparent, int maxDistance, int maxLength) {
         Preconditions.checkState(!this.getHandle().generation, "Cannot get line of sight during world generation");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get line of sight async"); // Canvas - aggressive thread checks
 
         if (transparent == null) {
             transparent = Sets.newHashSet(Material.AIR, Material.CAVE_AIR, Material.VOID_AIR);
@@ -275,6 +289,7 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
     }
 
     public net.minecraft.world.phys.EntityHitResult rayTraceEntity(int maxDistance, boolean ignoreBlocks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot ray trace entity async"); // Canvas - aggressive thread checks
         net.minecraft.world.phys.EntityHitResult rayTrace = getHandle().getTargetEntity(maxDistance);
         if (rayTrace == null) {
             return null;
@@ -306,6 +321,7 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
     @Override
     public RayTraceResult rayTraceBlocks(double maxDistance, FluidCollisionMode fluidCollisionMode) {
         Preconditions.checkState(!this.getHandle().generation, "Cannot ray tray blocks during world generation");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot ray trace blocks async"); // Canvas - aggressive thread checks
 
         Location eyeLocation = this.getEyeLocation();
         Vector direction = eyeLocation.getDirection();
@@ -314,59 +330,70 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public int getRemainingAir() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get remaining air async"); // Canvas - aggressive thread checks
         return this.getHandle().getAirSupply();
     }
 
     @Override
     public void setRemainingAir(int ticks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set remaining air async"); // Canvas - aggressive thread checks
         this.getHandle().setAirSupply(ticks);
     }
 
     @Override
     public int getMaximumAir() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get max air async"); // Canvas - aggressive thread checks
         return this.getHandle().maxAirTicks;
     }
 
     @Override
     public void setMaximumAir(int ticks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set max air async"); // Canvas - aggressive thread checks
         this.getHandle().maxAirTicks = ticks;
     }
 
     @Override
     public ItemStack getItemInUse() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get item in use async"); // Canvas - aggressive thread checks
         net.minecraft.world.item.ItemStack item = this.getHandle().getUseItem();
         return item.isEmpty() ? null : CraftItemStack.asCraftMirror(item);
     }
 
     @Override
     public int getItemInUseTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get item in use ticks async"); // Canvas - aggressive thread checks
         return this.getHandle().getUseItemRemainingTicks();
     }
 
     @Override
     public void setItemInUseTicks(int ticks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set item in use ticks async"); // Canvas - aggressive thread checks
         this.getHandle().useItemRemaining = ticks;
     }
 
     @Override
     public int getArrowCooldown() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get arrow cooldown async"); // Canvas - aggressive thread checks
         return this.getHandle().removeArrowTime;
     }
 
     @Override
     public void setArrowCooldown(int ticks) {
         Preconditions.checkArgument(ticks >= 0, "Amount of ticks before next arrow removal must be non-negative");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set arrow cooldown async"); // Canvas - aggressive thread checks
         this.getHandle().removeArrowTime = ticks;
     }
 
     @Override
     public int getArrowsInBody() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get arrows in body async"); // Canvas - aggressive thread checks
         return this.getHandle().getArrowCount();
     }
 
     @Override
     public void setArrowsInBody(final int count, final boolean fireEvent) { // Paper
         Preconditions.checkArgument(count >= 0, "New arrow amount must be non-negative");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set arrows in body async"); // Canvas - aggressive thread checks
         if (!fireEvent) {
             this.getHandle().getEntityData().set(net.minecraft.world.entity.LivingEntity.DATA_ARROW_COUNT_ID, count);
         } else {
@@ -376,28 +403,33 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public boolean isInvulnerable() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if invulnerable async"); // Canvas - aggressive thread checks
         return this.getHandle().isInvulnerableTo((ServerLevel) this.getHandle().level(), this.getHandle().damageSources().generic());
     }
 
     @Override
     public int getBeeStingerCooldown() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get bee stinger cooldown async"); // Canvas - aggressive thread checks
         return this.getHandle().removeStingerTime;
     }
 
     @Override
     public void setBeeStingerCooldown(int ticks) {
         Preconditions.checkArgument(ticks >= 0, "New amount of ticks before next bee stinger removal must be non-negative");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set bee stinger cooldown async"); // Canvas - aggressive thread checks
         this.getHandle().removeStingerTime = ticks;
     }
 
     @Override
     public int getBeeStingersInBody() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get bee stingers in body async"); // Canvas - aggressive thread checks
         return this.getHandle().getStingerCount();
     }
 
     @Override
     public void setBeeStingersInBody(int count) {
         Preconditions.checkArgument(count >= 0, "New bee stinger amount must be >= 0");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set bee stingers in body async"); // Canvas - aggressive thread checks
         this.getHandle().setStingerCount(count);
     }
 
@@ -412,11 +444,13 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public void damage(double amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot damage entity async"); // Canvas - aggressive thread checks
         this.damage(amount, this.getHandle().damageSources().generic());
     }
 
     @Override
     public void damage(double amount, org.bukkit.entity.Entity source) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot damage entity async"); // Canvas - aggressive thread checks
         DamageSource reason = this.getHandle().damageSources().generic();
 
         if (source instanceof HumanEntity) {
@@ -431,6 +465,7 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
     @Override
     public void damage(double amount, org.bukkit.damage.DamageSource damageSource) {
         Preconditions.checkArgument(damageSource != null, "damageSource cannot be null");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot damage entity async"); // Canvas - aggressive thread checks
 
         this.damage(amount, ((CraftDamageSource) damageSource).getHandle());
     }
@@ -451,52 +486,62 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public int getMaximumNoDamageTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get max no damage ticks async"); // Canvas - aggressive thread checks
         return this.getHandle().invulnerableDuration;
     }
 
     @Override
     public void setMaximumNoDamageTicks(int ticks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set max no damage ticks async"); // Canvas - aggressive thread checks
         this.getHandle().invulnerableDuration = ticks;
     }
 
     @Override
     public double getLastDamage() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get last damage async"); // Canvas - aggressive thread checks
         return this.getHandle().lastHurt;
     }
 
     @Override
     public void setLastDamage(double damage) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set last damage async"); // Canvas - aggressive thread checks
         this.getHandle().lastHurt = (float) damage;
     }
 
     @Override
     public int getNoDamageTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get invulnerable time async"); // Canvas - aggressive thread checks
         return this.getHandle().invulnerableTime;
     }
 
     @Override
     public void setNoDamageTicks(int ticks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set invulnerable time async"); // Canvas - aggressive thread checks
         this.getHandle().invulnerableTime = ticks;
     }
 
     @Override
     public int getNoActionTicks() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get no action ticks async"); // Canvas - aggressive thread checks
         return this.getHandle().getNoActionTime();
     }
 
     @Override
     public void setNoActionTicks(int ticks) {
         Preconditions.checkArgument(ticks >= 0, "ticks must be >= 0");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set no action ticks async"); // Canvas - aggressive thread checks
         this.getHandle().setNoActionTime(ticks);
     }
 
     @Override
     public Player getKiller() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get killer async"); // Canvas - aggressive thread checks
         return Optionull.map(this.getHandle().getLastHurtByPlayer(), player -> (Player) player.getBukkitEntity());
     }
 
     @Override
     public void setKiller(Player killer) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set killer async"); // Canvas - aggressive thread checks
         net.minecraft.server.level.ServerPlayer nmsKiller = killer == null ? null : ((CraftPlayer) killer).getHandle();
         this.getHandle().setLastHurtByMob(nmsKiller);
         if (nmsKiller != null) {
@@ -509,7 +554,7 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public boolean addPotionEffect(PotionEffect effect, boolean force) {
-        org.spigotmc.AsyncCatcher.catchOp("effect add"); // Paper
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot add potion effect async"); // Canvas - aggressive thread checks
         this.getHandle().addEffect(org.bukkit.craftbukkit.potion.CraftPotionUtil.fromBukkit(effect), EntityPotionEffectEvent.Cause.PLUGIN); // Paper - Don't ignore icon
         return true;
     }
@@ -525,22 +570,26 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public boolean hasPotionEffect(PotionEffectType type) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check has effect async"); // Canvas - aggressive thread checks
         return this.getHandle().hasEffect(CraftPotionEffectType.bukkitToMinecraftHolder(type));
     }
 
     @Override
     public PotionEffect getPotionEffect(PotionEffectType type) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get potion effect async"); // Canvas - aggressive thread checks
         MobEffectInstance handle = this.getHandle().getEffect(CraftPotionEffectType.bukkitToMinecraftHolder(type));
         return (handle == null) ? null : org.bukkit.craftbukkit.potion.CraftPotionUtil.toBukkit(handle); // Paper
     }
 
     @Override
     public void removePotionEffect(PotionEffectType type) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot remove potion effect async"); // Canvas - aggressive thread checks
         this.getHandle().removeEffect(CraftPotionEffectType.bukkitToMinecraftHolder(type), EntityPotionEffectEvent.Cause.PLUGIN);
     }
 
     @Override
     public Collection<PotionEffect> getActivePotionEffects() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get active potion effects async"); // Canvas - aggressive thread checks
         List<PotionEffect> effects = new ArrayList<>();
         for (MobEffectInstance handle : this.getHandle().activeEffects.values()) {
             effects.add(org.bukkit.craftbukkit.potion.CraftPotionUtil.toBukkit(handle)); // Paper
@@ -550,6 +599,7 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public boolean clearActivePotionEffects() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot clear active potion effects async"); // Canvas - aggressive thread checks
         return this.getHandle().removeAllEffects(EntityPotionEffectEvent.Cause.PLUGIN);
     }
 
@@ -557,6 +607,7 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
     @SuppressWarnings("unchecked")
     public <T extends Projectile> T launchProjectile(Class<? extends T> projectile, Vector velocity, java.util.function.Consumer<? super T> function) {
         Preconditions.checkState(!this.getHandle().generation, "Cannot launch projectile during world generation");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot launch projectile async"); // Canvas - aggressive thread checks
 
         net.minecraft.world.level.Level world = ((CraftWorld) this.getWorld()).getHandle();
         net.minecraft.world.entity.Entity launch = null;
@@ -675,12 +726,14 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
     @Override
     public boolean hasLineOfSight(Entity other) {
         Preconditions.checkState(!this.getHandle().generation, "Cannot check line of sight during world generation");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check has line of sight async"); // Canvas - aggressive thread checks
 
         return this.getHandle().hasLineOfSight(((CraftEntity) other).getHandle());
     }
 
     @Override
     public boolean hasLineOfSight(Location loc) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check has line of sight async"); // Canvas - aggressive thread checks
         if (this.getHandle().level() != ((CraftWorld) loc.getWorld()).getHandle()) {
             return false;
         }
@@ -696,18 +749,21 @@ public class CraftLivingEntity extends CraftEntity implements LivingEntity {
 
     @Override
     public boolean getRemoveWhenFarAway() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get persistence required async"); // Canvas - aggressive thread checks
         return this.getHandle() instanceof Mob mob && !mob.isPersistenceRequired();
     }
 
     @Override
     public void setRemoveWhenFarAway(boolean remove) {
         if (this.getHandle() instanceof Mob mob) {
+            ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set persistence required async"); // Canvas - aggressive thread checks
             mob.persistenceRequired = !remove;
         }
     }
 
     @Override
     public @NonNull EntityEquipment getEquipment() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get entity equipment async"); // Canvas - aggressive thread checks
         return this.equipment;
     }
 
diff --git a/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java b/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
index 12c78e216db4336688c4204a561bb0fa68c9d11e..64d061597ae009e6e24646b216b50c222a7d1320 100644
--- a/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
+++ b/src/main/java/org/bukkit/craftbukkit/entity/CraftPlayer.java
@@ -312,6 +312,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     }
 
     public GameProfile getProfile() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get game profile off region"); // Canvas - aggressive thread checks
         return this.getHandle().getGameProfile();
     }
 
@@ -411,6 +412,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public double getEyeHeight(boolean ignorePose) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get eye height off region"); // Canvas - aggressive thread checks
         if (ignorePose) {
             return 1.62;
         } else {
@@ -487,6 +489,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setPlayerListHeaderFooter(BaseComponent @Nullable [] header, BaseComponent @Nullable [] footer) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set playerlist header/footer off region"); // Canvas - aggressive thread checks
         if (header != null) {
             String headerJson = CraftChatMessage.bungeeToJson(header);
             playerListHeader = net.kyori.adventure.text.serializer.gson.GsonComponentSerializer.gson().deserialize(headerJson);
@@ -506,6 +509,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setPlayerListHeaderFooter(@Nullable BaseComponent header, @Nullable BaseComponent footer) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set playerlist header/footer off region"); // Canvas - aggressive thread checks
         this.setPlayerListHeaderFooter(header == null ? null : new BaseComponent[]{header},
                 footer == null ? null : new BaseComponent[]{footer});
     }
@@ -577,12 +581,14 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public String getDisplayName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get display name off region"); // Canvas - aggressive thread checks
         if (true) return io.papermc.paper.adventure.DisplayNames.getLegacy(this); // Paper
         return this.getHandle().displayName;
     }
 
     @Override
     public void setDisplayName(final String name) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set display name off region"); // Canvas - aggressive thread checks
         this.getHandle().adventure$displayName = name != null ? net.kyori.adventure.text.serializer.legacy.LegacyComponentSerializer.legacySection().deserialize(name) : net.kyori.adventure.text.Component.text(this.getName()); // Paper
         this.getHandle().displayName = name == null ? this.getName() : name;
     }
@@ -590,6 +596,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     // Paper start
     @Override
     public void playerListName(net.kyori.adventure.text.Component name) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set playerlist name off region"); // Canvas - aggressive thread checks
         getHandle().listName = name == null ? null : io.papermc.paper.adventure.PaperAdventure.asVanilla(name);
         if (getHandle().connection == null) return; // Updates are possible before the player has fully joined
         for (ServerPlayer player : server.getHandle().players) {
@@ -600,6 +607,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     }
     @Override
     public net.kyori.adventure.text.Component playerListName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get playerlist name off region"); // Canvas - aggressive thread checks
         return getHandle().listName == null ? net.kyori.adventure.text.Component.text(getName()) : io.papermc.paper.adventure.PaperAdventure.asAdventure(getHandle().listName);
     }
     @Override
@@ -613,11 +621,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     // Paper end
     @Override
     public String getPlayerListName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get playerlist name off region"); // Canvas - aggressive thread checks
         return this.getHandle().listName == null ? this.getName() : CraftChatMessage.fromComponent(this.getHandle().listName);
     }
 
     @Override
     public void setPlayerListName(String name) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set playerlist name off region"); // Canvas - aggressive thread checks
         if (name == null) {
             name = this.getName();
         }
@@ -632,11 +642,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int getPlayerListOrder() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player list order off region"); // Canvas - aggressive thread checks
         return this.getHandle().listOrder;
     }
 
     @Override
     public void setPlayerListOrder(int order) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player list order off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(order >= 0, "order cannot be negative");
 
         this.getHandle().listOrder = order;
@@ -655,28 +667,33 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public String getPlayerListHeader() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player list header off region"); // Canvas - aggressive thread checks
         return (this.playerListHeader == null) ? null : net.kyori.adventure.text.serializer.legacy.LegacyComponentSerializer.legacySection().serialize(this.playerListHeader);
     }
 
     @Override
     public String getPlayerListFooter() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player list footer off region"); // Canvas - aggressive thread checks
         return (this.playerListFooter == null) ? null : net.kyori.adventure.text.serializer.legacy.LegacyComponentSerializer.legacySection().serialize(this.playerListFooter); // Paper - Adventure
     }
 
     @Override
     public void setPlayerListHeader(String header) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player list header off region"); // Canvas - aggressive thread checks
         this.playerListHeader = header == null ? null : net.kyori.adventure.text.serializer.legacy.LegacyComponentSerializer.legacySection().deserialize(header); // Paper - Adventure
         this.updatePlayerListHeaderFooter();
     }
 
     @Override
     public void setPlayerListFooter(String footer) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player list footer off region"); // Canvas - aggressive thread checks
         this.playerListFooter = footer == null ? null : net.kyori.adventure.text.serializer.legacy.LegacyComponentSerializer.legacySection().deserialize(footer); // Paper - Adventure
         this.updatePlayerListHeaderFooter();
     }
 
     @Override
     public void setPlayerListHeaderFooter(String header, String footer) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player list header/footer off region"); // Canvas - aggressive thread checks
         this.playerListHeader = header == null ? null : net.kyori.adventure.text.serializer.legacy.LegacyComponentSerializer.legacySection().deserialize(header); // Paper - Adventure
         this.playerListFooter = footer == null ? null : net.kyori.adventure.text.serializer.legacy.LegacyComponentSerializer.legacySection().deserialize(footer); // Paper - Adventure
         this.updatePlayerListHeaderFooter();
@@ -940,6 +957,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     public boolean breakBlock(Block block) {
         Preconditions.checkArgument(block != null, "Block cannot be null");
         Preconditions.checkArgument(block.getWorld().equals(this.getWorld()), "Cannot break blocks across worlds");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot destroy block off region"); // Canvas - aggressive thread checks
 
         return this.getHandle().gameMode.destroyBlock(new BlockPos(block.getX(), block.getY(), block.getZ()));
     }
@@ -1167,11 +1185,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public WorldBorder getWorldBorder() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access player border off region"); // Canvas - aggressive thread checks
         return this.clientWorldBorder;
     }
 
     @Override
     public void setWorldBorder(WorldBorder border) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player border off region"); // Canvas - aggressive thread checks
         CraftWorldBorder craftBorder = (CraftWorldBorder) border;
 
         if (border != null && !craftBorder.isVirtual() && !craftBorder.getWorld().equals(this.getWorld())) {
@@ -1238,6 +1258,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     }
 
     public boolean hasClientWorldBorder() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if client border exists off region"); // Canvas - aggressive thread checks
         return this.clientWorldBorder != null;
     }
 
@@ -1319,11 +1340,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public boolean hasSeenWinScreen() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if player has seen win screen off region"); // Canvas - aggressive thread checks
         return getHandle().seenCredits;
     }
 
     @Override
     public void setHasSeenWinScreen(boolean hasSeenWinScreen) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set if player has seen win screen off region"); // Canvas - aggressive thread checks
         getHandle().seenCredits = hasSeenWinScreen;
     }
     // Paper end
@@ -1337,6 +1360,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void lookAt(org.bukkit.entity.@NonNull Entity entity, @NonNull LookAnchor playerAnchor, @NonNull LookAnchor entityAnchor) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot make player look at anchor off region"); // Canvas - aggressive thread checks
         this.getHandle().lookAt(toNmsAnchor(playerAnchor), ((CraftEntity) entity).getHandle(), toNmsAnchor(entityAnchor));
     }
 
@@ -1361,26 +1385,31 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setSneaking(boolean sneak) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set sneaking flag off region"); // Canvas - aggressive thread checks
         this.getHandle().setShiftKeyDown(sneak);
     }
 
     @Override
     public boolean isSneaking() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get sneaking flag off region"); // Canvas - aggressive thread checks
         return this.getHandle().isShiftKeyDown();
     }
 
     @Override
     public boolean isSprinting() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get sprinting flag off region"); // Canvas - aggressive thread checks
         return this.getHandle().isSprinting();
     }
 
     @Override
     public void setSprinting(boolean sprinting) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set sprinting flag off region"); // Canvas - aggressive thread checks
         this.getHandle().setSprinting(sprinting);
     }
 
     @Override
     public void loadData() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot load player data off region"); // Canvas - aggressive thread checks
         this.server.getHandle().playerIo.load(this.getHandle().nameAndId())
             .map(tag -> TagValueInput.create(ProblemReporter.DISCARDING, this.server.getServer().registryAccess(), tag))
             .ifPresent(this.getHandle()::load);
@@ -1388,28 +1417,33 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void saveData() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot save player data off region"); // Canvas - aggressive thread checks
         this.server.getHandle().playerIo.save(this.getHandle());
     }
 
     @Deprecated
     @Override
     public void updateInventory() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot send inventory update off region"); // Canvas - aggressive thread checks
         this.getHandle().containerMenu.sendAllDataToRemote();
     }
 
     @Override
     public void setSleepingIgnored(boolean isSleeping) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set sleeping ignored off region"); // Canvas - aggressive thread checks
         this.getHandle().fauxSleeping = isSleeping;
         ((CraftWorld) this.getWorld()).getHandle().updateSleepingPlayerList();
     }
 
     @Override
     public boolean isSleepingIgnored() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get sleeping ignored off region"); // Canvas - aggressive thread checks
         return this.getHandle().fauxSleeping;
     }
 
     @Override
     public Location getRespawnLocation(final boolean loadLocationAndValidate) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access respawn location off region"); // Canvas - aggressive thread checks
         final ServerPlayer.RespawnConfig respawnConfig = this.getHandle().getRespawnConfig();
         if (respawnConfig == null) return null;
         final LevelData.RespawnData respawnData = respawnConfig.respawnData();
@@ -1428,6 +1462,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setRespawnLocation(Location location, boolean override) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set respawn location off region"); // Canvas - aggressive thread checks
         if (location == null) {
             this.getHandle().setRespawnPosition(null, false, PlayerSetSpawnEvent.Cause.PLUGIN);
         } else {
@@ -1455,11 +1490,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public Input getCurrentInput() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access player current input off region"); // Canvas - aggressive thread checks
         return new CraftInput(this.getHandle().getLastClientInput());
     }
 
     @Override
     public Location getBedLocation() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access bed location off region"); // Canvas - aggressive thread checks
         Preconditions.checkState(this.isSleeping(), "Not sleeping");
 
         BlockPos bed = this.getHandle().getRespawnConfig().respawnData().pos();
@@ -1468,12 +1505,14 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public boolean hasDiscoveredRecipe(NamespacedKey recipe) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access recipe book off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(recipe != null, "recipe cannot be null");
         return this.getHandle().getRecipeBook().contains(CraftNamespacedKey.toResourceKey(Registries.RECIPE, recipe));
     }
 
     @Override
     public Set<NamespacedKey> getDiscoveredRecipes() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access recipe book off region"); // Canvas - aggressive thread checks
         ImmutableSet.Builder<NamespacedKey> bukkitRecipeKeys = ImmutableSet.builder();
         this.getHandle().getRecipeBook().known.forEach(key -> bukkitRecipeKeys.add(CraftNamespacedKey.fromMinecraft(key.identifier())));
         return bukkitRecipeKeys.build();
@@ -1481,96 +1520,115 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void incrementStatistic(Statistic statistic) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.incrementStatistic(this.getHandle().getStats(), statistic, this.getHandle());
     }
 
     @Override
     public void decrementStatistic(Statistic statistic) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.decrementStatistic(this.getHandle().getStats(), statistic, this.getHandle());
     }
 
     @Override
     public int getStatistic(Statistic statistic) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get statistic off region"); // Canvas - aggressive thread checks
         return CraftStatistic.getStatistic(this.getHandle().getStats(), statistic);
     }
 
     @Override
     public void incrementStatistic(Statistic statistic, int amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.incrementStatistic(this.getHandle().getStats(), statistic, amount, this.getHandle());
     }
 
     @Override
     public void decrementStatistic(Statistic statistic, int amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.decrementStatistic(this.getHandle().getStats(), statistic, amount, this.getHandle());
     }
 
     @Override
     public void setStatistic(Statistic statistic, int newValue) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.setStatistic(this.getHandle().getStats(), statistic, newValue, this.getHandle());
     }
 
     @Override
     public void incrementStatistic(Statistic statistic, Material material) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.incrementStatistic(this.getHandle().getStats(), statistic, material, this.getHandle());
     }
 
     @Override
     public void decrementStatistic(Statistic statistic, Material material) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.decrementStatistic(this.getHandle().getStats(), statistic, material, this.getHandle());
     }
 
     @Override
     public int getStatistic(Statistic statistic, Material material) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get statistic off region"); // Canvas - aggressive thread checks
         return CraftStatistic.getStatistic(this.getHandle().getStats(), statistic, material);
     }
 
     @Override
     public void incrementStatistic(Statistic statistic, Material material, int amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.incrementStatistic(this.getHandle().getStats(), statistic, material, amount, this.getHandle());
     }
 
     @Override
     public void decrementStatistic(Statistic statistic, Material material, int amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.decrementStatistic(this.getHandle().getStats(), statistic, material, amount, this.getHandle());
     }
 
     @Override
     public void setStatistic(Statistic statistic, Material material, int newValue) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.setStatistic(this.getHandle().getStats(), statistic, material, newValue, this.getHandle());
     }
 
     @Override
     public void incrementStatistic(Statistic statistic, EntityType entityType) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.incrementStatistic(this.getHandle().getStats(), statistic, entityType, this.getHandle());
     }
 
     @Override
     public void decrementStatistic(Statistic statistic, EntityType entityType) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.decrementStatistic(this.getHandle().getStats(), statistic, entityType, this.getHandle());
     }
 
     @Override
     public int getStatistic(Statistic statistic, EntityType entityType) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get statistic off region"); // Canvas - aggressive thread checks
         return CraftStatistic.getStatistic(this.getHandle().getStats(), statistic, entityType);
     }
 
     @Override
     public void incrementStatistic(Statistic statistic, EntityType entityType, int amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.incrementStatistic(this.getHandle().getStats(), statistic, entityType, amount, this.getHandle());
     }
 
     @Override
     public void decrementStatistic(Statistic statistic, EntityType entityType, int amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.decrementStatistic(this.getHandle().getStats(), statistic, entityType, amount, this.getHandle());
     }
 
     @Override
     public void setStatistic(Statistic statistic, EntityType entityType, int newValue) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot modify statistic off region"); // Canvas - aggressive thread checks
         CraftStatistic.setStatistic(this.getHandle().getStats(), statistic, entityType, newValue, this.getHandle());
     }
 
     @Override
     public void setPlayerTime(long time, boolean tickTime) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player time off region"); // Canvas - aggressive thread checks
         this.getHandle().timeOffset = time;
         this.getHandle().relativeTime = tickTime;
 
@@ -1584,46 +1642,55 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public long getPlayerTimeOffset() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player time offset off region"); // Canvas - aggressive thread checks
         return this.getHandle().timeOffset;
     }
 
     @Override
     public long getPlayerTime() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player time off region"); // Canvas - aggressive thread checks
         return this.getHandle().getPlayerTime();
     }
 
     @Override
     public boolean isPlayerTimeRelative() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if player time is relative off region"); // Canvas - aggressive thread checks
         return this.getHandle().relativeTime;
     }
 
     @Override
     public void resetPlayerTime() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot reset player time off region"); // Canvas - aggressive thread checks
         this.setPlayerTime(0, true);
     }
 
     @Override
     public void setPlayerWeather(WeatherType type) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player weather off region"); // Canvas - aggressive thread checks
         this.getHandle().setPlayerWeather(type, true);
     }
 
     @Override
     public WeatherType getPlayerWeather() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player weather off region"); // Canvas - aggressive thread checks
         return this.getHandle().weatherType;
     }
 
     @Override
     public int getExpCooldown() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get xp cooldown off region"); // Canvas - aggressive thread checks
         return this.getHandle().takeXpDelay;
     }
 
     @Override
     public void setExpCooldown(int ticks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set xp cooldown off region"); // Canvas - aggressive thread checks
         this.getHandle().takeXpDelay = CraftEventFactory.callPlayerXpCooldownEvent(this.getHandle(), ticks, PlayerExpCooldownChangeEvent.ChangeReason.PLUGIN).getNewCooldown();
     }
 
     @Override
     public void resetPlayerWeather() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot reset player weather off region"); // Canvas - aggressive thread checks
         this.getHandle().resetPlayerWeather();
     }
 
@@ -1704,17 +1771,20 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     public void setGameMode(GameMode mode) {
         Preconditions.checkArgument(mode != null, "GameMode cannot be null");
         if (this.getHandle().connection == null) return;
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set gamemode off region"); // Canvas - aggressive thread checks
 
         this.getHandle().setGameMode(GameType.byId(mode.getValue()), org.bukkit.event.player.PlayerGameModeChangeEvent.Cause.PLUGIN, null); // Paper - Expand PlayerGameModeChangeEvent
     }
 
     @Override
     public GameMode getGameMode() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get gamemode off region"); // Canvas - aggressive thread checks
         return GameMode.getByValue(this.getHandle().gameMode.getGameModeForPlayer().getId());
     }
 
     @Override
     public GameMode getPreviousGameMode() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get previous gamemode off region"); // Canvas - aggressive thread checks
         GameType previousGameMode = this.getHandle().gameMode.getPreviousGameModeForPlayer();
 
         return (previousGameMode == null) ? null : GameMode.getByValue(previousGameMode.getId());
@@ -1722,6 +1792,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int applyMending(int amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot apply mending to player off region"); // Canvas - aggressive thread checks
         ServerPlayer handle = this.getHandle();
         // Logic copied from ExperienceOrb#repairPlayerItems
 
@@ -1752,6 +1823,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void giveExp(int exp, boolean applyMending) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot give player exp off region"); // Canvas - aggressive thread checks
         if (applyMending) {
             exp = this.applyMending(exp);
         }
@@ -1760,16 +1832,19 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void giveExpLevels(int levels) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot give player exp levels off region"); // Canvas - aggressive thread checks
         this.getHandle().giveExperienceLevels(levels);
     }
 
     @Override
     public float getExp() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access player exp off region"); // Canvas - aggressive thread checks
         return this.getHandle().experienceProgress;
     }
 
     @Override
     public void setExp(float exp) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player exp off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(exp >= 0.0 && exp <= 1.0, "Experience progress must be between 0.0 and 1.0 (%s)", exp);
         this.getHandle().experienceProgress = exp;
         this.getHandle().lastSentExp = -1;
@@ -1777,11 +1852,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int getLevel() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get player exp level off region"); // Canvas - aggressive thread checks
         return this.getHandle().experienceLevel;
     }
 
     @Override
     public void setLevel(int level) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player exp level off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(level >= 0, "Experience level must not be negative (%s)", level);
         this.getHandle().experienceLevel = level;
         this.getHandle().lastSentExp = -1;
@@ -1789,11 +1866,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int getTotalExperience() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get total player experience off region"); // Canvas - aggressive thread checks
         return this.getHandle().totalExperience;
     }
 
     @Override
     public void setTotalExperience(int exp) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set total player experience off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(exp >= 0, "Total experience points must not be negative (%s)", exp);
         this.getHandle().totalExperience = exp;
     }
@@ -1805,6 +1884,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setExperienceLevelAndProgress(final int totalExperience) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set exp level and progress off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(totalExperience >= 0, "Total experience points must not be negative (%s)", totalExperience);
         int level = calculateLevelsForExperiencePoints(totalExperience);
         int remainingPoints = totalExperience - calculateTotalExperiencePoints(level);
@@ -1816,6 +1896,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int getExperiencePointsNeededForNextLevel() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot access points needed for next exp level off region"); // Canvas - aggressive thread checks
         return this.getHandle().getXpNeededForNextLevel();
     }
 
@@ -1866,11 +1947,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     @Override
     @Deprecated
     public void hidePlayer(Player player) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot hide player off region"); // Canvas - aggressive thread checks
         this.hideEntity0(null, player);
     }
 
     @Override
     public void hideEntity(Plugin plugin, org.bukkit.entity.Entity entity) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot hide entity off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(plugin != null, "Plugin cannot be null");
         Preconditions.checkArgument(plugin.isEnabled(), "Plugin (%s) cannot be disabled", plugin.getName());
 
@@ -1962,11 +2045,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     @Override
     @Deprecated
     public void showPlayer(Player player) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot show player off region"); // Canvas - aggressive thread checks
         this.showEntity0(null, player);
     }
 
     @Override
     public void showEntity(Plugin plugin, org.bukkit.entity.Entity entity) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot show entity off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(plugin != null, "Plugin cannot be null");
         // Don't require that plugin be enabled. A plugin must be allowed to call
         // showPlayer during its onDisable() method.
@@ -2041,6 +2126,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     // Paper start
     @Override
     public void setPlayerProfile(com.destroystokyo.paper.profile.PlayerProfile profile) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set player profile off region"); // Canvas - aggressive thread checks
         ServerPlayer self = this.getHandle();
         GameProfile gameProfile = com.destroystokyo.paper.profile.CraftPlayerProfile.asAuthlibCopy(profile);
         if (!self.sentListPacket) {
@@ -2140,11 +2226,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     // Paper start - Add Listing API for Player
     @Override
     public boolean isListed(Player other) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if other player is listed off region"); // Canvas - aggressive thread checks
         return !this.unlistedEntities.contains(other.getUniqueId());
     }
 
     @Override
     public boolean unlistPlayer(@NonNull Player other) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot unlist other player off region"); // Canvas - aggressive thread checks
         Preconditions.checkNotNull(other, "hidden entity cannot be null");
         if (this.getHandle().connection == null) return false;
         if (!this.canSee(other)) return false;
@@ -2159,6 +2247,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public boolean listPlayer(@NonNull Player other) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot list other player off region"); // Canvas - aggressive thread checks
         Preconditions.checkNotNull(other, "hidden entity cannot be null");
         if (this.getHandle().connection == null) return false;
         if (!this.canSee(other)) throw new IllegalStateException("Player cannot see other player");
@@ -2174,6 +2263,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public Map<String, Object> serialize() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot serialize player off region"); // Canvas - aggressive thread checks
         Map<String, Object> result = new LinkedHashMap<>();
 
         result.put("name", this.getName());
@@ -2188,31 +2278,37 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public long getFirstPlayed() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get first played off region"); // Canvas - aggressive thread checks
         return this.firstPlayed;
     }
 
     @Override
     public long getLastPlayed() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get last played off region"); // Canvas - aggressive thread checks
         return this.lastPlayed;
     }
 
     @Override
     public boolean hasPlayedBefore() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if has played before off region"); // Canvas - aggressive thread checks
         return this.hasPlayedBefore;
     }
 
     public void setFirstPlayed(long firstPlayed) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set first played off region"); // Canvas - aggressive thread checks
         this.firstPlayed = firstPlayed;
     }
 
     // Paper start - getLastPlayed replacement API
     @Override
     public long getLastLogin() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get last login off region"); // Canvas - aggressive thread checks
         return this.getHandle().loginTime;
     }
 
     @Override
     public long getLastSeen() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get last seen off region"); // Canvas - aggressive thread checks
         return this.isOnline() ? System.currentTimeMillis() : this.lastSaveTime;
     }
     // Paper end - getLastPlayed replacement API
@@ -2255,26 +2351,31 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public boolean beginConversation(Conversation conversation) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot begin conversation off region"); // Canvas - aggressive thread checks
         return this.conversationTracker.beginConversation(conversation);
     }
 
     @Override
     public void abandonConversation(Conversation conversation) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot abandon conversation off region"); // Canvas - aggressive thread checks
         this.conversationTracker.abandonConversation(conversation, new ConversationAbandonedEvent(conversation, new ManuallyAbandonedConversationCanceller()));
     }
 
     @Override
     public void abandonConversation(Conversation conversation, ConversationAbandonedEvent details) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot abandon conversation off region"); // Canvas - aggressive thread checks
         this.conversationTracker.abandonConversation(conversation, details);
     }
 
     @Override
     public void acceptConversationInput(String input) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot accept conversation input off region"); // Canvas - aggressive thread checks
         this.conversationTracker.acceptConversationInput(input);
     }
 
     @Override
     public boolean isConversing() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if conversing off region"); // Canvas - aggressive thread checks
         return this.conversationTracker.isConversing();
     }
 
@@ -2457,26 +2558,31 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setMetadata(String metadataKey, MetadataValue newMetadataValue) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set metadata off region"); // Canvas - aggressive thread checks
         this.server.getPlayerMetadata().setMetadata(this, metadataKey, newMetadataValue);
     }
 
     @Override
     public List<MetadataValue> getMetadata(String metadataKey) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get metadata off region"); // Canvas - aggressive thread checks
         return this.server.getPlayerMetadata().getMetadata(this, metadataKey);
     }
 
     @Override
     public boolean hasMetadata(String metadataKey) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if has metadata off region"); // Canvas - aggressive thread checks
         return this.server.getPlayerMetadata().hasMetadata(this, metadataKey);
     }
 
     @Override
     public void removeMetadata(String metadataKey, Plugin owningPlugin) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot remove metadata off region"); // Canvas - aggressive thread checks
         this.server.getPlayerMetadata().removeMetadata(this, metadataKey, owningPlugin);
     }
 
     @Override
     public boolean setWindowProperty(Property prop, int value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set window property off region"); // Canvas - aggressive thread checks
         AbstractContainerMenu container = this.getHandle().containerMenu;
         if (container.getBukkitView().getType() != prop.getType()) {
             return false;
@@ -2492,11 +2598,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public boolean isFlying() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if flying off region"); // Canvas - aggressive thread checks
         return this.getHandle().getAbilities().flying;
     }
 
     @Override
     public void setFlying(boolean value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set flying flag off region"); // Canvas - aggressive thread checks
         boolean needsUpdate = getHandle().getAbilities().flying != value; // Paper - Only refresh abilities if needed
         if (!this.getAllowFlight()) {
             Preconditions.checkArgument(!value, "Player is not allowed to fly (check #getAllowFlight())");
@@ -2508,11 +2616,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public boolean getAllowFlight() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if flight is allowed off region"); // Canvas - aggressive thread checks
         return this.getHandle().getAbilities().mayfly;
     }
 
     @Override
     public void setAllowFlight(boolean value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set if flight is allowed off region"); // Canvas - aggressive thread checks
         if (this.isFlying() && !value) {
             this.getHandle().getAbilities().flying = false;
         }
@@ -2524,17 +2634,20 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     // Paper start - flying fall damage
     @Override
     public void setFlyingFallDamage(@NonNull TriState flyingFallDamage) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set has flying fall damage off region"); // Canvas - aggressive thread checks
         getHandle().flyingFallDamage = flyingFallDamage;
     }
 
     @Override
     public @NonNull TriState hasFlyingFallDamage() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get has flying fall damage off region"); // Canvas - aggressive thread checks
         return getHandle().flyingFallDamage;
     }
     // Paper end - flying fall damage
 
     @Override
     public void setFlySpeed(float value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set fly speed off region"); // Canvas - aggressive thread checks
         this.validateSpeed(value);
         ServerPlayer player = this.getHandle();
         player.getAbilities().flyingSpeed = value / 2f;
@@ -2544,6 +2657,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setWalkSpeed(float value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set walk speed off region"); // Canvas - aggressive thread checks
         this.validateSpeed(value);
         ServerPlayer player = this.getHandle();
         player.getAbilities().walkingSpeed = value / 2f;
@@ -2553,11 +2667,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public float getFlySpeed() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get fly speed off region"); // Canvas - aggressive thread checks
         return this.getHandle().getAbilities().flyingSpeed * 2f;
     }
 
     @Override
     public float getWalkSpeed() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get walk speed off region"); // Canvas - aggressive thread checks
         return this.getHandle().getAbilities().walkingSpeed * 2f;
     }
 
@@ -2567,6 +2683,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setMaxHealth(double amount) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set max health off region"); // Canvas - aggressive thread checks
         super.setMaxHealth(amount);
         this.health = Math.min(this.health, amount);
         this.getHandle().resetSentInfo();
@@ -2574,25 +2691,30 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void resetMaxHealth() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot reset max health off region"); // Canvas - aggressive thread checks
         super.resetMaxHealth();
         this.getHandle().resetSentInfo();
     }
 
     @Override
     public CraftScoreboard getScoreboard() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get scoreboard off region"); // Canvas - aggressive thread checks
         return this.server.getScoreboardManager().getPlayerBoard(this);
     }
 
     public @Nullable CraftScoreboard getScoreboardOverride() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get scoreboard override off region"); // Canvas - aggressive thread checks
         return this.scoreboardOverride;
     }
 
     public void setScoreboardOverride(@Nullable CraftScoreboard scoreboardOverride) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set scoreboard override off region"); // Canvas - aggressive thread checks
         this.scoreboardOverride = scoreboardOverride;
     }
 
     @Override
     public void setScoreboard(Scoreboard scoreboard) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set scoreboard off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(scoreboard != null, "Scoreboard cannot be null");
         Preconditions.checkState(this.getHandle().connection != null, "Cannot set scoreboard yet (invalid player connection)");
         if (!(scoreboard instanceof CraftScoreboard craftScoreboard)) {
@@ -2604,6 +2726,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setHealthScale(double value) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set health scale off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(value > 0F, "Health value (%s) must be greater than 0", value);
         this.healthScale = value;
         this.scaledHealth = true;
@@ -2617,6 +2740,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setHealthScaled(boolean scale) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set if health scaled off region"); // Canvas - aggressive thread checks
         if (this.scaledHealth != (this.scaledHealth = scale)) {
             this.updateScaledHealth();
         }
@@ -2624,10 +2748,12 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public boolean isHealthScaled() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get if health scaled off region"); // Canvas - aggressive thread checks
         return this.scaledHealth;
     }
 
     public float getScaledHealth() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get scaled health off region"); // Canvas - aggressive thread checks
         return (float) (this.isHealthScaled() ? this.getHealth() * this.getHealthScale() / this.getMaxHealth() : this.getHealth());
     }
 
@@ -2665,6 +2791,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void sendHealthUpdate() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot send health updates off region"); // Canvas - aggressive thread checks
         FoodData foodData = this.getHandle().getFoodData();
         // Paper start - cancellable death event
         ClientboundSetHealthPacket packet = new ClientboundSetHealthPacket(this.getScaledHealth(), foodData.getFoodLevel(), foodData.getSaturationLevel());
@@ -2704,12 +2831,14 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public org.bukkit.entity.Entity getSpectatorTarget() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get spectator target off region"); // Canvas - aggressive thread checks
         Entity followed = this.getHandle().getCamera();
         return followed == this.getHandle() ? null : followed.getBukkitEntity();
     }
 
     @Override
     public void setSpectatorTarget(org.bukkit.entity.Entity entity) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set spectator target off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(this.getGameMode() == GameMode.SPECTATOR, "Player must be in spectator mode");
         this.getHandle().setCamera((entity == null) ? null : ((CraftEntity) entity).getHandle());
     }
@@ -2749,6 +2878,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public org.bukkit.advancement.AdvancementProgress getAdvancementProgress(org.bukkit.advancement.Advancement advancement) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get advancement progress off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(advancement != null, "advancement");
 
         CraftAdvancement craft = (CraftAdvancement) advancement;
@@ -2760,12 +2890,14 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int getClientViewDistance() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get client view distance off region"); // Canvas - aggressive thread checks
         return (this.getHandle().requestedViewDistance() == 0) ? Bukkit.getViewDistance() : this.getHandle().requestedViewDistance();
     }
 
     // Paper start
     @Override
     public java.util.Locale locale() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get locale off region"); // Canvas - aggressive thread checks
         return getHandle().adventure$locale;
     }
     // Paper end
@@ -2778,6 +2910,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public String getLocale() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get locale off region"); // Canvas - aggressive thread checks
         // Paper start - Locale change event
         final String locale = this.getHandle().language;
         return locale != null ? locale : "en_us";
@@ -2786,11 +2919,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     // Paper start
     public void setAffectsSpawning(boolean affects) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set affects spawning off region"); // Canvas - aggressive thread checks
         this.getHandle().affectsSpawning = affects;
     }
 
     @Override
     public boolean getAffectsSpawning() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get affects spawning off region"); // Canvas - aggressive thread checks
         return this.getHandle().affectsSpawning;
     }
     // Paper end
@@ -2804,6 +2939,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void openSign(@NonNull Sign sign, @NonNull Side side) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open sign off region"); // Canvas - aggressive thread checks
         CraftSign.openSign(sign, this, side);
     }
 
@@ -2829,11 +2965,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     // Paper start
     @Override
     public net.kyori.adventure.text.Component displayName() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get display name off region"); // Canvas - aggressive thread checks
         return this.getHandle().adventure$displayName;
     }
 
     @Override
     public void displayName(final net.kyori.adventure.text.Component displayName) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set display name off region"); // Canvas - aggressive thread checks
         this.getHandle().adventure$displayName = displayName != null ? displayName : net.kyori.adventure.text.Component.text(this.getName());
         this.getHandle().displayName = null;
     }
@@ -2841,6 +2979,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     @Override
     public void deleteMessage(net.kyori.adventure.chat.SignedMessage.Signature signature) {
         if (getHandle().connection == null) return;
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot delete message off region"); // Canvas - aggressive thread checks
         net.minecraft.network.chat.MessageSignature sig = new net.minecraft.network.chat.MessageSignature(signature.bytes());
 
         this.getHandle().connection.send(new net.minecraft.network.protocol.game.ClientboundDeleteChatPacket(new net.minecraft.network.chat.MessageSignature.Packed(sig)));
@@ -2905,18 +3044,21 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void sendPlayerListHeader(final net.kyori.adventure.text.Component header) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot send playerlist header off region"); // Canvas - aggressive thread checks
         this.playerListHeader = header;
         this.adventure$sendPlayerListHeaderAndFooter();
     }
 
     @Override
     public void sendPlayerListFooter(final net.kyori.adventure.text.Component footer) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot send playerlist footer off region"); // Canvas - aggressive thread checks
         this.playerListFooter = footer;
         this.adventure$sendPlayerListHeaderAndFooter();
     }
 
     @Override
     public void sendPlayerListHeaderAndFooter(final net.kyori.adventure.text.Component header, final net.kyori.adventure.text.Component footer) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot send playerlist header/footer off region"); // Canvas - aggressive thread checks
         this.playerListHeader = header;
         this.playerListFooter = footer;
         this.adventure$sendPlayerListHeaderAndFooter();
@@ -2981,6 +3123,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public @NonNull Iterable<? extends net.kyori.adventure.bossbar.BossBar> activeBossBars() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get active bossbars off region"); // Canvas - aggressive thread checks
         if (this.activeBossBars != null) {
             return java.util.Collections.unmodifiableSet(this.activeBossBars);
         }
@@ -2989,6 +3132,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void showBossBar(final net.kyori.adventure.bossbar.BossBar bar) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot show boss bars off region"); // Canvas - aggressive thread checks
         net.kyori.adventure.bossbar.BossBarImplementation.get(bar, io.papermc.paper.adventure.BossBarImplementationImpl.class).playerShow(this);
         if (this.activeBossBars == null) {
             this.activeBossBars = new HashSet<>();
@@ -2998,6 +3142,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void hideBossBar(final net.kyori.adventure.bossbar.BossBar bar) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot hide boss bars off region"); // Canvas - aggressive thread checks
         net.kyori.adventure.bossbar.BossBarImplementation.get(bar, io.papermc.paper.adventure.BossBarImplementationImpl.class).playerHide(this);
         if (this.activeBossBars != null) {
             this.activeBossBars.remove(bar);
@@ -3043,6 +3188,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     public void openBook(ItemStack book) {
         Preconditions.checkArgument(book != null, "ItemStack cannot be null");
         Preconditions.checkArgument(book.hasData(DataComponentTypes.WRITTEN_BOOK_CONTENT), "ItemStack must have a 'written_book_content' component");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open book off region"); // Canvas - aggressive thread checks
 
         net.minecraft.world.item.ItemStack bookItem = CraftItemStack.asNMSCopy(book);
         ServerPlayer serverPlayer = this.getHandle();
@@ -3052,6 +3198,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void openBook(final Book book) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot open book off region"); // Canvas - aggressive thread checks
         final ItemStack mutatedItem = ItemType.WRITTEN_BOOK.createItemStack(); // dummy item
         mutatedItem.setData(DataComponentTypes.WRITTEN_BOOK_CONTENT, WrittenBookContent.writtenBookContent("", "").addPages(book.pages()));
         sendBookOpen(CraftItemStack.unwrap(mutatedItem));
@@ -3076,16 +3223,19 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public float getCooldownPeriod() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get item cooldown period off region"); // Canvas - aggressive thread checks
         return getHandle().getCurrentItemAttackStrengthDelay();
     }
 
     @Override
     public float getCooledAttackStrength(float adjustTicks) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get cooled attack strength off region"); // Canvas - aggressive thread checks
         return getHandle().getAttackStrengthScale(adjustTicks);
     }
 
     @Override
     public void resetCooldown() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot reset cooldown off region"); // Canvas - aggressive thread checks
         getHandle().resetAttackStrengthTicker();
     }
     // Paper end
@@ -3181,36 +3331,43 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int getWardenWarningCooldown() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get warden warning cooldown off region"); // Canvas - aggressive thread checks
         return this.getHandle().wardenSpawnTracker.cooldownTicks;
     }
 
     @Override
     public void setWardenWarningCooldown(int cooldown) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set warden warning cooldown off region"); // Canvas - aggressive thread checks
         this.getHandle().wardenSpawnTracker.cooldownTicks = Math.max(cooldown, 0);
     }
 
     @Override
     public int getWardenTimeSinceLastWarning() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get warden time since last warning off region"); // Canvas - aggressive thread checks
         return this.getHandle().wardenSpawnTracker.ticksSinceLastWarning;
     }
 
     @Override
     public void setWardenTimeSinceLastWarning(int time) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set warden time since last warning off region"); // Canvas - aggressive thread checks
         this.getHandle().wardenSpawnTracker.ticksSinceLastWarning = time;
     }
 
     @Override
     public int getWardenWarningLevel() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get warden warning level off region"); // Canvas - aggressive thread checks
         return this.getHandle().wardenSpawnTracker.getWarningLevel();
     }
 
     @Override
     public void setWardenWarningLevel(int warningLevel) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set warden warning level off region"); // Canvas - aggressive thread checks
         this.getHandle().wardenSpawnTracker.setWarningLevel(warningLevel);
     }
 
     @Override
     public void increaseWardenWarningLevel() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set warden warning level off region"); // Canvas - aggressive thread checks
         this.getHandle().wardenSpawnTracker.increaseWarningLevel();
     }
     // Paper end
@@ -3218,11 +3375,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     // Paper start
     @Override
     public Duration getIdleDuration() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get idle duration off region"); // Canvas - aggressive thread checks
         return Duration.ofMillis(net.minecraft.util.Util.getMillis() - this.getHandle().getLastActionTime());
     }
 
     @Override
     public void resetIdleDuration() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot reset idle duration off region"); // Canvas - aggressive thread checks
         this.getHandle().resetLastActionTime();
     }
     // Paper end
@@ -3230,19 +3389,19 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
     // Paper start - Add chunk view API
     @Override
     public Set<java.lang.Long> getSentChunkKeys() {
-        org.spigotmc.AsyncCatcher.catchOp("accessing sent chunks");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get sent chunk keys off region"); // Canvas - aggressive thread checks
         return FeatureHooks.getSentChunkKeys(this.getHandle());
     }
 
     @Override
     public Set<org.bukkit.Chunk> getSentChunks() {
-        org.spigotmc.AsyncCatcher.catchOp("accessing sent chunks");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get sent chunks off region"); // Canvas - aggressive thread checks
         return FeatureHooks.getSentChunks(this.getHandle());
     }
 
     @Override
     public boolean isChunkSent(final long chunkKey) {
-        org.spigotmc.AsyncCatcher.catchOp("accessing sent chunks");
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot check if chunk was sent off region"); // Canvas - aggressive thread checks
         return FeatureHooks.isChunkSent(this.getHandle(), chunkKey);
     }
     // Paper end
@@ -3254,31 +3413,37 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int getViewDistance() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get view distance off region"); // Canvas - aggressive thread checks
         return ca.spottedleaf.moonrise.common.PlatformHooks.get().getViewDistance(this.getHandle());
     }
 
     @Override
     public void setViewDistance(final int viewDistance) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set view distance off region"); // Canvas - aggressive thread checks
         FeatureHooks.setViewDistance(this.getHandle(), viewDistance); // Paper - chunk system
     }
 
     @Override
     public int getSimulationDistance() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get simulation distance off region"); // Canvas - aggressive thread checks
         return ca.spottedleaf.moonrise.common.PlatformHooks.get().getTickViewDistance(this.getHandle());
     }
 
     @Override
     public void setSimulationDistance(final int simulationDistance) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set simulation distance off region"); // Canvas - aggressive thread checks
         FeatureHooks.setSimulationDistance(this.getHandle(), simulationDistance); // Paper - chunk system
     }
 
     @Override
     public int getSendViewDistance() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get send view distance off region"); // Canvas - aggressive thread checks
         return ca.spottedleaf.moonrise.common.PlatformHooks.get().getSendViewDistance(this.getHandle());
     }
 
     @Override
     public void setSendViewDistance(final int viewDistance) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set send view distance off region"); // Canvas - aggressive thread checks
         FeatureHooks.setSendViewDistance(this.getHandle(), viewDistance); // Paper - chunk system
     }
 
@@ -3295,6 +3460,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public @NonNull PlayerGiveResult give(final @NonNull Collection<@NonNull ItemStack> items, final boolean dropIfFull) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot give player items off region"); // Canvas - aggressive thread checks
         Preconditions.checkArgument(items != null, "items cannot be null");
         if (items.isEmpty()) return PaperPlayerGiveResult.EMPTY; // Early opt out for empty input.
 
@@ -3326,6 +3492,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public float getSidewaysMovement() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get sideways movement off region"); // Canvas - aggressive thread checks
         final boolean leftMovement = this.getHandle().getLastClientInput().left();
         final boolean rightMovement = this.getHandle().getLastClientInput().right();
 
@@ -3334,6 +3501,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public float getForwardsMovement() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get forwards movement off region"); // Canvas - aggressive thread checks
         final boolean forwardMovement = this.getHandle().getLastClientInput().forward();
         final boolean backwardMovement = this.getHandle().getLastClientInput().backward();
 
@@ -3342,11 +3510,13 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public int getDeathScreenScore() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get death screen score off region"); // Canvas - aggressive thread checks
         return getHandle().getScore();
     }
 
     @Override
     public void setDeathScreenScore(final int score) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set death screen score off region"); // Canvas - aggressive thread checks
         getHandle().setScore(score);
     }
 
@@ -3357,6 +3527,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public org.bukkit.entity.Entity releaseLeftShoulderEntity() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot release left shoulder entity off region"); // Canvas - aggressive thread checks
         if (!getHandle().getShoulderEntityLeft().isEmpty()) {
             Entity entity = getHandle().releaseLeftShoulderEntity();
             if (entity != null) {
@@ -3369,6 +3540,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public org.bukkit.entity.Entity releaseRightShoulderEntity() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot release right shoulder entity off region"); // Canvas - aggressive thread checks
         if (!getHandle().getShoulderEntityRight().isEmpty()) {
             Entity entity = getHandle().releaseRightShoulderEntity();
             if (entity != null) {
@@ -3381,6 +3553,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public org.bukkit.entity.Entity getShoulderEntityLeft() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get left shoulder entity off region"); // Canvas - aggressive thread checks
         if (!this.getHandle().getShoulderEntityLeft().isEmpty()) {
             try (ProblemReporter.ScopedCollector scopedCollector = new ProblemReporter.ScopedCollector(this.getHandle().problemPath(), LOGGER)) {
                 return net.minecraft.world.entity.EntityType.create(
@@ -3396,6 +3569,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setShoulderEntityLeft(org.bukkit.entity.Entity entity) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set left shoulder entity off region"); // Canvas - aggressive thread checks
         if (entity != null) {
             Preconditions.checkArgument(((CraftEntity) entity).getHandle().getType().canSerialize(), "Cannot set entity of type %s as a shoulder entity", entity.getType().getKey());
         }
@@ -3407,6 +3581,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public org.bukkit.entity.Entity getShoulderEntityRight() {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot get right shoulder entity off region"); // Canvas - aggressive thread checks
         if (!this.getHandle().getShoulderEntityRight().isEmpty()) {
             try (ProblemReporter.ScopedCollector scopedCollector = new ProblemReporter.ScopedCollector(this.getHandle().problemPath(), LOGGER)) {
                 return net.minecraft.world.entity.EntityType.create(
@@ -3422,6 +3597,7 @@ public class CraftPlayer extends CraftHumanEntity implements Player, PluginMessa
 
     @Override
     public void setShoulderEntityRight(org.bukkit.entity.Entity entity) {
+        ca.spottedleaf.moonrise.common.util.TickThread.canvas$tryEnsureThreadCheck(this.entity, "Cannot set right shoulder entity off region"); // Canvas - aggressive thread checks
         if (entity != null) {
             Preconditions.checkArgument(((CraftEntity) entity).getHandle().getType().canSerialize(), "Cannot set entity of type %s as a shoulder entity", entity.getType().getKey());
         }
