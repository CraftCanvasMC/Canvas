/**
 * This package is specifically for the ticket system, where we can propagate data to holding types to initiate
 * execution of certain functions asynchronously with the ability to provide custom data in the ticket type too
 */
@NullMarked
package io.canvasmc.canvas.util.ticket;

import org.jspecify.annotations.NullMarked;
