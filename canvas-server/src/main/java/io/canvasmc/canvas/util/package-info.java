/**
 * Canvas utility sources. All utilities should be properly annotated and have written javadocs for necessary methods,
 * fields, and class headers. There are of course exceptions to requirements of JDs, but most should be properly
 * documented unless genuinely not necessary to do so
 */
@NullMarked
// TODO - repackage
package io.canvasmc.canvas.util;

import org.jspecify.annotations.NullMarked;
