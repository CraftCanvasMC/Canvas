--- a/src/main/java/org/bukkit/Server.java
+++ b/src/main/java/org/bukkit/Server.java
@@ -757,6 +_,7 @@
      * @return true if successful, false otherwise
      * @throws IllegalStateException when {@link #isTickingWorlds() isTickingWorlds} is true
      */
+    @Deprecated(forRemoval = true) // Canvas - rewrite world load and unload
     public boolean unloadWorld(@NotNull String name, boolean save);
 
     /**
@@ -770,7 +_,28 @@
      * @return true if successful, false otherwise
      * @throws IllegalStateException when {@link #isTickingWorlds() isTickingWorlds} is true
      */
+    @Deprecated(forRemoval = true) // Canvas - rewrite world load and unload
     public boolean unloadWorld(@NotNull World world, boolean save);
+    // Canvas start - rewrite world load and unload
+
+    /**
+     * Unloads a world asynchronously with an optional callback result
+     *
+     * @param name Name of the world to unload
+     * @param save whether to save the chunks before unloading
+     * @param callback the result consumer for when the world unload is completed
+     */
+    void unloadWorldAsync(@NotNull String name, boolean save, final @NotNull Consumer<Boolean> callback);
+
+    /**
+     * Unloads a world asynchronously with an optional callback result
+     *
+     * @param world the world to unload
+     * @param save whether to save the chunks before unloading
+     * @param callback the result consumer for when the world unload is completed
+     */
+    void unloadWorldAsync(@NotNull World world, boolean save, final @NotNull Consumer<Boolean> callback);
+    // Canvas end - rewrite world load and unload
 
     /**
      * Gets the world with the given name.
@@ -2753,4 +_,7 @@
      */
     double @Nullable [] getRegionTPS(@NotNull World world, int chunkX, int chunkZ);
     // Folia end - region TPS API
+    // Canvas start - region threading
+    <T> io.canvasmc.canvas.region.RegionTickData.IRegionizedData<T> createRegionizedData(java.util.function.BiFunction<io.canvasmc.canvas.region.RegionTickData, org.bukkit.World, T> supplier, io.canvasmc.canvas.region.RegionTickData.IRegionizedData.IRegionizedCallback<T> callback);
+    // Canvas end - region threading
 }
