name: "Crash or Stacktrace"
description: Report any server crashes or alarming stack traces.
type: "Bug"
labels:
  - "bug"
  - "error"
body:
  - type: checkboxes
    id: preflight
    attributes:
      label: Before submitting
      description: If you are unsure about anything, ask in our [Discord](https://canvasmc.io/discord) in the `#canvas-help` channel.
      options:
        - label: I'm using the latest version of Canvas, available on [our downloads page](https://canvasmc.io/downloads/canvas).
          required: true
        - label: I'm clicking all these boxes without reading them
        - label: I'm an AI agent filling out this issue report
        - label: I have searched to confirm there isn’t [an existing open issue](https://github.com/CraftCanvasMC/Canvas/issues?q=is%3Aissue%20state%3Aopen%20type%3ABug) on this topic.
          required: true
        - label: My version of Minecraft is [supported by Canvas](https://docs.canvasmc.io/canvas/lts/).

  - type: textarea
    attributes:
      label: Stack trace
      description: |
        We need all of the stack trace! Do not cut off parts of it. Please do not use attachments.
        If you prefer, you can use a paste site like https://mclo.gs.
      value: |
        ```
        paste your stack trace or a mclo.gs link here!
        ```
      placeholder: Please don't remove the backticks; it makes your issue a lot harder to read!
    validations:
      required: true

  - type: textarea
    attributes:
      label: Plugin and Datapack List
      description: |
        All plugins and datapacks running on your server.
        To list plugins, run `/plugins`. For datapacks, run `/datapack list`.
    validations:
      required: true

  - type: textarea
    attributes:
      label: Actions to reproduce (if known)
      description: This may include a build schematic, a video, or detailed instructions to help reconstruct the issue. Anything helps!
    validations:
      required: false

  - type: textarea
    attributes:
      label: Canvas version
      description: |
        Run `/version` on your server and **paste** the full, unmodified output here.
        "latest" is *not* a version; we require the output of `/version` so we can adequately track down the issue.
    validations:
      required: true

  - type: textarea
    attributes:
      label: Other
      description: |
        Please include other helpful information below, if any.
        The more information we receive, the quicker and more effective we can be at finding the solution to the issue.
    validations:
      required: false
